---
titulo: "Solapes del capítulo 2 con los capítulos 4, 5 y 6"
tipo: informe de investigación
estado: "correcciones aplicadas el 2026-08-21 (7 de 8 fichas + C4 en remediación posterior); C8 desviada a decisión de Francisco; las 33 repeticiones compatibles siguen pendientes de su lectura"
fecha: 2026-08-21
---

# Solapes del capítulo 2 con los capítulos 4, 5 y 6

> [!note] Qué es esto y qué NO es
> Barrido **diagnóstico** del perímetro `02 Marco Teórico` ↔ `04 Implementación` / `05 Evaluación` /
> `06 Conclusiones`. Nació como inventario y **ya se ha ejecutado la fase de corrección**: la
> declaración original «no se ha editado ninguna nota de la memoria» **caducó el 2026-08-21**.
> Cada solape se clasifica en **dos cubos y solo dos**:
> - **CONTRADICCIÓN** = defecto. Se corrige. Es lo que hay que leer.
> - **REPETICIÓN COMPATIBLE** = lo mismo dicho dos veces de forma compatible. **No se toca.**
>   Decidir «cuál sobra» es decisión de Francisco, no del barrido.
>
> Las sedes se citan **por título de sección o fragmento literal**, nunca por `fichero:línea`.
> No se introduce ninguna cifra nueva: todas las que aparecen están ya publicadas en las notas.

---

## Estado a 2026-08-21 — qué se ejecutó y qué sigue esperándote

> [!important] Lo que se ha hecho ya, para que no se corrija dos veces
> El **cubo de contradicciones se ha aplicado sobre las notas de la memoria** el **2026-08-21**.
> El **cubo de repeticiones compatibles NO se ha tocado**: sigue íntegro y es lo que queda para tu
> lectura.

| Ficha | Estado a 2026-08-21 | Nota |
|---|---|---|
| **C1** | **Aplicada** | Callout de `5.0.6` acotado |
| **C1b** | **Aplicada** | Callouts de verificación pendiente añadidos en `2.1.6` y `2.3.3` |
| **C2** | **Aplicada** | Rótulo y «dos contrastes KS» corregidos en `2.1.6.8`. *Un segundo filo sobrevivía en `6.2.6`: ver apéndice* |
| **C3** | **Aplicada** | Las dos negaciones matizadas |
| **C4** | **Aplicada en la remediación posterior** | Se despachó tarde porque el resumen del barrido no la nombró. **Verificada en disco:** `2.2.5.1` remite hoy a `[[4.3 Preprocesamiento de los datasets\|§4.3.1]]` y mantiene `A.1` como segunda sede |
| **C5** | **Aplicada** | Subapartado nuevo en `5.2.2`. *El mecanismo de `probe` quedó mal explicado: ver apéndice* |
| **C6** | **Aplicada** | Remisión de `2.2.3.3` resuelta con sede |
| **C7** | **Aplicada** | Peldaño 1 de `6.2.6` ampliado; remisiones de `2.3.2.5` y `4.2` punto 5 con sede |
| **C8** | **NO aplicada — DESVIADA a tu decisión** | **No es un olvido.** Ver el bloque siguiente |

> [!warning] C8 no se ha aplicado a propósito
> La ficha proponía reducir la razón técnica del límite 5 de `6.1` § 6.1.4 y remitir a `5.3`. El
> propio informe la declaraba **la más discutible de las ocho**: leída con rigor es **repetición
> compatible** —las cuatro sedes dicen lo mismo y ninguna contradice a otra—, y en ese cubo
> **«cuál sobra» lo decides tú, no el barrido**. Por la regla dura del encargo —no resolver
> repeticiones compatibles— **`6.1.4` límite 5 se ha dejado intacta**. Si al leerla decides que
> sobra la razón técnica, la corrección propuesta sigue descrita en la ficha C8, lista para
> despachar.

> [!note] Sobre las sedes citadas con fragmentos literales
> Varias fichas citan **texto que las correcciones ya han sustituido**. Las citas **se conservan a
> propósito**, porque son la traza de cuál era el defecto, pero **corresponden al estado anterior a
> la corrección y no deben buscarse en disco**. Son estas: `5.0.6` «utiliza… de uno solo» ·
> `2.1.6.8` «Deriva de la distribución» · `2.3.3.2` «tampoco proyecta» · `2.2.3.3` «Ese salto se
> discute en 6.2» · `2.3.2.5` y `4.2` punto 5 (remisiones a `6.2` sin sede) · `5.2.2` «sin advertir
> que el orden se ha invertido».

---

## Resumen: tamaño de la lectura

| Cubo | 2↔4 | 2↔5 | 2↔6 | **Total** |
|---|---:|---:|---:|---:|
| **CONTRADICCIÓN** (se corrige) | 2 | 3 | 3 | **8** |
| **REPETICIÓN COMPATIBLE** (no se toca) | 15 | 10 | 8 | **33** |
| **Total de solapes inventariados** | 17 | 13 | 11 | **41** |

**Lectura corta:** el perímetro está **sano**. La disciplina de remisión del capítulo 2 («esto no se
reproduce aquí, véase X») funciona: 33 de los 41 solapes son repetición deliberada y declarada.
De las 8 contradicciones, **cinco son de una sola frase** (una etiqueta, un alcance, una remisión
mal apuntada) y **tres piden una frase nueva de reconciliación**, no un cambio de cifra.
**Ninguna contradicción afecta a ningún número publicado en `Resultados/`.**

Reparto de las 8 por naturaleza:

| Naturaleza | Cuántas | Fichas |
|---|---:|---|
| Alcance de una fuente declarado de dos maneras | 1 | C1 |
| Un fenómeno nombrado de dos maneras que se excluyen | 1 | C2 |
| Una afirmación de «no se hace X» que el propio trabajo sí hace | 1 | C3 |
| Remisión que apunta a un apartado que no tiene el contenido | 4 | C4, C6, C7, C8 |
| Predicción del capítulo 2 no reconciliada con la medición | 1 | C5 |

---

# Bloque 2↔4 — Marco Teórico ↔ Implementación

## Contradicciones

### C4 — El mapeo tipo → categoría se remite a `4.2`, y está en `4.3.1`

> **Estado: APLICADA en la remediación posterior (2026-08-21).** Se despachó tarde porque el resumen
> del barrido no la nombró. Verificado en disco: `2.2.5.1` remite hoy a `4.3` § 4.3.1 y conserva
> `A.1` como segunda sede. La cita literal de la fila «Sede 2» es del estado **anterior**.

| | |
|---|---|
| **Concepto** | Mapeo de los 39 tipos de ataque a las 4 categorías (+`normal`), 40 etiquetas, 5 clases |
| **Sede 2** | `2.2.5 Taxonomía de ataques y panorama de amenazas` § 2.2.5.1 — «El mapeo completo tipo → categoría es un artefacto del propio pipeline y **se documenta en [[4.2 Base de datos utilizada]] y en [[A.1 Columnas del dataset NSL-KDD]]**» |
| **Sede 4** | La tabla completa (`normal` 1 · `dos` 10 · `probe` 6 · `r2l` 15 · `u2r` 8) está en `4.3 Preprocesamiento de los datasets` § 4.3.1 «Adición y eliminación de etiquetas» |
| **Etiqueta** | **CONTRADICCIÓN** (remisión) |
| **Por qué lo es** | `4.2` **no contiene** el mapeo, y además desvía activamente: su callout «Trazabilidad — el 78,3 % de DoS en D3» dice «se publica en 4.3 (§4.3.6) y **no se reproduce aquí**». Un lector que siga la remisión de `2.2.5.1` no encuentra la tabla. |
| **Sede canónica propuesta** | **`4.3` § 4.3.1**, porque es donde vive la tabla, el fragmento de código de los dos `LabelEncoder` y la trazabilidad a `specialized_nsl_kdd_mappings_and_info.txt`. |
| **Corrección** | Reapuntar la remisión de `2.2.5.1` a `4.3` § 4.3.1 (manteniendo `A.1` como segunda sede). Una frase. |

> Ojo: la **otra** remisión de `2.2.5.1` sí es correcta — el «52 instancias … ratio ≈ 883:1
> documentado en [[4.2 Base de datos utilizada]]» está efectivamente en `4.2` § 4.2.1. No tocarla.

### C5 — «Las rejillas son de dos ejes por algoritmo» no vale para la etapa 1

> [!caution] Colisión de numeración preexistente, y estado propio
> Esta ficha **comparte el identificador C5** con «La jerarquía de dificultad predicha en `2.2.5.2`…»
> del bloque 2↔5. La numeración **no se renumera** (lo pide el encargo), así que se distinguen por
> título. La **C5 que la tabla de la lista corta da por aplicada es la de la jerarquía** (`5.2.2`).
> **Esta —la de los dos ejes de `2.1.6.4`— NO estaba aplicada al verificarla en disco el 2026-08-21**:
> la frase sigue diciendo «Las rejillas son de **dos ejes por algoritmo**». Está **en remediación en
> esta misma tanda** (`2.1.6` la está corrigiendo otro agente).

| | |
|---|---|
| **Concepto** | Tamaño y forma de las rejillas de búsqueda de hiperparámetros |
| **Sede 2** | `2.1.6 Metodologías y buenas prácticas` § 2.1.6.4 — «Las rejillas son de **dos ejes por algoritmo**, y sus valores concretos no se dan aquí» |
| **Sede 4** | `4.4 Entrenamiento del modelo de detección de anomalías` § «Algoritmos y espacios de búsqueda» — `LocalOutlierFactor`: **un solo eje** (`n_neighbors` ∈ {10, 20, 35, 50}); `Autoencoder (MLP)`: **un solo eje** (`hidden_layer_sizes`, 2 configuraciones) |
| **Etiqueta** | **CONTRADICCIÓN** (alcance) |
| **Por qué lo es** | La afirmación de `2.1.6.4` es universal («por algoritmo») y solo se cumple en la **etapa de firmas**, donde los cuatro sí tienen dos ejes (`4.5`: profundidad+hoja, árboles+profundidad, vecinos+pesos, tasa+iteraciones). El párrafo siguiente de `2.1.6.4` sí acota la etapa 1 («el mecanismo no puede ser el mismo»), pero la frase de los dos ejes va **antes** de esa acotación y se lee como general. |
| **Sede canónica propuesta** | **`4.4` y `4.5`** para la forma de cada rejilla (son las que la documentan por algoritmo, y `A.3` § A.3.3 para los valores). `2.1.6.4` conserva el **criterio** (rejilla pequeña y publicada), que es lo suyo. |
| **Corrección** | Acotar la frase de `2.1.6.4` a la etapa de firmas («las rejillas de la etapa de firmas son de dos ejes por algoritmo») o sustituir «dos ejes» por «uno o dos ejes». Una frase. |

## Repeticiones compatibles (2↔4) — NO se tocan

| # | Concepto | Sede en el capítulo 2 | Sede en el capítulo 4 | Sede canónica propuesta y por qué |
|---|---|---|---|---|
| R1 | Umbral percentil 95 ⇒ ~5 % de falsas alarmas por diseño | `2.2.4` § 2.2.4.2, «Correspondencia con el sistema construido» | `4.4` § «Protocolo…», punto 4 | **`4.4`**: es donde se declara el criterio idéntico para los cuatro y la salvedad de que la tasa efectiva difiere. `2.2.4.2` lo cita como correspondencia del paradigma |
| R2 | Fuga de información: las tres formas típicas | `2.1.6` § 2.1.6.5 (tabla de 3 casos) | `4.3.2` (caso 2), `4.3.3` (caso 3), `4.3.4` (caso 1), `4.3.7` (tabla de las 4 transformaciones) | **`2.1.6.5` el *qué*, `4.3.7` el *dónde*.** Ya está resuelto así de forma explícita: los cuatro apartados de `4.3` dicen «aquí no se reexplica» |
| R3 | Desbalance de D3: `u2r` 52 frente a `dos` 45.927, ratio ≈ 883:1 | `2.1.6.3`, `2.1.6.6`, `2.2.5` § 2.2.5.1 («U2R reúne exactamente 52 instancias») | `4.3` § 4.3.6 (tabla completa), `4.5` («52 frente a 45.927 … 0,09 %»), `4.2` § 4.2.1 (883,21:1) | **`4.3` § 4.3.6**: única sede con la tabla de las 4 categorías y su artefacto (`specialized_nsl_kdd_composicion_d3.csv`). **Cinco sedes para el mismo par de cifras** — es el solape más repetido del bloque |
| R4 | «El *accuracy* miente bajo desbalance» | `2.1.6` § 2.1.6.6 («responder siempre `dos` daría alrededor de un 78 % de exactitud») | `4.5` («un clasificador que nunca predijera `u2r` conservaría un accuracy superior al 99,9 %»), `4.3.6` («apenas perdería una milésima de accuracy») | **`2.1.6.6`**. **AVISO: el 78 % y el 99,9 % NO se contradicen** — son dos clasificadores hipotéticos distintos (siempre-`dos` vs nunca-`u2r`). No «arreglar» esto |
| R5 | SMOTE dentro de cada *fold*, con `ImbPipeline` | `2.1.6.2` (tabla de decisiones), `2.1.6.5` (caso 1) | `4.3` § 4.3.4 (código), `4.5` («aquí no se reexplica ninguna de las dos cosas») | **`4.3` § 4.3.4**: la materialización con el fragmento que la demuestra |
| R6 | Perímetro anti-fuga: las cinco decisiones y sobre qué se resuelve cada una | `2.1.6` § 2.1.6.2 (tabla de 5 filas) | `4.4` puntos 3-4, `4.5` («D2 es intocable para el balanceo y para el ajuste»), `5.3` (calibración OOF) | **`2.1.6.2`** como cuadro único; los capítulos 4 y 5 instancian filas suyas |
| R7 | **Las tres decisiones tomadas mirando D2** (*data snooping*) | `2.1.6` § 2.1.6.2, callout «Declaración: tres decisiones del sistema se tomaron mirando D2» | `4.3.5`, `4.3.7`, `4.6` · y en el cap. 5: `5.0.4`, `5.1.4`, `5.2.3`, `5.4` · y `6.1.3` | **`6.1.3`** (inventario) + **`4.3.5` § *Decisión experimental*** (cifras). **Nueve sedes con la misma formulación literal y la coletilla «el detalle no se reproduce aquí»: es la repetición más extensa de la memoria, y es DELIBERADA.** No es un defecto |
| R8 | El autoencoder es un MLP y su *score* es el MSE de reconstrucción | `2.1.4.3`, `2.3.1` § «El único componente neuronal…», `2.1.2.5` (callout) | `4.4` § «Algoritmos y espacios de búsqueda», tercer punto | **`2.3.1`** § «El único componente neuronal, y por qué no es "profundo"» — ya declarada canónica por `2.1.4.3` y por el cierre de `2.1.4` |
| R9 | Del árbol se extraen reglas legibles (`export_text`) | `2.1.4.1.1`, `2.2.4.1`, `2.3.1.2`, `2.3.3.4` | `4.5` § «Extracción de firmas legibles» | **`4.5`** para las reglas efectivas; **`2.3.1.2`** para el contraste reglas escritas/aprendidas. `2.3.3.4` y `2.3.1.2` ya dicen «no se repite aquí» |
| R10 | NSL-KDD es tabular: 41 características, 125.973 registros de train | `2.3.1` § 2.3.1.3 (1) | `4.2` § «Las 41 características y sus tres grupos» | **`4.2`**: `2.3.1.3` ya remite allí |
| R11 | 17 tipos 0-day / 3.750 filas / 29,2 % de los ataques de D2 | `2.1.6` § 2.1.6.8, punto 2 | `4.2` § 4.2.1 (con el callout «el "17": medido en disco, no citado»), `4.3.6` propiedad 1 | **`4.2` § 4.2.1**: única sede con la trazabilidad del recuento y la «vacuna» contra el 14 de KDD'99 |
| R12 | Reproducibilidad: semilla 42 + *pinning* + artefactos persistidos | `2.1.6` § 2.1.6.7 (tabla de tres palancas) | `4.6` § «Un pipeline reproducible de principio a fin» | **`2.1.6.7`** el marco, **`4.6`** la instancia. Las tres palancas aparecen dos veces casi punto por punto |
| R13 | El orden de la cascada: la etapa 2 no tiene clase `normal` | `2.2.4` § 2.2.4.5, puntos 1-3 | `4.5` primer punto del protocolo · `5.3` callout H-5 | **`2.2.4.5`** el argumento; `5.3` lo consume para justificar H-5 |
| R14 | Régimen **semisupervisado (one-class)** de la etapa 1 | `2.1.2` § 2.1.2.6 (regla terminológica), `2.1.4.2`, `2.2.4.2` | `4.4` (encabezado), `5.0.1` precisión 2 | **`2.1.2.6`**, que se declara expresamente sede de la regla terminológica de toda la memoria |
| R15 | Selección de características 122 → 54 y su efecto en la generalización | `2.1.3` § 2.1.3.3 (marco) | `4.3.5`, `4.6` · `5.2.3` · `6.1.1` | **`4.3.5`** (construcción y veredicto H1) + **`5.2.3`** (respaldo empírico). Ya está repartido así explícitamente |

---

# Bloque 2↔5 — Marco Teórico ↔ Evaluación

## Contradicciones

### C1 — El alcance de lo que se toma de Arp et al. `[18]` está declarado de dos maneras incompatibles

> **Estado: APLICADA (2026-08-21)**, junto con su defecto adjunto **C1b**. Las citas literales de las
> filas siguientes son del estado **anterior** a la corrección.

| | |
|---|---|
| **Concepto** | Cuánto se usa del catálogo de *pitfalls* de Arp et al. `[18]` |
| **Sede 5** | `5.0 Protocolo de evaluación` § 5.0.6, callout «Alcance exacto de lo que se toma de [18]» — «De la taxonomía de *pitfalls* este trabajo utiliza **la denominación y la numeración de uno solo**: el **P5** (*Biased Parameter Selection*)» |
| **Sedes 2** | `2.1.6` § 2.1.6.5 — «En seguridad está catalogado como **P3, *Data Snooping***»; y `2.3.3 Límites y consideraciones éticas`, introducción — cita el catálogo de **diez** errores, dos de ellos con nombre y número (**P9** *Lab-Only Evaluation*, **P10** *Inappropriate Threat Model*), más la **cita textual** «*each paper suffers from at least three pitfalls*» y el dato de los treinta artículos revisados |
| **Sedes fuera del perímetro que confirman el choque** | `4.3.7` (usa P3 **y** P5) · `5.4` (usa **P9**) · `6.1.4.2` (tabla completa **P1–P10** con denominación) |
| **Etiqueta** | **CONTRADICCIÓN** |
| **Por qué lo es** | La declaración de `5.0.6` es una **afirmación sobre el trabajo entero** («este trabajo utiliza … de uno solo»), y es falsa a la vista de `2.1.6.5`, `2.3.3`, `4.3.7`, `5.4` y `6.1.4.2`. No es una diferencia de énfasis: es una acotación de honestidad bibliográfica que, tal como está escrita, no describe el trabajo. |
| **Sede canónica propuesta** | **`6.1 Conclusiones` § 6.1.4.2**, cuyo callout de verificación pendiente sí declara el alcance real y correcto: «de ella proceden la **existencia y el número del catálogo**, y la **denominación de los diez errores**… Queda por comprobar la numeración y la redacción exacta». Es la formulación que hay que propagar. |
| **Corrección** | Reescribir el callout de `5.0.6` para que su alcance sea **de apartado** («en este apartado solo se invoca el P5 por su número») o alinearlo con la formulación de `6.1.4.2`. Una frase. |

> [!warning] Defecto adjunto detectado en la misma ficha
> La regla del proyecto exige que **toda nota que cite una fuente no verificada lleve junto a la cita
> el callout `> [!warning] Verificación pendiente — sin acceso al texto completo`**. Lo llevan
> `4.3.7`, `5.0.6` y `6.1.4.2`. **No lo llevan `2.1.6` (§2.1.6.5 y §2.1.6.6, que citan `[18]`) ni
> `2.3.3` (que estructura toda su introducción sobre `[18]`, incluida una cita textual).** `2.3.3` es
> el caso más expuesto, porque es la única sede que **cita literalmente** el artículo. Esto no es un
> solape, pero se corrige en el mismo pase.

### C2 — El desplazamiento D1→D2 se llama «deriva» en el capítulo 2 y «no es deriva» en el 5

> **Estado: APLICADA (2026-08-21)** en `2.1.6.8`. La cita «Deriva de la distribución» es del estado
> **anterior**. **Pero su segundo filo sobrevivía en `6.2` § 6.2.6, peldaño 2** (atribución del FPR
> sobre la medición (A), que no la sostiene): está **en remediación en esta misma tanda**. Ver apéndice.

| | |
|---|---|
| **Concepto** | Cómo se nombra el fenómeno que separa D1 de D2, y qué lo mide |
| **Sede 2** | `2.1.6` § 2.1.6.8 «La sobreestimación de la validación cruzada frente al test», punto 1 — «**Deriva de la distribución (*drift*).** En NSL-KDD está **medida** en este trabajo mediante contrastes de Kolmogórov-Smirnov entre D1 y D2» |
| **Sede 5** | `5.1 Resultados del modelo de detección de anomalías` § 5.1.3, subapartado **«Cómo se llama el fenómeno, y cómo no»** — «Lo que separa a D1 de D2 es un **desplazamiento entre particiones** (*dataset shift*…), **no una deriva temporal** (*concept drift*)», con tabla de qué exigiría afirmar cada una y la constancia de que **NSL-KDD no tiene ninguna columna de fecha** |
| **Refuerzos** | `6.2` § 6.2.6, peldaño 2 — «**Debe nombrarse con precisión: es desplazamiento entre particiones, no deriva temporal.** Sin esa precisión, la afirmación se cae en cuanto alguien pregunte por la marca de tiempo». `6.1` § 6.1.4, límite 1 — «Cualquier análisis de **deriva temporal** … queda fuera» |
| **Etiqueta** | **CONTRADICCIÓN** (nomenclatura) |
| **Por qué lo es** | `5.1.3` y `6.2.6` **declaran expresamente no verificable** el término que `2.1.6.8` usa como rótulo de una magnitud «medida en este trabajo». Es exactamente el ataque que `6.2.6` anticipa: quien lea `2.1.6.8` y luego pregunte por el eje temporal encuentra que no existe. |
| **Segundo filo de la misma ficha** | `2.1.6.8` habla de **un** contraste KS («entre D1 y D2»), cuando `4.2` § *Desplazamiento de distribución…*, `5.1.3` y `6.1.4` insisten en que son **dos mediciones separadas y no intercambiables** —(A) D1 vs D2 completo y (B) D1 vs las 9.711 filas normales de D2— y en que **solo (B)** puede sostener la atribución del FPR. Tal como está, `2.1.6.8` respalda la lectura que `5.1.3` prohíbe. |
| **Sede canónica propuesta** | **`5.1` § 5.1.3** para la **nomenclatura** (es la que fija el término y la evidencia) y **`4.2` § 4.2.1** para las **cifras** de (A) y (B), que declara ser «el único que las tabula». |
| **Corrección** | En `2.1.6.8` punto 1: sustituir el rótulo «Deriva de la distribución» por «**Desplazamiento entre particiones** (*dataset shift*)», remitir a `5.1.3` para el deslinde frente a la deriva temporal, y decir «**dos** contrastes KS separados» en lugar de uno. Dos frases. |
| **Lo que NO hay que tocar** | El uso de «*drift*» como sinónimo de *dataset shift* en `4.3.2` («manifestación del *drift* entre train y test»), `5.2.2` («desplazamiento distribucional D3→D2») y `6.1.1`/`6.1.3` («desplazamiento distribucional (*drift*)») **es compatible**: ninguno afirma deriva temporal. El que choca es el rótulo «deriva» de `2.1.6.8` unido a la afirmación de estar medida. |

### C3 — «El trabajo no proyecta a prevalencia operativa», y `5.4` publica una proyección

> **Estado: APLICADA (2026-08-21)** en `2.3.3.2` y en `6.1.4`. La cita «tampoco proyecta» es del
> estado **anterior**.

| | |
|---|---|
| **Concepto** | Si las cifras de precisión del trabajo están proyectadas a una prevalencia realista |
| **Sede 2** | `2.3.3` § 2.3.3.2, callout **«Alcance de este trabajo»** — «La evaluación **tampoco proyecta** sus resultados a una prevalencia operativa, de modo que las cifras de precisión que se reportan en el capítulo 5 son las correspondientes a la composición de clases del conjunto de evaluación» |
| **Sede 6 (misma afirmación)** | `6.1` § 6.1.4, límite 2, punto 2 — «**ninguna cifra de precisión publicada en el capítulo 5 … está proyectada** a la prevalencia de clases de una red real» · y `6.1.4.2`, fila **P9** — «se declara … que la precisión **no está proyectada** a una prevalencia operativa» |
| **Sede 5** | `5.4 Conclusiones del capítulo` § **«Qué significa este FPR a prevalencia realista»** — publica la fórmula `precisión(p)` y **una tabla de proyección** a prevalencias del 56,9 %, 10 %, 5 %, 1 % y 0,1 %, más la traducción operativa «una alarma real por cada 13 emitidas» |
| **Etiqueta** | **CONTRADICCIÓN** |
| **Por qué lo es** | Es una negación explícita de algo que el trabajo **sí hace**, en el capítulo al que la negación apunta. La reconciliación existe y es sencilla —lo que `5.4` proyecta es un **escenario ilustrativo declarado**, no un resultado medido, y así lo dice su propio callout «Alcance de esta proyección»— pero **ninguna de las dos sedes negativas la recoge**, así que el texto se contradice tal como está. |
| **Sede canónica propuesta** | **`5.4` § «Qué significa este FPR a prevalencia realista»**, porque es la única que tiene la proyección, su fórmula, su fuente (`bin_recall` y `bin_fpr` de `metricas_hibrido.csv`) y su salvedad de alcance. |
| **Corrección** | Matizar las dos negaciones: «ninguna cifra de precisión **publicada como resultado** está proyectada; la proyección **ilustrativa** a prevalencias hipotéticas, con su alcance declarado, está en `5.4`». Una frase en `2.3.3.2` y una en `6.1.4` (límite 2 y fila P9 comparten redacción). |

### C5 — La jerarquía de dificultad predicha en `2.2.5.2` no se reconcilia con la tabla de `5.2.2`

> **Estado: APLICADA (2026-08-21)** — es la C5 que la lista corta da por aplicada (no confundir con la
> ficha homónima de los dos ejes, en el bloque 2↔4). La cita «sin advertir que el orden se ha
> invertido» es del estado **anterior**: `5.2.2` tiene ya el subapartado de reconciliación. **El
> mecanismo de `probe` quedó mal explicado en él** y está en remediación en esta misma tanda: ver apéndice.

| | |
|---|---|
| **Concepto** | Gradación de detectabilidad DoS → Probe → R2L → U2R |
| **Sede 2** | `2.2.5` § 2.2.5.2 «Gradación de visibilidad en red» — «**La jerarquía de dificultad DoS → Probe → R2L → U2R es una predicción *a priori*, no una observación posterior**… si los resultados la reprodujeran se estaría confirmando una hipótesis, y **si la contradijeran habría que sospechar de una fuga de información o de un artefacto del conjunto de datos**», con el compromiso de que «se comprueban empíricamente en el capítulo 5» |
| **Sede 5** | `5.2` § 5.2.2, tabla de F1 por categoría sobre los 9.083 ataques de tipo conocido de D2: el orden medido es `dos` > **`r2l`** > **`probe`** > `u2r` en DecisionTree (0.961 / 0.912 / 0.749 / 0.361), RandomForest (0.998 / 0.937 / 0.902 / 0.452) y HistGradientBoosting (0.995 / 0.921 / 0.869 / 0.432); solo KNN respeta el orden predicho (0.993 / 0.911 / 0.927 / 0.245) |
| **Etiqueta** | **CONTRADICCIÓN** |
| **Por qué lo es** | El capítulo 2 **se compromete por escrito** a que el capítulo 5 comprueba la jerarquía y **anuncia qué significaría que no se reprodujera** (sospecha de fuga o de artefacto). En tres de los cuatro algoritmos **no se reproduce** —`r2l` queda por encima de `probe`— y **el capítulo 5 no lo menciona en ningún punto**: `5.2.2` describe `r2l` como «el caso intermedio» y `probe` como «esencialmente resuelta», sin advertir que el orden se ha invertido respecto de la predicción. La promesa queda sin cumplir y la predicción, sin evaluar. |
| **Sede canónica propuesta** | **`5.2` § 5.2.2** para el veredicto, porque es donde están las cifras. `2.2.5.2` mantiene la predicción, que es lo suyo. |
| **Corrección** | Añadir en `5.2.2` una frase que cierre el bucle: la jerarquía se reproduce en los extremos (`dos` arriba, `u2r` abajo) pero **`r2l` supera a `probe`** en tres de los cuatro algoritmos, y decir con qué se explica —el propio `5.2.2` ya apunta la vía, «precisión muy alta (≥ 0.97) pero recall entre 0.84 y 0.89» en `r2l`, contra un `probe` con soporte pequeño (n=1.106) y fuga hacia otras clases— **sin invocar fuga de datos**, que `4.3.7` descarta línea a línea. |
| **Aviso** | **No hay que mover ninguna cifra.** Es una frase de reconciliación. Si se prefiere, la alternativa es rebajar `2.2.5.2` de «predicción comprobable» a «tendencia esperable», pero eso pierde el argumento más fino de esa sección. |

## Repeticiones compatibles (2↔5) — NO se tocan

| # | Concepto | Sede en el capítulo 2 | Sede en el capítulo 5 | Sede canónica propuesta y por qué |
|---|---|---|---|---|
| R16 | Definiciones de precisión, recall, F1, FPR, exactitud, macro/ponderado, ROC y PR | `2.1.5` completa | `5.0.3` (métricas obligatorias, sin redefinir), `5.1.2` (tabla), `A.2` | **`2.1.5`**: `5.0.3` ya se limita a exigirlas y `2.1.5` declara en su cierre que «los capítulos 4 y 5 las emplean sin volver a justificar» |
| R17 | **«Ninguna cifra de recall sin su FPR al lado»** | `2.1.5.4`, `2.1.6.6` (callout), `2.2.4.3` («Consecuencia para la evaluación»), `2.3.3.1` | `5.0.3` métrica obligatoria 1, `5.0.4` prohibición 2, `5.1.2` caveat P-2, `5.3` (tabla 0-day con FPR por detector), `5.4` caveat 1, `6.1.4.2` fila P8 | **`2.2.4.3`** el argumento (prevalencia) + **`5.0.3`/`5.0.4`** la regla de protocolo. **Diez sedes**: es la segunda repetición más extendida de la memoria, y es intencional (es la decisión P-2) |
| R18 | Falacia de la tasa base `[24]` | `2.2.4` § 2.2.4.3 (desarrollo con Axelsson), `2.3.3.1` (consecuencia humana) | `5.4` § «Qué significa este FPR a prevalencia realista» | **`2.2.4.3`**. Gestión ejemplar: `2.3.3.1` dice «no se reproduce aquí» y `5.4` dice «ya desarrollado en 2.2.4, al que aquí solo se le ponen los números» |
| R19 | Chow `[16]`: un umbral de confianza **no** es una regla de rechazo óptima | `2.1.6` § 2.1.6.4, callout «Un umbral de confianza no es una regla de rechazo óptima» | `5.3` § «El enrutado a `unknown`…», párrafo «Queda una precisión sobre el marco teórico del rechazo» | **`2.1.6.4`** la teoría, **`5.3`** la consecuencia. **Es el caso más señalable del bloque:** `2.1.6.4` remite a `5.3` «para su consecuencia medida», pero `5.3` **vuelve a exponer el argumento completo** (optimalidad sobre posteriores verdaderas, `predict_proba` como proporción de votos, «el resultado de Chow *limita* lo que cabe afirmar»). Son compatibles palabra por palabra; el párrafo de `5.3` podría condensarse a una remisión. **Decisión de Francisco** |
| R20 | Clasificador de conjunto cerrado sobre-confiado ante lo desconocido; *open-set* `[49]` `[61]` | `2.3.2` § 2.3.2.4, «El modelo que no sabe decir "no lo sé"» | `5.3` puntos 2 y 3 del enrutado a `unknown` · `6.1.4` límite 5 · `6.2.2` | **`2.3.2.4`** el marco (dice «sin cifras, la cuantificación pertenece al capítulo 5») + **`5.3`** la cifra y la teoría aplicada. **Cuatro sedes** para el mismo argumento; las dos del capítulo 6 lo repiten por tercera y cuarta vez |
| R21 | La validación cruzada sobrestima frente al test | `2.1.6` § 2.1.6.8 (con el caso del baseline y «aquí no se anticipan» las cifras) | `5.2.2` (brecha 0.945–0.980 en CV frente a 0.746–0.822 en D2), `5.3` (baseline 0.909 → 0.472), `5.4` | **`2.1.6.8`** el marco, **`5.3`** las cifras del baseline. Coherente en ambos sentidos |
| R22 | `snmpgetattack` y el **límite de representación** por flujo | `2.2.3.4` («Encaje en el trabajo», que remite), `2.2.5.2` punto 2, `2.3.3.6` | `5.3` hallazgo 2 (recall máximo 0.152), `5.4` · `6.1.3`, `6.1.4` · `6.2.6` peldaño 3 | **`5.3`** para la medición y **`2.3.3.6`** para el enunciado conceptual. Ya está declarado así: `2.2.3.4` dice «ninguno se reproduce en esta sección» |
| R23 | Modo pasivo y diferido: no se mide tiempo real ni bloqueo | `2.2.2.3` (callout «Alcance de este trabajo»), `2.2.3.3`, `2.3.3.7` (callout «Postura adoptada») | `5.4` § «Qué NO mide la latencia publicada» · `6.1.4` límite 2 | **`2.2.3.3`** § «Encaje en el trabajo» para la casilla; **`5.4`** para lo que las columnas de tiempo sí y no miden |
| R24 | Frontera de bloques del NIDS: captura y extracción de características fuera de alcance | `2.2.2` § 2.2.2.4 (tabla de correspondencia + Figura 2.5) | `5.4` (tabla «¿Está medida aquí?») · `6.1.4` límite 2 punto 1 | **`2.2.2.4`** la frontera, **`5.4`** la consecuencia sobre las cifras de caudal. Compatibles y complementarias |
| R25 | Barrido de 10 semillas (1-10), media, sd `ddof=1`, banda [mín, máx], **sin contraste estadístico**; la semilla 42 fuera de las bandas | `2.1.6` § 2.1.6.7, último párrafo | `5.0.5` (vocabulario de reproducibilidad `[20]` y «brecha principal»), `5.2.4`, `5.4` · `6.1.4` límite 4 | **`6.1.4` límite 4** (qué se hizo y qué queda declarado) + **`5.2.4`** (bandas) + **`A.3`** (volcado). Nota menor: `2.1.6.7` remite solo a `A.3` y omite que las **bandas** las publica `5.2.4`; no es contradicción, `A.3` sí tiene el volcado |

---

# Bloque 2↔6 — Marco Teórico ↔ Conclusiones

## Contradicciones

### C6 — `2.2.3.3` promete que `6.2` discute el salto a tiempo real, y `6.2` no lo discute

> **Estado: APLICADA (2026-08-21).** La cita «Ese salto se discute en `6.2`» es del estado **anterior**.

| | |
|---|---|
| **Concepto** | El paso de operación pasiva y diferida a despliegue en línea / tiempo real |
| **Sede 2** | `2.2.3` § 2.2.3.3 — «**no se mide** el rendimiento en tiempo real —*throughput* sostenido, latencia por conexión, pérdida de paquetes— ni ninguna capacidad de bloqueo… **Ese salto se discute en [[6.2 Líneas futuras]]**» |
| **Sede 6** | `6.2 Líneas futuras` — sus seis líneas son: 6.2.1 *ensemble* de detectores · 6.2.2 reconocimiento de conjunto abierto · 6.2.3 vector de error de reconstrucción · 6.2.4 conjuntos de características por etapa · 6.2.5 generación automática de firmas · 6.2.6 dataset con reloj, deriva y secuencia. **Ninguna aborda el despliegue en línea, el caudal sostenido ni el bloqueo** |
| **Etiqueta** | **CONTRADICCIÓN** (remisión sin sede) |
| **Por qué lo es** | `6.2` declara un criterio de admisión restrictivo («solo entra aquí lo que está respaldado por una medición del propio trabajo o por una limitación estructural verificada»), de modo que la línea no está «pendiente de escribir»: está **excluida por criterio**. La remisión de `2.2.3.3` apunta a un apartado que, por su propia regla, no tiene ese contenido. |
| **Sede canónica propuesta** | **`5.4` § «Qué NO mide la latencia publicada»** — es la única sede del trabajo que trata el asunto en serio: enumera lo que queda fuera (captura, ensamblado, extracción) y declara «**el caudal de extremo a extremo de un despliegue no puede acotarse por debajo con los datos disponibles**» y «requeriría una implementación del extractor de características sobre tráfico crudo y su cronometraje, trabajo que este TFG no aborda». |
| **Corrección** | Reapuntar la remisión de `2.2.3.3` a `5.4` § «Qué NO mide la latencia publicada» **o** dar de alta en `6.2` la línea del extractor de características cronometrado, que sí cumpliría el criterio de admisión (está respaldada por la medición y la salvedad de `5.4`). **La segunda opción es la mejor**, porque hoy `5.4` deja el hueco abierto sin destinatario. |

### C7 — `2.3.2.5` remite a `6.2` la «validación cruzada entre datasets», que `6.2` no recoge

> **Estado: APLICADA (2026-08-21).** Las remisiones citadas de `2.3.2.5` y de `4.2` punto 5
> corresponden al estado **anterior**.

| | |
|---|---|
| **Concepto** | Entrenar sobre un dataset y evaluar sobre otro, como prueba de generalización |
| **Sede 2** | `2.3.2` § 2.3.2.5, último párrafo — «la **validación cruzada entre datasets** —entrenar sobre uno y evaluar sobre otro— es la única forma de comprobar si un resultado obtenido sobre NSL-KDD generaliza más allá de él. Excede el alcance de esta entrega y **se recoge como línea futura en [[6.2 Líneas futuras]]**» |
| **Sede 6** | `6.2` § 6.2.6, peldaño 1 propone **migrar** a un dataset con marca de tiempo (CIC-IDS2017) y reejecutar el pipeline. **Migrar no es validación cruzada entre datasets**: no hay entrenamiento en uno y evaluación en otro en ninguna de las seis líneas |
| **Sede 4 con el mismo defecto** | `4.2` § «Por qué se eligió NSL-KDD», punto 5 — «la **validación sobre datasets modernos** queda recogida como línea futura (véase [[6.2 Líneas futuras]])». Misma remisión, misma ausencia de sede |
| **Etiqueta** | **CONTRADICCIÓN** (remisión sin sede) |
| **Por qué lo es** | Dos apartados distintos prometen que `6.2` contiene una línea que `6.2` no tiene. Y aquí hay un agravante de fuente: la referencia de validación cruzada entre datasets es una de las **cerradas como «no verificable sin acceso institucional; no sostiene ninguna afirmación del trabajo»**, así que **la línea no puede escribirse apoyándose en ella** sin romper el criterio con el que se cerró. |
| **Sede canónica propuesta** | **`6.2` § 6.2.6**, ampliando el peldaño 1: la migración a CIC-IDS2017 ya está verificada con `[51]` y **habilita** la evaluación cruzada; basta enunciar la evaluación cruzada como consecuencia del peldaño 1 y apoyarla en `[51]`, no en la fuente sin verificar. |
| **Corrección** | O bien añadir esa frase al peldaño 1 de `6.2.6` y dejar las dos remisiones válidas, o bien rebajar las remisiones de `2.3.2.5` y `4.2` a «excede el alcance de esta entrega» sin prometer sede. **Lo primero es preferible**: `2.3.2.5` tiene razón en que es la única prueba de generalización real, y `6.2.6` ya trae el candidato verificado. |

### C8 — El «13,4 %» y su explicación teórica viven en cuatro sedes, y una de ellas lo reabre como si fuera nuevo

> [!warning] Estado: NO APLICADA — desviada al cubo de decisión de Francisco. **No es un olvido.**
> `6.1` § 6.1.4, límite 5 **se ha dejado intacta a propósito**, conforme a la regla dura de no
> resolver repeticiones compatibles. Razón: esta ficha es **repetición compatible** —las cuatro sedes
> dicen lo mismo y ninguna contradice a otra—, como advierte la nota de honestidad al final de la
> propia ficha, y en ese cubo «cuál sobra» **lo decide Francisco**. La corrección propuesta queda
> descrita abajo, lista para despachar si él así lo resuelve.

| | |
|---|---|
| **Concepto** | Por qué solo el 13,4 % de los 0-day detectados llega a etiquetarse `unknown` |
| **Sede 2** | `2.3.2` § 2.3.2.4, «El modelo que no sabe decir "no lo sé"» — enuncia el mecanismo y declara: «En este trabajo el comportamiento descrito **se observa de forma directa** en la evaluación del sistema; su cuantificación pertenece al capítulo 5… **Aquí se enuncia únicamente como advertencia general del campo, sin cifras**» |
| **Sede 6** | `6.1` § 6.1.4, límite 5 — reexpone el argumento completo (riesgo de espacio abierto `[61]`, «no es un fallo de calibración del umbral ni un artefacto del experimento, sino el comportamiento **esperado**») **con la cifra del 13,4 %** · y `6.2` § 6.2.2 la repite una cuarta vez, también con la cifra |
| **Sede 5 (canónica de hecho)** | `5.3` § «El enrutado a `unknown`: el 13,4 %, explicado», con sus tres afirmaciones separadas, la tabla de procedencia y el callout «Qué NO explica este 13,4 %» |
| **Etiqueta** | **CONTRADICCIÓN** (de alcance declarado) |
| **Por qué lo es** | `2.3.2.4` declara que la **cuantificación** pertenece al capítulo 5 y que allí se enuncia «sin cifras» — correcto. Pero el capítulo 6 **cuantifica dos veces más** (`6.1.4` límite 5 y `6.2.2`), de modo que el reparto que `2.3.2.4` anuncia («marco aquí, cifra en el 5») no es el que el documento cumple. `6.1.4` límite 5 además **reexpone la razón técnica completa** en lugar de consumirla, que es lo que hace el resto de `6.1.4` con los demás límites. |
| **Sede canónica propuesta** | **`5.3` § «El enrutado a `unknown`: el 13,4 %, explicado»**, sin discusión: es la única con la tabla de procedencia (`metricas_hibrido_calibracion.csv`, columna `d2_pct_unknown_entre_0day_det` = 0,133564), el recuento derivado (386 sobre 2.890) y la tabla de sensibilidad del umbral. |
| **Corrección** | Dejar `5.3` intacta. En `6.1.4` límite 5, reducir la *razón técnica* a su enunciado y remitir a `5.3` (como ya hace el propio `6.1.4` en los límites 1 y 6). `6.2.2` ya remite bien («La cifra, su procedencia … y su alcance … están en `5.3`»), así que **`6.2.2` no se toca**. |

> Nota de honestidad sobre esta ficha: es la más discutible de las ocho. Puede leerse como
> repetición compatible —las cuatro sedes dicen lo mismo y ninguna contradice a otra— y en ese caso
> **sale del cubo de corrección y pasa al de Francisco**. Se ha clasificado como contradicción
> **solo** porque `2.3.2.4` hace una afirmación explícita sobre dónde vive la cifra que el capítulo 6
> no respeta.

## Repeticiones compatibles (2↔6) — NO se tocan

| # | Concepto | Sede en el capítulo 2 | Sede en el capítulo 6 | Sede canónica propuesta y por qué |
|---|---|---|---|---|
| R26 | El catálogo de diez errores metodológicos `[18]` como armazón del análisis crítico | `2.3.3`, introducción | `6.1` § 6.1.4.2 «Balance frente al catálogo de errores metodológicos» (tabla P1–P10) | **`2.3.3`** el marco, **`6.1.4.2`** la instancia. Ya está declarado: `6.1.4` dice «El marco conceptual… está desarrollado en `2.3.3` y no se repite aquí» |
| R27 | **P9** (*Lab-Only Evaluation*) y **P10** (*Inappropriate Threat Model*) | `2.3.3` § 2.3.3.2 y § 2.3.3.3 | `6.1.4` límites 2 y 3 · `6.1.4.2` filas P9 y P10 | **`2.3.3.2`/`2.3.3.3`** el marco, **`6.1.4` límites 2 y 3** la instancia. **Cada límite se enuncia tres veces** (marco, límite, fila de la tabla); es coste del formato de tabla, no un defecto |
| R28 | Modelo de amenaza: seis dimensiones, adversario no adaptativo, *feature space* / *problem space* `[52]` | `2.3.3` § 2.3.3.3 · `2.3.2` § 2.3.2.3 (que declara por qué no desarrolla la IA ofensiva) | `6.1.4` límite 3 (+ su callout «Dónde vive la instanciación», que remite a `3.1.5`) | **`2.3.3.3`** el marco, **`3.1` § 3.1.5** la especificación, **`6.1.4` límite 3** el consumo. Cadena de tres bien declarada |
| R29 | Obsolescencia del corpus: tráfico de 1998, tipos obsoletos, métricas no extrapolables | `2.2.5` § 2.2.5.3 (tabla a favor/en contra) · `2.3.3` § 2.3.3.6 | `6.1.4` límite 6 · (y `4.2` punto 5) | **`2.2.5.3`** (tabla del equilibrio, que `2.3.2.5` ya declara canónica: «está tabulado en `2.2.5` §2.2.5.3 y no se reproduce aquí») + **`4.2`** para la justificación de la elección |
| R30 | Evaluación **desagregada por clase** y promedio **macro** como criterio ético y metodológico | `2.3.3` § 2.3.3.6 (cierre) · `2.1.5.3` · `2.1.6.6` | `6.1.4.2` fila P7 · (y `5.0.3` métrica obligatoria 3, con el respaldo de las *model cards* `[60]`) | **`5.0.3`** para la obligación de reporte y **`2.1.5.3`** para la definición del macro. `2.3.3.6` aporta el argumento ético, que es propio suyo |
| R31 | Firmas y anomalías **no compiten, se complementan** (tesis del trabajo) | `2.2.4` § 2.2.4.4 (tabla comparativa) y § 2.2.4.5 («combinarlos domina a elegir uno») | `6.1` § 6.1.2 «Validación de la tesis del híbrido» | **`2.2.4.4`/`2.2.4.5`** el argumento *a priori*, **`6.1.2`** la validación con datos (que ya remite a `5.3` para la comparación completa) |
| R32 | Explicabilidad: en seguridad no es opcional; la etapa 2 es interpretable por construcción | `2.3.3` § 2.3.3.4 · `2.3.1` § 2.3.1.2 | `6.1.4.2` fila P4 · (y `4.5` § «Extracción de firmas legibles») | **`2.3.3.4`** el argumento y la tabla de los dos enfoques, **`4.5`** las reglas efectivas. `2.3.3.4` ya dice «no se repite aquí» |
| R33 | Aprendizaje continuo: se examina si el sistema podría incorporarlo | `2.1.1` § 2.1.1.2, sobre la Figura 2.4 — «si el sistema de este trabajo podría incorporarlo… se examina en `6.2`» | `6.2` § «Nota sobre el aprendizaje continuo y el paradigma *Nested Learning*» · `6.1.4` límite 1 | **`6.2`** § esa nota, que es la única sede con las cuatro razones y con el callout «Dos etiquetas que no deben usarse». **Esta remisión sí se honra** (a diferencia de C6 y C7) |

---

# Lista corta: lo que se corrige

Ocho fichas, ordenadas de mayor a menor coste de arreglo. **Ninguna toca un número publicado.**
La columna **Estado** refleja el disco a **2026-08-21**: siete aplicadas, C4 aplicada en la
remediación posterior y **C8 desviada a decisión de Francisco** (no es un olvido; ver el bloque de
estado de cabecera).

| # | Estado | Sede a corregir | Qué hay que cambiar | Sede canónica que manda | Coste |
|---|---|---|---|---|---|
| **C5** | **Aplicada** | `5.2` § 5.2.2 | Cerrar el bucle con la predicción de `2.2.5.2`: `r2l` supera a `probe` en 3 de los 4 algoritmos; explicarlo **sin** invocar fuga de datos | `5.2` § 5.2.2 (tiene las cifras) | 1 párrafo |
| **C6** | **Aplicada** | `2.2.3` § 2.2.3.3 **o** `6.2` | La remisión «Ese salto se discute en `6.2`» no tiene sede. Preferible: dar de alta en `6.2` la línea del extractor cronometrado, respaldada por `5.4` | `5.4` § «Qué NO mide la latencia publicada» | 1 línea nueva en `6.2` |
| **C7** | **Aplicada** | `2.3.2` § 2.3.2.5 **y** `4.2` punto 5 **o** `6.2` § 6.2.6 | La «validación cruzada entre datasets» no está en `6.2`. Preferible: enunciarla como consecuencia del peldaño 1, apoyada en `[51]` y **no** en la fuente sin verificar | `6.2` § 6.2.6, peldaño 1 | 1 frase |
| **C3** | **Aplicada** | `2.3.3` § 2.3.3.2 (callout «Alcance de este trabajo») **y** `6.1` § 6.1.4 límite 2 + fila P9 | «No se proyecta a prevalencia operativa» es falso: `5.4` proyecta. Matizar a «ninguna precisión **publicada como resultado**; la proyección ilustrativa está en `5.4`» | `5.4` § «Qué significa este FPR a prevalencia realista» | 2 frases |
| **C2** | **Aplicada** (segundo filo residual en `6.2.6`: ver apéndice) | `2.1.6` § 2.1.6.8, punto 1 | Rótulo «Deriva de la distribución» → «**Desplazamiento entre particiones** (*dataset shift*)», remitir a `5.1.3`, y decir **dos** contrastes KS (A) y (B), no uno | `5.1` § 5.1.3 (nomenclatura) · `4.2` § 4.2.1 (cifras) | 2 frases |
| **C1** | **Aplicada** | `5.0` § 5.0.6, callout «Alcance exacto de lo que se toma de [18]» | «utiliza la numeración de **uno solo**» es falso a nivel de trabajo (`2.1.6.5` usa P3; `2.3.3` usa P9/P10; `6.1.4.2` usa los diez). Acotar a «en este apartado» o alinear con `6.1.4.2` | `6.1` § 6.1.4.2 (callout de verificación pendiente) | 1 frase |
| **C1b** | **Aplicada** | `2.1.6` (§2.1.6.5 y §2.1.6.6) **y** `2.3.3` (introducción) | **Falta el callout obligatorio** `> [!warning] Verificación pendiente — sin acceso al texto completo` junto a las citas de `[18]`. `2.3.3` es el caso urgente: es la única sede que **cita literalmente** el artículo | Modelo a copiar: el callout de `4.3.7` / `6.1.4.2` | 2 callouts |
| **C8** | **NO aplicada — desviada a tu decisión, no es un olvido** | `6.1` § 6.1.4, límite 5 | Reducir la razón técnica a su enunciado y remitir a `5.3`, como ya hace `6.1.4` en los límites 1 y 6. **`6.2.2` no se toca** (ya remite bien). *Ficha discutible: puede pasar al cubo de decisión de Francisco* | `5.3` § «El enrutado a `unknown`: el 13,4 %, explicado» | 1 párrafo condensado |

## Lo que este barrido deja fuera a propósito

- **No se propone borrar ninguna repetición.** Las 33 fichas del cubo compatible se informan y se dejan
  como están. Las tres de mayor volumen, por si Francisco quiere entrar por ahí, son **R7** (las tres
  decisiones sobre D2, nueve sedes con formulación literal repetida), **R17** (recall+FPR, diez sedes) y
  **R19** (el argumento de Chow expuesto completo dos veces, en `2.1.6.4` y en `5.3`).
- **No se ha tocado `features.md`, `Bibliografía.md` ni `00 Índice TFG.md`**, ni ninguna nota de la
  memoria. Tampoco se ha emitido ningún marcador `[n]` nuevo: los `[n]` que aparecen en este informe
  son los que ya están en las notas citadas.
- **Perímetro estricto:** capítulo 2 contra capítulos 4, 5 y 6. Los solapes internos del capítulo 5
  con el 6, y los de ambos con `A.2`/`A.3`, quedan fuera de este encargo y no se han inventariado —
  aunque en el camino se han visto **dos remisiones más a `6.2` sin sede**, en `5.1.3` (recalibrado
  periódico del umbral) y en `5.4` (la reducción del FPR como primera prioridad), que son el mismo
  defecto que C6 y C7 y se arreglarían en el mismo pase.

---

# Apéndice — Lo que la auditoría encontró DESPUÉS del barrido (2026-08-21)

> [!note] Por qué va en apéndice
> Estos seis puntos **no son solapes nuevos del inventario**: son **residuos y efectos colaterales** de
> aplicar las ocho fichas, detectados al auditar el resultado. Por eso **no se renumeran las fichas
> C1–C8 ni se altera el recuento de 41 solapes ni el de 33 repeticiones compatibles**: las tablas de
> arriba siguen valiendo tal cual. **Todos están en remediación en esta misma tanda.**

| # | Sede | Qué falla | Origen |
|---|---|---|---|
| **A-1** | `6.2` § 6.2.6, **peldaño 2** | Sobrevive el **segundo filo de C2**: atribuye el FPR apoyándose en la medición **(A)** (D1 vs D2 completo), que **no la sostiene** — solo **(B)** (D1 vs las 9.711 filas normales de D2) puede hacerlo | Residuo de **C2**: se corrigió `2.1.6.8` y no la sede paralela de `6.2` |
| **A-2** | `5.2` § 5.2.2, **subapartado nuevo** | El **mecanismo de `probe`** queda mal explicado en la frase de reconciliación añadida | Efecto colateral de **C5** (jerarquía) |
| **A-3** | `6.2`, callout `> [!todo]` | Contiene una **frase falsa** | Detectado al auditar `6.2` tras C6/C7 |
| **A-4** | `2.1.6` (tabla) y `A.3` § A.3.11.6 | **Residuo de nomenclatura «deriva»**: quedan usos del término que C2 retiró de `2.1.6.8` | Residuo de **C2**, en sedes no barridas por ser tabla y apéndice |
| **A-5** | `5.1` § 5.1.3 y `5.4` | **Dos remisiones a `6.2` sin sede** (recalibrado periódico del umbral · el FPR como primera prioridad). Mismo defecto que C6 y C7 | Ya anticipado en «Lo que este barrido deja fuera a propósito», fuera de perímetro |
| **A-6** | `2.1.6` § 2.1.6.4 | La frase «las rejillas son de **dos ejes por algoritmo**» **seguía sin acotar** al verificarla en disco (ficha C5 del bloque 2↔4, la de la colisión de numeración) | Ficha del inventario no aplicada en el barrido |

**Lo que este apéndice NO cambia:** ninguna cifra publicada, ninguna de las **33 repeticiones
compatibles** —que siguen intactas y son lo que queda para la lectura de Francisco—, ni la
clasificación de **C8**, que permanece desviada a su decisión.
