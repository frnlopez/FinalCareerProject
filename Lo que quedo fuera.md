# Lo que quedó fuera

Contenido de alto valor presente en las notas del vault y **ausente** del `.docx`
(`Proyecto_Fin_de_Grado-FJLM-2026.docx`, extraído el 2026-08-26). Fichero acumulativo:
cada ciclo añade un capítulo. Excluido todo lo ya recogido en `Las tareas de Kiko.md`.

## Capítulo 5

> [!note] Re-verificado contra `Resultados\docx\REPASO_20260826-1908\`
> Los 8 hallazgos escritos en el ciclo anterior se contrastaron uno a uno contra la extracción
> fresca de las 19:08, y el inventario quedó en **8 hallazgos** numerados del 1 al 8. Ninguno se
> retira; cuatro se han **acotado** al recortar la parte que la extracción refuta, y uno que se
> había dado por caído **se repone acotado**:
> - *«Qué NO explica este 13,4 %»* (era el hallazgo 6) → **se REPONE**, acotado a la mitad del
>   punto de operación (hoy hallazgo 6). Cayó entero por error: la **Tabla 5.9 «Calibración del
>   umbral de confianza sobre probabilidades out-of-fold de D3»** aporta **las cifras, no la
>   refutación**. La mitad de *calibración* sí está cubierta, pero **no por la Tabla 5.9**, sino
>   por el **límite 5 de 6.1.4** («no es un fallo de calibración del umbral ni un artefacto del
>   experimento»). La mitad del **punto de operación** no tiene contraparte en ninguna sede del
>   `.docx` (cero coincidencias de «límite estructural», «2,9» y «El compromiso se desplaza»).
> - Recortes aplicados por refutación: el FPR de diseño del 5 % y su causa **sí** están publicados
>   (cap. 3 y limitación «Tasa de falsos positivos condicionada por el drift» del cap. 6); el
>   argumento de la **firma de la función de calibración** **sí** está en el §3 «Protocolo de
>   evaluación común»; las celdas de la variante **54** del eje SMOTE **sí** están en la Tabla 15;
>   y el trío de campos que anuncia §6.1.4 no era el que se le atribuía.

1. **La tabla de las cuatro prohibiciones explícitas del protocolo con su razón, el desglose de las dos familias de columnas de tiempo, y el deslinde `P5` (Arp et al.) frente a `P-5` (decisión propia)** · `5.0 Protocolo de evaluación` § *5.0.4 Prohibiciones explícitas* · el `.docx` publica las métricas obligatorias pero no la tabla de prohibiciones, y sin embargo la **invoca**: §5.4 cita «prohibición 4 del protocolo, 5.0», una referencia que hoy no tiene destino. Ojo: los **cuatro vetos sobre D2** que el `.docx` sí enuncia **no son** las cuatro prohibiciones del vault —la 4 del vault es la de los tiempos—, así que la remisión sigue colgando aunque el lector encuentre cuatro vetos. **No** se pierde el argumento de la **firma de la función de calibración**, que el §3 «Protocolo de evaluación común» trae literal («no admite D2 en su firma, de modo que la prohibición no depende de la disciplina de quien programa, sino de la estructura del código») · en el capítulo 5, tras «Métricas Obligatorias».

2. **La autoauditoría del propio trabajo contra los criterios C1-C7, con los dos que NO se cumplían (C3 y C6) y el caso `accuracy_D2`** · `5.0 Protocolo de evaluación` § *5.0.6 El mismo filtro, aplicado a uno mismo* · el `.docx` enuncia los siete criterios y los aplica **solo hacia fuera**; desaparece la tabla de cumplimiento propio, el ejemplo de las dos `accuracy_D2` (0,9683 y 0,7395) que eran magnitudes distintas bajo el mismo nombre, y las dos decisiones de la corrección (no renombrar columnas, no recalcular cifras). Es contribución metodológica propia y respalda las conclusiones de rigor del capítulo 6 · en el capítulo 5, tras «Criterios aplicados».

3. **La explicación del exceso de falsos positivos: por qué el umbral p95 promete ≈5 % y se mide 10,2 %** · `5.1 Resultados del modelo de detección de anomalías` § *5.1.3 El desplazamiento entre particiones explica el exceso de falsos positivos* · el FPR de diseño del 5 % **sí** está publicado (cap. 3, párrafo del percentil 95 sobre el subconjunto de validación de D1, y repetido en 2.x) y su causa también (cap. 6, limitación «Tasa de falsos positivos condicionada por el drift»: «por encima del ~5 % de diseño, debido al desplazamiento distribucional»). Lo genuinamente ausente es la **mecánica de la explicación**: la atribución del exceso a la medición KS **(B)** y no a la **(A)**, la salvedad de que `delta = (A) − (B)` es una **comparación y no una descomposición** (el KS no es aditivo sobre una mezcla), la tabla de exceso por variante (54 → 10,2 % ≈ 2×; 122 → 8,5 % ≈ 1,7×) y la regla de no citarlo como rango «entre el 8 y el 10 %» · entre «Métricas de desempeño» y «Resultados finales» de §5.1.

4. **El contraste con la jerarquía de dificultad `dos`→`probe`→`r2l`→`u2r` predicha en §2.2.5.2, y la tabla precisión/recall de `probe`** · `5.2 Resultados del modelo de detección basado en firmas` § *Contraste con la jerarquía de dificultad predicha* · **premisa corregida tras verificar la extracción**: el `.docx` **no** llega a plantear la dicotomía «reproducirla confirma / contradecirla obliga a sospechar de una fuga o de un artefacto del conjunto de datos» —no está en el documento, y su sede natural, `2.2.5.2`, tampoco se volcó (hallazgo 6 del capítulo 2)—. Lo que sí publica el capítulo 2 es la gradación de dificultad en prosa, categoría a categoría (DoS «la más marcada», Probe «relativamente visible», R2L «Aquí empieza la dificultad», U2R «Prácticamente nula»), de modo que la memoria **sí instala una expectativa** de orden y luego **la incumple en el tramo intermedio sin explicarlo**. Es ese contraste el que falta, y no la respuesta a una dicotomía inexistente. *(Si se aplica el hallazgo 6 del capítulo 2, la dicotomía pasa a existir y este apartado se convierte además en su respuesta; conviene aplicarlos juntos.)* Se pierde el dato clave de que a `probe` no se le escapan sus ataques (recall 0,998-1,000) sino que se le atribuyen los de otras clases (precisión 0,599-0,864) · en §5.2, tras la Tabla 5.4 de F1 por categoría.

5. **La acotación de Chow: la regla de rechazo por umbral es óptima sobre las posteriores verdaderas, y `predict_proba` no lo es** · `5.3 Resultados del sistema híbrido` § *El enrutado a `unknown`: el 13,4 %, explicado* · el `.docx` conserva los argumentos de Scheirer y Bendale pero pierde el que cierra el razonamiento: el resultado clásico de rechazo **limita** lo que cabe afirmar del umbral en vez de respaldarlo. *(La entrada de Chow figura además en la lista bibliográfica del `.docx` sin cita en el cuerpo que la ancle: eso **no** se cuenta aquí como hallazgo propio — es **remisión a B1** de `Las tareas de Kiko.md`.)* · en §5.3, cerrando el bloque de tres afirmaciones sobre el 13,4 %.

6. **La sensibilidad del punto de operación: mover el umbral desplaza el compromiso pero no elimina el límite estructural** · `5.3 Resultados del sistema híbrido` § callout *Qué NO explica este 13,4 %*, mitad del **punto de operación** · el `.docx` publica las cifras de la Tabla 5.9 pero **no la refutación que se construye con ellas**: que llevar el umbral a 0,6 sube el enrutado a `unknown` al 34,0 % a costa de multiplicar por **2,9** los falsos `unknown` OOF (0,031 % → 0,089 %) y de bajar el `f1_macro-5` de 0,641 a 0,592, de donde se sigue la tesis de que **el compromiso se desplaza pero el límite estructural no desaparece**. Cero coincidencias en el `.docx` de «límite estructural», «2,9» y «El compromiso se desplaza». *(La otra mitad del callout —que no es un fallo de calibración— **sí** está cubierta, pero por el **límite 5 de 6.1.4**, no por la Tabla 5.9.)* · en §5.3, cerrando el bloque sobre el 13,4 %.

7. **La lectura comparada de las Figuras 5.11 y 5.12: la forma del error, no solo su magnitud** · `5.3 Resultados del sistema híbrido` § *Comparación con el baseline RF monolítico* · en el `.docx` la Figura 5.12 aparece con pie y sin análisis; se pierde el argumento de que en el baseline la masa de error cae en la **columna `normal`** (el ataque vuelve a legítimo) mientras que en el híbrido cae en la columna **`unknown`** (error de atribución dentro de lo ya alarmado). Es lo que explica la brecha de recall 0-day y sostiene la tesis del capítulo 6 · en §5.3, entre la Tabla 5.11 y la Figura 5.12.

8. **El argumento de escala entre los dos ejes de balanceo, y las celdas de la variante 122** · `5.2 Resultados del modelo de detección basado en firmas` § *5.2.4.4 La decisión de balanceo no es constante entre semillas* (acotación final) · el eje SMOTE vs no balancear está **parcialmente publicado**: la Tabla 15 del `.docx` ya trae las celdas de la variante **54** con sus desviaciones —incluido el **± 0,0657** de HistGradientBoosting—, y tanto 0,0266 como 0,0180 son restas directas de esa tabla. Lo ausente son (i) las celdas de la variante **122** (0,9523/0,9172 y 0,9724/0,8044, con el **± 0,0824**) y (ii), sobre todo, el **argumento de escala** que hace comparables los dos ejes: que **el margen más estrecho de este eje (0,0266, KNN-54) supera al más amplio del eje no establecido (0,0180, DecisionTree-54)**, junto con la lectura de que sin balancear no solo se rinde menos, sino de forma **más errática entre *folds*** · en §5.2.4, tras el reparto de las 40 celdas.

## Capítulo 6

Contrastado contra `Resultados\docx\REPASO_20260826-1908\`. El capítulo 6 del vault son
**dos notas** (`6.1 Conclusiones` y `6.2 Líneas futuras`) y el volcado al `.docx` las recoge
casi íntegras; lo que sigue son las tres ausencias que superan el umbral. **Excluido** todo lo
que ya está en `Las tareas de Kiko.md` (**6.4**, **6.5**, **6.6**, **6.7**, **6.9**, **6.10**,
**6.11**, **6.12**, **B1** y **A14**), incluida por tanto la remisión colgante a
`A.3 Ficha del sistema`, que es el mismo tema que **A14**.

1. **La declaración de deslinde con `2.3.3` que abre §6.1.4 y fija la estructura de los seis límites** · `6.1 Conclusiones` § *6.1.4 Límites de lo medible con NSL-KDD* (párrafo que empieza «El marco conceptual de estos límites —la brecha entre laboratorio y operación, el papel del modelo de amenaza, el sesgo y la obsolescencia del corpus— está desarrollado en…») · el `.docx` salta del párrafo «El apartado anterior enumera límites del sistema construido…» directamente al encabezado «Los seis límites, uno a uno», de modo que se pierden dos cosas a la vez: (i) la declaración de que el marco conceptual **vive en `2.3.3` y no se repite aquí** —que es lo que justifica que este apartado no lo desarrolle y lo que evita que se lea como una duplicación del capítulo 2—, y (ii) el anuncio del **esquema de tres campos** con el que están escritos los seis límites, que es **su razón técnica sobre el NSL-KDD · la fuente que lo enuncia · la medición que sí se hizo en su lugar**. Sin él, los seis puntos aparecen con una plantilla que nadie ha presentado, y el `.docx` sí invoca ese deslinde después, en los límites 2 y 3 («enunciado de marco en 2.3.3»), sin haberlo establecido · en el capítulo 6, al inicio de 6.1.4, entre «El apartado anterior enumera límites del sistema construido» y «Los seis límites, uno a uno». **Filtro (a).**

2. **El aviso de verificación pendiente sobre Arp *et al.* y Scheirer *et al.*, las dos fuentes que sostienen la Tabla 6.1 y el límite 5** · `6.1 Conclusiones` § *6.1.4.2 Balance frente al catálogo de errores metodológicos* (callout final `> [!warning] Verificación pendiente — sin acceso al texto completo`) · el `.docx` publica la Tabla 6.1 entera —los diez errores P1–P10 con su denominación en inglés— y cierra con el balance «ocho de los diez errores quedan cubiertos», pero **no declara en ningún punto que de Arp *et al.* solo se han verificado resumen y metadatos**, de modo que la **numeración y la redacción exacta de cada pitfall** siguen sin comprobar contra el original; lo mismo vale para Scheirer *et al.*, de quien se toma el enunciado de que umbralizar un clasificador cerrado no acota el riesgo de espacio abierto (límite 5 y §5.3). Se pierde además la acotación que salva el capítulo: que las afirmaciones **sobre este trabajo** se sostienen por su razón técnica y por los artefactos de `Resultados/`, y que **de ninguna de las dos se toma ninguna cifra**. Duele porque es exactamente el tipo de salvedad que el resto del capítulo 6 sí practica (el `.docx` sí conserva el aviso equivalente en 6.2.5), y su ausencia deja la conclusión de rigor apoyada en una atribución nominal sin verificar · en el capítulo 6, cerrando 6.1.4.2, inmediatamente después del párrafo «El balance es, por tanto: ocho de los diez errores…» y del pie de la Tabla 6.1. **Filtros (a) y (c).**

3. **La remisión a `3.1.5 Modelo de amenaza` como sede única de la especificación del adversario** · `6.1 Conclusiones` § *6.1.4 Límites de lo medible con NSL-KDD*, límite 3 «No hay adversario adaptativo (P10)» (callout `> [!note] Dónde vive la instanciación`) · el `.docx` afirma en ese límite que «el modelo de amenaza se **escribe** en lugar de asumirse» y enumera sus rasgos en una línea (adversario no adaptativo, sin conocimiento del modelo, opera sobre flujos ya extraídos, no envenena el entrenamiento), pero **remite solo al marco conceptual de `2.3.3`**; desaparece el puntero a la especificación completa dimensión a dimensión —capacidades, conocimiento, momento de acción y adaptatividad— y a la justificación, apoyada en las propiedades del NSL-KDD, de que no se realice un experimento adversario. También se pierde la frase que declara la relación entre ambos sitios: que este apartado **consume** esa especificación para acotar el alcance de los resultados y **no la duplica**. La sede existe en el `.docx` (§3.1.5 figura en el índice de contenido), así que la afirmación de que el modelo de amenaza «se escribe» queda hoy sin destino verificable para el lector · en el capítulo 6, dentro del límite 3 de 6.1.4, tras «ataques de un adversario que no intenta evadirlo». **Filtro (a).**

## Capítulo 4

Contrastado contra `Resultados\docx\REPASO_20260826-1908\`. El capítulo 4 del vault son **seis notas**
(`4.1 Tecnologías utilizadas`, `4.2 Base de datos utilizada`, `4.3 Preprocesamiento de los datasets`,
`4.4`, `4.5` y `4.6 Conclusiones del capítulo`) y es, con diferencia, el capítulo del que más contenido
se ha quedado en el vault: el volcado conserva la línea argumental de cada apartado pero **suelta casi
todos los bloques de cuantificación propia** (las dos tablas de reparto del one-hot, la segunda medición
de drift, los avisos de alcance). El inventario llega al tope de **8 hallazgos** y se ha priorizado por
umbral, dejando fuera ausencias reales pero menores (la lista de dependencias transitivas de `4.1`, la
nota del corte redundante de la Regla 1 en `4.5`). **Excluido** todo lo ya registrado en
`Las tareas de Kiko.md` por tema: **4.7**–**4.12**, **4.15**, **4.16**, **T12**, **A5**, **A6**, **A9**,
**A10**, **A11**, **A13**, **A16**, **3.6** (la confesión de *data snooping* duplicada) y **B1**.

> [!note] Alcance del inventario en este capítulo
> Este fichero registra **ausencias**. En el capítulo 4 se admiten, señaladas **explícitamente como
> tales**, un par de **erratas de etiquetado** del `.docx` que **no son ausencias** (van en el
> hallazgo 2): cifras que el `.docx` **sí publica**, pero con el denominador o el estadístico
> equivocados. Se conservan aquí porque comparten sede con la ausencia que las acompaña; **no
> deben confundirse con contenido que se quedó en el vault**.

1. **La segunda medición de drift —(B) D1 frente a las 9.711 filas normales de D2—, su tabla comparada con (A) en las dos variantes, las dos figuras que la representan y el aviso de que `delta = (A) − (B)` no es una descomposición** · `4.2 Base de datos utilizada` § *Desplazamiento de distribución entre entrenamiento y test (drift)* · el `.docx` publica **solo la medición (A)** (37 de 54, ≈ 69 %, con el ranking `src_bytes`/`dst_bytes`/`dst_host_rerror_rate`) y salta de su figura KS directamente a los outliers, de modo que desaparecen: (i) la medición **(B)**, significativa en **25 de las 54** características (31 de 122), que es la que acredita que *hasta el tráfico legítimo del test se distribuye distinto*; (ii) la tabla de las dos mediciones por variante; (iii) el desplome de `src_bytes` (0,346 → 0,091) y `dst_bytes` (0,317 → 0,107) al retirar los ataques de la población de comparación; (iv) las dos figuras propias, la de (B) y la comparativa (A) frente a (B); y (v) el aviso de que el KS es un supremo y **no es aditivo sobre una mezcla**, por lo que el delta es comparación y no reparto de causas. Duele por dos razones: **(B) es una medición propia del lote de reapertura (T2), no material importado**, y es la única que puede sostener la atribución del exceso de falsos positivos; y en su ausencia el `.docx` cierra el apartado atribuyendo el FPR a (A) y citándolo **como rango, «~8–10 %»**, que es exactamente la forma de citarlo que el protocolo del capítulo 5 prohíbe. *(Distinto del hallazgo 3 del capítulo 5, que acota la **explicación causal** en §5.1.3: aquí falta la **medición y su tabla** en su sede primaria.)* · en el capítulo 4, dentro de «Desplazamiento de distribución entre entrenamiento y test», tras el párrafo «37 de las 54 características (≈ 69 %) presentan drift…». **Coste de aplicación:** en el `.docx` la **Figura 4.5 ya está ocupada por los outliers**, de modo que insertar ahí las **dos** figuras del vault obliga a **renumerar** la de outliers y a arrastrar el corrimiento por el resto del capítulo 4 (y sus llamadas desde la prosa). Quien lo aplique debe hacerlo de una vez y verificar que la serie `4.x` sigue siendo **contigua y sin duplicados**. **Filtros (a) y (c).**

2. **La tabla de doble denominador del reparto de D2 y la lectura de R2L** · `4.2 Base de datos utilizada` § *Tamaño y reparto de los subconjuntos* · **lo ausente** es la tabla que da cada categoría de D2 con **sus dos denominadores** (DoS 7.458, Probe 2.421, R2L 2.754, U2R 200, normal 9.711, con las dos columnas de porcentaje: sobre D2 completo y sobre los 12.833 ataques) y, con ella, la lectura de que **R2L sube al 21,5 % de los ataques del test** —el dato que anticipa por qué esa categoría domina el error del sistema—. Sin la tabla, el lector del `.docx` no dispone de ninguna cifra homogénea para comparar el reparto de D3 con el de D2 · en el capítulo 4, en la viñeta «El test no replica el reparto del entrenamiento». **Filtro (a).**
   > [!warning] Dos ERRATAS del `.docx`, **no ausencias** (registradas aquí por compartir sede)
   > No son contenido que se quedara en el vault: son cifras que el `.docx` **publica mal etiquetadas**.
   > - **Denominador mezclado.** El `.docx` escribe «en D3 la categoría DoS supone ~78 % de los ataques, pero en D2 **baja a ~33 %**». Los dos números están sobre poblaciones distintas: el 78,3 % es sobre ataques y el 33,1 % sobre D2 **completo, normales incluidos**. El valor homogéneo, y el que debería figurar, es **58,1 % sobre los 12.833 ataques de D2**.
   > - **Estadístico mal nombrado.** El `.docx` dice «La **mediana** de outliers en D1 es del 4,78 %» (§ *Valores atípicos (outliers)*); el estadístico es una **media entre características**, no una mediana. Falta además el **2,44 %** de la variante de 122.
   >
   > **No trasladar a `Las tareas de Kiko.md`**: quedan consignadas aquí, marcadas como errata.

3. **La cuarta razón de la elección del dataset: la partición train/test predefinida, con el 23 % / 16 % de la revisión de 89 datasets de Goldschmidt y Chudá** · `4.2 Base de datos utilizada` § *Por qué se eligió NSL-KDD* · el `.docx` anuncia «cuatro razones, asumida su contrapartida» y enumera **solo tres razones** más la contrapartida: falta entera la razón de que NSL-KDD traiga partición oficial, que es la única de las cuatro apoyada en evidencia comparativa externa —solo el **23 %** de 89 datasets públicos de NIDS la trae, el **16 %** entre los publicados desde 2020, y su ausencia se describe como propensa a evaluaciones sesgadas y reporte selectivo—. Es también la razón que conecta la elección con la disciplina de no re-particionar el test, que el `.docx` sí defiende en «Las tres particiones oficiales» pero deja sin fundamento cuantitativo. Además, el recuento anunciado no cuadra con el enumerado · en el capítulo 4, en la lista de «¿Por qué se eligió NSL-KDD?», como cuarta razón antes de la contrapartida. **Dos avisos para quien lo aplique:** (i) **la numeración de la cita no es trasladable tal cual** — el vault escribe `[74, §5.5.4]` para Goldschmidt y Chudá, pero en el `.docx` esa obra es **`[72]`**; y el localizador **`§5.5.4` no se escribe dentro del marcador** (derogado el 2026-08-24: el marcador va **a pelo**, `[72]`, y el sitio exacto vive en la columna «Localizador por uso» de `Bibliografía.md`). (ii) **el 23 % y el 16 % son cifras de fuente externa**, tomadas de esa revisión de 89 datasets: **no hay artefacto en `Resultados/` que las respalde** y no debe buscarse allí. **Filtros (a) y (b).**

4. **El bloque «El peso real del one-hot en el espacio de características»: la tabla de reparto por variable categórica y sus tres lecturas** · `4.3 Preprocesamiento de los datasets` § *4.3.3 Codificación de variables categóricas → El peso real del one-hot en el espacio de características* · el `.docx` conserva la regla del vocabulario y el delta 77 → 122, pero **no publica ni una sola de las cifras de reparto**: que de las 122 características **84 son dummies y 38 numéricas** (el **68,9 %** del espacio lo crea el preprocesado), que `service` aporta **70 columnas** —el **57,4 % de las 122** y el **83,3 % de todas las dummies**—, que `protocol_type` aporta 3 y `flag` 11, y que de las **45 columnas recuperadas por el fix, 44 son servicios y 1 es un flag**. Es cuantificación propia sobre el artefacto `specialized_nsl_kdd_vocabulario_onehot.csv` y es la que da sentido a la afirmación, que el `.docx` sí hace, de que el fix recuperó «algunas de las señales más discriminantes»: sin la tabla, el lector no puede saber que el fix es esencialmente un fix de `service` · en el capítulo 4, cerrando 4.3.3, tras el párrafo de la exclusión de las categorías exclusivas de D2. **Filtros (a) y (b).**

5. **El bloque «Qué se poda realmente»: que la selección de características es una poda del one-hot de `service`, con el contrafactual 122 → 109 y el aviso del efecto asimétrico sobre las dos etapas** · `4.3 Preprocesamiento de los datasets` § *4.3.5 Selección de características → Qué se poda realmente: la selección es un recorte del one-hot de `service`* · el `.docx` publica el resumen «122 → 54 (−1 varianza, −7 correlación, −60 importancia)» pero pierde el desglose que lo interpreta: de las **60 eliminadas por importancia, 58 son dummies** (55 de `service` y 3 de `flag`, ninguna de `protocol_type`), el bloque `service_*` cae de **70 a 15 (−78,6 %)** —las 70 columnas de partida son las del hallazgo 4, al que se remite para no volver a enunciarlas—, el **81 % de las 68 eliminaciones** son columnas de esa única variable y **sin tocar `service` el espacio habría bajado de 122 a 109, no a 54**. Con ello desaparece también el aviso de que la poda tiene **dos caras**: mejora la etapa de firmas (deja de sobreajustar dummies de servicios raros de D3) y **perjudica al detector de anomalías** (un servicio inusual es la señal débil que delata un ataque nunca visto). Esa segunda cara es la que el `.docx` contradice en 4.6, donde afirma que la selección «resultó tener un doble efecto beneficioso» · en el capítulo 4, dentro de 4.3.5, entre «El detalle completo… está en `selected_features.txt`» y el apartado del experimento H1. **Filtros (a) y (b).**

6. **La limitación asumida de SMOTE sobre variables one-hot** · `4.3 Preprocesamiento de los datasets` § *4.3.4 Equilibrio y balanceo de muestras* (callout `> [!warning] Limitación asumida de SMOTE sobre variables one-hot`) · el `.docx` conserva el protocolo del mini-experimento y el argumento anti-*leakage* de aplicar SMOTE dentro de cada *fold*, pero no declara en ningún punto que la interpolación lineal de SMOTE, aplicada sobre las **26 columnas dummy binarias del set de 54** (3 de `protocol_type_*`, 15 de `service_*`, 8 de `flag_*`), genere **valores fraccionarios en variables que en los datos reales solo valen 0 o 1** (`flag_S0 = 0,4`), ni la salvedad de que los árboles lo toleran porque sus cortes siguen separando en torno a 0,5, ni el descarte explícito de SMOTE-NC por alcance. Duele porque el `.docx` adopta SMOTE como estrategia por defecto en tres de los cuatro algoritmos de firmas y presenta esa decisión como cerrada por el experimento, sin la impureza que el propio trabajo se reconoce · en el capítulo 4, cerrando 4.3.4, tras las tres conclusiones del experimento y antes de 4.3.5. **Filtro (b).**

7. **La REMISIÓN a la acotación «qué sí miró D2», en las cuatro sedes del capítulo 4 que enuncian la regla sin acotar** · `4.4 Entrenamiento del modelo de detección de anomalías` § *Protocolo de entrenamiento y validación*, punto 3 · `4.5 Entrenamiento del modelo de detección basado en firmas` § *Protocolo de entrenamiento*, última viñeta · `4.6 Conclusiones del capítulo` § *La ausencia de fuga de información como principio transversal* (callout `> [!warning] Alcance real de la regla: qué sí miró D2`) y § *Comparación amplia bajo protocolo común* · **el `.docx` sí acota la regla, y lo hace bien**: el capítulo 3, en § *Protocolo de evaluación común*, la desarrolla entera —«Ese perímetro es acotado y conviene no ampliarlo al enunciarlo, porque hay una excepción y se declara aquí. Tres decisiones del sistema se tomaron comparando resultados sobre D2: el detector de la etapa 1, el clasificador de la etapa 2 y el set de características (54 frente a 122)»—, y vuelve a aparecer en 4.3.5, en el capítulo 6 y tabulada. **Lo que falta no es la confesión, sino que las sedes del capítulo 4 la enuncien sin acotar ni remitir a ella**, de modo que el lector que llega al cierre del capítulo 4 lee una inmunidad sin salvedad ni puntero. Las cuatro sedes:
   - **`4.4`** — no es una omisión, sino una **afirmación reforzada y falsa**: el `.docx` escribe «la elección es metodológicamente defendible porque el conjunto solo contiene ataques conocidos del entrenamiento… **y D2 no interviene en ningún momento**», donde el vault dice «D2 no interviene en **esta selección**» con su salvedad. El refuerzo del cuantificador convierte una afirmación acotada y cierta en una **afirmación universal que el propio capítulo 3 desmiente**; esto agrava la gravedad respecto de las otras sedes.
   - **`4.5`** — «D2 es intocable durante todo el proceso», sin salvedad.
   - **`4.6`, cierre del capítulo** — «D2 permanece intocable para el entrenamiento, para el ajuste de hiperparámetros, para la fijación de los umbrales y para la estrategia de balanceo… Esta disciplina es lo que hace creíbles los resultados».
   - **`4.6` § *Comparación amplia bajo protocolo común*** — el `.docx` **suprime el inciso del vault** «—selección hecha, como se ha declarado arriba, con métricas sobre D2—», que era justamente la remisión interna que enlazaba la comparación con la salvedad.

   **Qué se pide: una REMISIÓN, no repetir el inciso.** Repetir la confesión en las cuatro sedes chocaría de frente con la tarea **3.6** de `Las tareas de Kiko.md`, que ordena dejarla **en un solo sitio** y remitir desde el otro. Lo aplicable es una fórmula breve del tipo «con el alcance acotado en el protocolo de evaluación común (cap. 3)» —y, en `4.4`, además **revertir «en ningún momento» a «en esta selección»** · en el capítulo 4: en el punto 3 del protocolo de 4.4, en la viñeta «D2 es intocable…» de 4.5, en el párrafo de cierre de 4.6 y restituyendo el inciso en § *Comparación amplia bajo protocolo común*. **Filtros (a) y (c).**

8. **El deslinde entre el AUC-ROC de validación de la etapa 1 y el AUC-ROC sobre D2 de §5.1.2** · `4.4 Entrenamiento del modelo de detección de anomalías` § *Configuraciones ganadoras* (callout `> [!important] El AUC-ROC de validación no es el de § 5.1.2`) · el `.docx` publica **las dos cifras en capítulos distintos y sin ninguna advertencia entre ellas**: el **0,9918** de IsolationForest en la Tabla 4.11 (AUC de validación, sobre `D1_val` + 5.000 filas de D3, solo ataques conocidos) y el **0,923** del capítulo 5 (sobre D2 completo, con los 17 tipos 0-day dentro). Falta la tabla de tres filas que las separa —población, contenido de ataque, valor— y, sobre todo, la lectura de que la caída **no es un error de ninguna de las dos** sino la diferencia esperable entre el conjunto sobre el que se eligió la configuración y el conjunto de test, más difícil; y la regla derivada de que las cifras de validación sirven para **ordenar configuraciones dentro de cada algoritmo**, no como estimación del desempeño del detector. Sin ese deslinde, la lectura natural del `.docx` es que el detector «empeoró» entre el capítulo 4 y el 5 · en el capítulo 4, tras la Tabla 4.11 y su párrafo de lectura, antes del apartado de firmas. **Filtro (a).**

## Capítulo 3

Contrastado contra `Resultados\docx\REPASO_20260826-1908\`. El capítulo 3 del vault son **seis notas**
(`3.1` a `3.6`) y el volcado es, de los cuatro capítulos barridos, **el más fiel en estructura y el
más agresivo en poda de argumentación**: conserva las cuatro tablas y los enunciados de cada
decisión, pero suelta casi todos los tramos que explican **por qué** la decisión es esa. El
inventario se cierra en **7 hallazgos, por debajo del tope de 8**, y la razón es que buena parte de
lo que en el vault vive en el capítulo 3 **sí está publicado, pero en otro capítulo**: el argumento
del orden de la cascada está en §2.2.4, la disciplina anti-fuga y el punto de ajuste del escalador
están en §4.3, y el contraste de FPR contra el baseline está en §5.3. Declararlas ausentes habría
sido falsear el recuento, así que se han recortado o descartado en vez de rellenar hasta ocho.
**Excluido** todo lo ya registrado en `Las tareas de Kiko.md` por tema: **3.5** (los párrafos del
contrafactual, que se mueven al capítulo 5), **3.6** (la confesión de *data snooping* duplicada),
**3.10**, **T12**, **5.4** (el diccionario de las decisiones H-*n*), **2.11** (los tres trabajos
anunciados y no escritos, que es donde vive el antagonista de la cascada invertida) y **B1**.

1. **La sección §3.2.2 «Por qué una cascada, y en este orden» completa: su deslinde con §2.2.4.5 y la tercera razón, el *semantic gap*** · `3.2 Arquitectura del sistema` § *3.2.2 Por qué una cascada, y en este orden* · el `.docx` **sí publica** la justificación del orden, pero **en el capítulo 2** y solo con dos de las tres razones: la de **Diseño** («El clasificador de firmas nunca ha visto tráfico normal») y la de **Cobertura** («es la única etapa capaz de reaccionar ante lo no catalogado»). Lo genuinamente ausente es (i) la tercera razón, el ***semantic gap*** —que la etapa 2 **no está ahí para detectar, sino para traducir** el veredicto poco accionable de la etapa 1 en un enunciado con significado operativo—, expresión que **no aparece ni una sola vez** en todo el `.docx`; y (ii) el **deslinde** que declara que el argumento general es de §2.2.4.5 y que §3.2.2 **solo lo instancia** sobre estos splits y estos modelos. Duele por una consecuencia concreta y verificable: el capítulo 3 del `.docx` abre la medición contrafactual con «Para comprobar que **el orden de la cascada fijado en §3.2.1** es el adecuado», y §3.2.1 no contiene ninguna justificación del orden — la remisión apunta a un apartado que solo describe las dos etapas. *(El «antagonista directo» sobre el mismo dataset con el orden invertido **no** se cuenta aquí: es el mismo tema que **2.11**, que ya reclama escribir los tres trabajos anunciados.)* · en el capítulo 3, entre «Las dos etapas» y el párrafo de la medición contrafactual. **Filtros (a) y (b).**

2. **La divergencia declarada con ADAM: que en el precedente clásico la segunda etapa puede exonerar y aquí eso está prohibido por diseño, con el matiz de que no es propiedad de toda cascada** · `3.2 Arquitectura del sistema` § callout *Divergencia con ADAM* · y `3.6 Conclusiones del capítulo`, segunda viñeta · el `.docx` no menciona ADAM en ningún punto del cuerpo (cero ocurrencias), y su capítulo 3 de conclusiones enumera **cuatro** viñetas donde el vault tiene cinco: la que falta es exactamente esta. Se pierden tres cosas encadenadas: que el antecedente más próximo usa la etapa 2 para **filtrar falsas alarmas y bajar el FPR del detector**, que aquí esa exoneración se **prohíbe** (decisiones **H-5** y **P-5**) de modo que la etapa 2 cualifica la alarma pero nunca la retira, y el **matiz** de que esa propiedad **no se sigue de la topología** sino de una elección propia —entrenar la etapa 2 solo con D3, sin clase `normal`—, siendo el precio de la alternativa perder la lectura de `unknown` como candidato a 0-day. Duele porque es el eje sobre el que se apoya P-5, que el `.docx` sí invoca en el capítulo 3 («en este sistema unknown es una alarma») y en el capítulo 5, sin haber declarado nunca frente a qué alternativa se decidió así; y porque la lectura como **divergencia de criterio y no como superioridad** es lo que impide que el capítulo se lea como una reivindicación no medida. *(La obra de Barbará figura además en la lista bibliográfica del `.docx` sin cita en el cuerpo: eso **no** se cuenta aquí — es **remisión a B1**, donde ya está registrada, aunque allí quede agrupada con la comparación tabular de §3.5, que es otra sede.)* · en el capítulo 3, cerrando el apartado de la arquitectura, y como quinta viñeta de «Conclusiones del capítulo». **Filtro (b).**

3. **§3.4.5 «El coste del paradigma»: la cadena que deriva los falsos positivos del propio planteamiento *one-class*, con la formulación de Sommer y Paxson** · `3.4 Modelo de detección de anomalías` § *3.4.5 El coste del paradigma* · el `.docx` publica en el capítulo 2 la versión genérica («El precio de dicha capacidad se paga en mayor tasa de falsos positivos») y en el capítulo 3 las cifras de FPR (10,2 % y 8,5 %) como término de contraste del contrafactual, pero **no el razonamiento que las anticipa desde el diseño**: (i) la formulación de que detectar lo novedoso obliga a modelar solo la normalidad y hacerlo bien **«exige tener un modelo *perfecto* de normalidad»** —expresión con cero ocurrencias en el `.docx`, donde Sommer y Paxson solo figuran en la lista bibliográfica y anclados al capítulo 5—; (ii) los **tres pasos** que la encadenan: D1 es una *muestra* del tráfico legítimo, toda región legítima poco representada en D1 recibe puntuación alta, y esas regiones se traducen en falsos positivos **sin que medie ningún error de implementación**; y (iii) la lectura que se sigue, que el FPR **no es un defecto del autoencoder ni un fallo de ajuste** sino el coste estructural del paradigma, agravado por fijar el umbral sobre D1. Duele porque el capítulo 6 mantiene una limitación entera («Tasa de falsos positivos condicionada por el drift») cuyo respaldo *de diseño* es este apartado: sin él, el FPR aparece por primera vez en resultados y se lee como un mal resultado en vez de como una consecuencia declarada de antemano · en el capítulo 3, cerrando «Modelo de detección de anomalías», tras «Papel en la cascada». **Filtros (b) y (c).**

4. **Las dos precisiones que sostienen el modelo de amenaza, la frase que acota lo que el capítulo 5 puede afirmar, y el callout de alcance de la exclusión** · `3.1 Requisitos del sistema` §§ *3.1.5.1 Adversario asumido* y *3.1.5.2 Por qué no se realiza un experimento adversario* (incluido el callout `> [!warning] Alcance de esta exclusión`) · el `.docx` vuelca la tabla de seis dimensiones y los **titulares** de las tres razones, pero suelta todo lo que las sostiene: (i) que el conocimiento es **caja negra porque no hay canal por el que dejara de serlo** —evaluación en diferido sobre corpus cerrado, sin consulta, respuesta observable ni bloqueo—, de modo que un adversario de caja gris o blanca **no tiene forma de existir** dentro del montaje, y no se descarta por conveniencia; (ii) que el **envenenamiento** se excluye porque D1 y D3 derivan de `KDDTrain+`, fichero fijo e inmutable, sin reentrenamiento en línea ni superficie de inyección; (iii) la frase que fija el alcance de todo el capítulo 5 —«el sistema detecta… **ataques de un adversario que no intenta evadirlo**; cualquier lectura más amplia excede lo medido»—; (iv) del tercer motivo, la distinción entre ataque **en el espacio de características** y **en el espacio del problema** y el dato de que el corpus **no conserva carga útil ni instante absoluto**, de forma que ni siquiera podría reconstruirse el flujo del que procedería un vector perturbado; y (v) el callout que declara que esto justifica **no hacer el experimento, no que el problema no exista**, y que **P10 sigue aplicando como límite declarado**. Duele porque el `.docx` **invoca después esas piezas sin haberlas establecido**: el capítulo 6 afirma que se argumenta «por qué un experimento adversario sobre características agregadas sería irrealizable **en el espacio del problema**», expresión que en el capítulo 3 no aparece · en el capítulo 3, dentro de «Modelo de amenaza»: las dos precisiones tras la tabla de dimensiones, y el callout cerrando la lista de los tres motivos. **Filtros (a) y (c).**

5. **Los dos rasgos del protocolo que tienen nombre propio en la literatura: la evaluación por subgrupos y el reconocimiento de conjunto abierto, declarados en su sede de diseño** · `3.3 Metodología de funcionamiento del sistema` § *3.3.2 Protocolo de evaluación común*, párrafo «Dos rasgos del protocolo tienen nombre propio en la literatura y no son una construcción *ad hoc* de este trabajo» · el `.docx` conserva el protocolo común y el perímetro de la regla sobre D2, pero pierde el párrafo que **legitima las dos métricas propias del trabajo antes de usarlas**: que el **recall 0-day desagregado por tipo** es una *evaluación por subgrupos*, práctica de reporte recomendada porque una métrica agregada oculta el desempeño en las poblaciones minoritarias —argumento con **cero ocurrencias** en el `.docx`—, y que la salida `unknown` corresponde al problema formal de **reconocimiento de conjunto abierto**, que es lo que justifica medir la tasa de enrutado a esa clase como métrica propia y con su alcance declarado (decisión **H-6**). El `.docx` sí usa el término *open-set* en los capítulos 5 y 6, pero **solo para explicar por qué el mecanismo se queda corto**, nunca en el capítulo 3 para fundamentar que medirlo es lo correcto. Duele porque deja las dos métricas más características del trabajo presentadas como invención propia en resultados, en lugar de como decisiones de diseño con respaldo · en el capítulo 3, cerrando «Protocolo de evaluación común», antes de «Modelo de detección de anomalías». **Filtros (a) y (b).**

6. **El apartado «Por qué familias clásicas y no una arquitectura tabular profunda», y en particular la observación pareada interna, que no es material bibliográfico** · `3.5 Modelo de detección basado en firmas` § *3.5.2 Algoritmos candidatos → Por qué familias clásicas y no una arquitectura tabular profunda* · **remisión parcial a B1**, que ya registra como decisión pendiente las obras de Grinsztajn, McElfresh, Shwartz-Ziv y Armon y Hollmann (TabPFN) y declara que «dependen de volcar la comparación tabular de §3.5, hoy ausente del `.docx`». Lo que **B1 no cubre** y sí se pierde es la parte **propia y no bibliográfica** del apartado: (i) la tabla que instancia los **cinco criterios de McElfresh sobre D3** —58.630 filas, ≈ 1.086 muestras por característica, colas largas en `src_bytes`/`dst_bytes`/`duration`/`count`, 60 de 122 características no informativas, 4 clases— con la conclusión acotada de que el problema **cae en el cuadrante donde la evidencia indica que no compensa**, y no que el aprendizaje profundo «no sirva»; (ii) la comprobación interna con datos del propio trabajo, **RandomForest supera a HistGradientBoosting en 8 de las 10 semillas, en las dos variantes**, enunciada expresamente como observación **pareada** y **solo sobre `f1_macro`**; (iii) la declaración de por qué se enuncia así —que el par 0,804 frente a 0,822 de la semilla 42 **no establece orden**, porque las bandas se solapan y el valor de RandomForest cae fuera de su propia banda—; y (iv) el callout **«Dos “8 de 10” distintos»**, que advierte de que el conteo de calidad y el de tiempo de entrenamiento son magnitudes independientes, con distinta solidez —el segundo solo se sostiene en la variante de 122— y **coinciden en el valor por casualidad**. Duele porque es autocrítica cuantificada sobre artefactos propios, y porque sin (iii) y (iv) la elección de familia clásica queda como preferencia por defecto, que es justamente lo que §3.1.4 prohíbe · en el capítulo 3, dentro de «Algoritmos candidatos» de la etapa de firmas, tras la Tabla 3.3. **Filtro (b).**

7. **El anunciado de la estructura de requisitos, que el `.docx` publica incompleto y contradicho por sus propios encabezados** · `3.1 Requisitos del sistema` § párrafo de apertura · el `.docx` abre el capítulo con «Dividimos los requisitos en **funcionales y de alcance**» y a continuación imprime **cinco** encabezados: «Requisitos funcionales», «Requisitos de datos», «Requisitos no funcionales», «Requisito de alcance» y «Modelo de amenaza». El vault trae la formulación correcta y completa: los requisitos se organizan en **funcionales, de datos, no funcionales y de alcance**, y **se cierran con el modelo de amenaza**, que es un requisito de naturaleza distinta —no dice qué hace el sistema, sino **frente a quién** afirma hacerlo—. Con el anuncio truncado se pierden además dos matices del propio §3.1.4: que el requisito de alcance se adopta como **decisión de proyecto** con la forma «**no basta con un algoritmo por etapa**», y que esa comparación alimenta directamente §5.1 y §5.2. Es el mismo defecto de forma que el hallazgo 3 del capítulo 4 (recuento anunciado que no cuadra con el enumerado), y aquí es más visible porque el desajuste es de dos frente a cinco · en el capítulo 3, en el párrafo de apertura de «Requisitos del sistema», y en el cierre de «Requisito de alcance». **Filtro (a).**

## Capítulo 2

Contrastado contra `Resultados\docx\REPASO_20260826-1908\`. El capítulo 2 del vault son **catorce notas**
(`2.1.1`–`2.1.6`, `2.2.1`–`2.2.5` y `2.3.1`–`2.3.3`) y el volcado es, de los cinco capítulos barridos,
**el que más masa de texto ha dejado atrás**: el `.docx` conserva los tramos expositivos de cada
apartado y **suelta casi todas las tablas comparativas, todos los cierres de sección y los dos
apartados finales de cada bloque**. Dos ausencias son de sección entera y se ven ya en el índice: **`2.1.6
Metodologías y buenas prácticas` no existe** (el `.docx` salta de `2.1.5` a `2.2`) y **`2.3.3` se corta
tras los tres pasos de P10**, dejando fuera **seis** de sus ocho apartados: el `.docx` solo publica los
dos que desarrollan P9 y P10 (`2.3.3.2` y `2.3.3.3`), de modo que no llegan `2.3.3.1`, `2.3.3.4`,
`2.3.3.5`, `2.3.3.6`, `2.3.3.7` ni `2.3.3.8`. **`2.3.3.1 El coste de los falsos positivos en
operación` se cuenta aquí** —no se volcó al capítulo 2— aunque su sustancia sí esté publicada más
adelante, en §5.4; por eso no genera hallazgo propio y sí figura, acotado, en el hallazgo 4. El inventario llega al tope de
**8 hallazgos** y se ha priorizado por umbral.

**Excluido** todo lo ya registrado en `Las tareas de Kiko.md` por tema, que en este capítulo es mucho:
**2.10** (estado del arte / trabajos relacionados), **2.11** (los tres trabajos anunciados en `2.2.4.5`
— Ji, Kim *et al.* y Khraisat *et al.*, incluida la crítica a la revisión de Khraisat por no taxonomizar
lo híbrido), **2.12** (**la tabla de los cuatro sentidos de «híbrido» y la definición operativa del
sentido (a)** de `2.2.3.2`, que es exactamente lo que 2.12 manda restituir), **2.13**, **2.14** (**los dos
tramos anunciados y no desarrollados de `2.3.2`**: `2.3.2.3` la cara ofensiva, `2.3.2.4` *adversarial ML*
y `2.3.2.5` el panorama de datasets), **2.16** (que ordena **comprimir** `2.3.3`, no ampliarla — ver el
aviso de abajo), **2.17**, **2.18**, **2.25**, **2.26**, **4.10** (el 883:1), **6.4** (el adversario
estático duplicado), **T13**, y **B1** en lo tocante a **CRISP-DM** (`2.1.6.1`), **Bergstra y Bengio**,
**Storkey**, **Barbará (ADAM)** y **Grinsztajn / McElfresh**. Excluidos también, por remisión, los
**Anexos C y D**: C.4, C.5, C.6 y todas las anclas de §2.1.1–2.1.3, §2.1.5, §2.2 y §2.3.

> [!warning] Tensión declarada con la tarea 2.16
> **2.16** ordena *comprimir* `2.3.3` porque «desarrolla P9 y P10 con extensión de capítulo de
> resultados». El hallazgo 8 de abajo pide **restituir otros apartados de esa misma sección**.
> No es contradicción: 2.16 apunta a los **dos apartados que sí se volcaron** (P9 y P10, hoy los únicos
> presentes y los más largos), y el hallazgo 8, a **dos de los seis que no se volcaron ninguno**.
> Aplicados juntos, el saldo de longitud puede ser incluso neutro. Quien los aplique debe hacerlo en ese
> orden: **primero comprimir P9/P10, después restituir**.

> [!important] Aviso de numeración: el vault y el `.docx` NO numeran igual
> Las notas del capítulo 2 arrastran una numeración `[n]` **propia y anterior al mapeo de la bibliografía
> del `.docx`**, y en varios casos el mismo número designa obras distintas. Verificados en disco:
> Arp *et al.* es **`[18]` en el vault** y **`[49]` en el `.docx`**; Axelsson (falacia de la tasa base) es
> **`[24]` en el vault** y **`[73]` en el `.docx`**; Sommer y Paxson es **`[21]` en el vault**; y `[12]`
> es Hastie en el vault pero **Khraisat *et al.* en el `.docx`**. **Ningún marcador del vault se traslada
> tal cual**: al aplicar cualquiera de estos hallazgos hay que remapear contra la lista del `.docx`, y lo
> que no tenga número allí va como `[CITA: …]` para que lo resuelva el `researcher`. Y a pelo, sin
> localizador (derogación del 2026-08-24): los `[12, cap. 7]` y `[5, cap. 3]` que aún hay en `2.1.6` y
> `2.1.5` **pierden el localizador** al volcarse.

1. **`2.1.6 Metodologías y buenas prácticas` no existe en el `.docx`, y con ella se cae el protocolo experimental: la tabla de los tres conjuntos con la línea roja del test, la tabla del perímetro anti-fuga con las cinco decisiones y dónde se resuelve cada una, y la tabla de las tres formas de fuga con la regla «todo el entrenamiento, nada del test»** · `2.1.6 Metodologías y buenas prácticas` §§ *2.1.6.2 Separación train/validación/test y la línea roja del test* y *2.1.6.5 Fuga de información (data leakage)* · el índice del `.docx` salta de «2.1.5. Métricas de evaluación de modelos» a «2.2. Ciberseguridad»: la sección entera falta. De ella, lo genuinamente ausente —y no publicado en ninguna otra sede— es (i) la tabla que asigna a cada conjunto **qué se hace con él y cuántas veces se usa**, con la distinción de que validación es donde se **decide** y test donde solo se **mide, una sola vez**; (ii) la tabla del **perímetro anti-fuga** que enumera las **cinco** decisiones de selección de la arquitectura y sobre qué partición se resuelve cada una (hiperparámetros de firmas → CV sobre D3; umbral del detector → percentil 95 sobre `D1_val`; configuración interna de cada algoritmo de la etapa 1 → AUC-ROC sobre `D1_val` + muestra de D3; balanceo → `f1_macro` en CV sobre D3; umbral de confianza del híbrido → probabilidades *out-of-fold* sobre D3); y (iii) la **tabla de las tres formas típicas de fuga** —sobremuestreo antes de particionar, escalador ajustado con test, vocabulario de categorías construido con el test— con el patrón común que las une («una transformación que aprende algo de los datos se ajusta sobre un conjunto que incluye el test») y la asimetría que de ahí se deriva, **todo el entrenamiento, nada del test**, con su advertencia de que descartar información legítima del entrenamiento *no* es prudencia anti-fuga. Duele porque el `.docx` **aplica esas reglas por todo el capítulo 4 sin haberlas enunciado nunca**: el capítulo 4 justifica el escalador ajustado solo sobre train invocando «*data leakage*» como término dado por definido, y `4.3.7` se titula «Preprocesado sin fuga de datos» sin que exista una sede que diga qué es la fuga. *(**Excluido** de este hallazgo: `2.1.6.1` **CRISP-DM**, que es la fila de CRISP-DM de **B1**; y la declaración de las **tres decisiones tomadas mirando D2**, que es **3.6**.)* · en el capítulo 2, como nuevo apartado 2.1.6 tras «Métricas de evaluación de modelos» y antes de «Ciberseguridad». **Filtros (a) y (b).**

2. **El resto de `2.1.6`: el argumento combinatorio de la estratificación con sus cifras, el criterio de rejilla pequeña y publicada, la renuncia declarada al contraste estadístico, y `2.1.6.8` con la definición del *semantic gap*** · `2.1.6 Metodologías y buenas prácticas` §§ *2.1.6.3 Validación cruzada estratificada*, *2.1.6.4 Búsqueda de hiperparámetros con rejillas pequeñas y publicadas*, *2.1.6.7 Reproducibilidad* y *2.1.6.8 La sobreestimación de la validación cruzada frente al test* · el `.docx` **usa** `StratifiedKFold(5)` en los capítulos 4 y 5 y dice que «los grids son deliberadamente pequeños (decisión de alcance del proyecto)», pero no publica en ningún punto: (i) **por qué** la estratificación no es opcional aquí —el argumento combinatorio de que un reparto aleatorio puede dejar un *fold* **sin ninguna muestra** de la clase rara, con `u2r` en **52** frente a las **45.927** de `dos` en D3, del orden de **41 muestras por fold** en entrenamiento, cifra que además condiciona la viabilidad de SMOTE con `k=5` vecinos—; (ii) las **dos razones** de la rejilla pequeña, que el objetivo es **comparar arquitecturas, no exprimir un algoritmo** (un presupuesto de búsqueda desigual contaminaría la comparación) y que una rejilla publicada es **auditable**; (iii) el callout que separa **tres términos que no son sinónimos** (*K-fold* / *N-fold* son el mismo procedimiento; la desviación típica es otra cosa, y en este trabajo aparece en **dos usos que no deben mezclarse**, entre *folds* y entre semillas); y (iv) **`2.1.6.8` entera**: que la validación cruzada estima el error bajo el supuesto de misma distribución, supuesto **falso** aquí por dos motivos que se suman —desplazamiento entre particiones, con el deslinde de que **no es *concept drift*** porque el corpus no tiene columna de fecha, y **17 tipos de ataque ausentes del entrenamiento**—, la conclusión de que la CV «estima lo que dice estimar y no otra cosa», la **expectativa de lectura** de que una caída apreciable entre validación y test **no indica un experimento mal hecho** sino uno honesto sobre un problema de conjunto abierto, y la **definición del *semantic gap***, que esta sede es la única que da. Duele por tres consecuencias verificables, y la primera **acotada tras verificación**: el `.docx` **sí** acompaña la degradación sobre D2 de una lectura mínima —«La brecha sistemática entre el `f1_macro` de validación cruzada (0.945–0.980) y el de D2 (0.746–0.822) refleja el desplazamiento distribucional D3→D2 … es una brecha de generalización esperable, no un fallo de los modelos»—, de modo que **no es cierto que la publique sin marco alguno**; lo que falta de ese marco es lo que esa frase no dice y solo `2.1.6.8` da: el **deslinde de que no es *concept drift*** (el corpus no tiene columna de fecha), el **segundo motivo que se suma al primero** —los **17 tipos de ataque ausentes del entrenamiento**, que el `.docx` menciona como propiedad del dataset pero nunca como causa de que la CV **sobreestime**—, la conclusión de que la CV «estima lo que dice estimar y no otra cosa» y la **definición del *semantic gap***. Además, sostiene la comparación entre los cuatro algoritmos sobre el mismo objeto de CV sin decir por qué eso la hace justa; y el término ***semantic gap*** tiene **cero ocurrencias** en todo el documento pese a que el capítulo 3 lo necesita (ver el hallazgo 1 del capítulo 3, que reclama la **instanciación** en §3.2.2; **aquí falta la definición**, que es otra sede y no lo duplica). *(**Excluidos**: el callout **«Un umbral de confianza no es una regla de rechazo óptima»**, que es el mismo tema que el hallazgo 5 del capítulo 5; y `2.1.6.6`, cuya sustancia —el `f1_macro` frente a la exactitud bajo desbalance— sí está publicada en 2.1.5 y en 2.2.5.)* · en el capítulo 2, en el mismo apartado 2.1.6 del hallazgo anterior. **Filtros (a) y (c).**

3. **`2.2.3.4 Limitaciones transversales de los NIDS`: cifrado, volumen y velocidad, evasión, falsas alarmas y la advertencia de Sommer y Paxson** · `2.2.3 Taxonomía de los IDS` § *2.2.3.4 Limitaciones transversales de los NIDS* · el `.docx` recorre los cuatro ejes de la taxonomía y pasa directamente a «Detección por firmas frente a detección por anomalías»: el apartado no está. Se pierden cinco piezas, y ninguna es decorativa: (i) el **cifrado** —con TLS generalizado el sensor observa un flujo opaco, y de ahí la consecuencia metodológica que **favorece el enfoque adoptado**: los métodos basados en **metadatos y estadísticas de flujo**, que es exactamente lo que codifican las características de NSL-KDD, siguen siendo aplicables sobre tráfico cifrado porque no dependen del *payload*—; (ii) **volumen y velocidad**, con el compromiso muestreo/agregación y su coste en resolución; (iii) la **evasión** clásica de IDS (fragmentación y solapamiento que explotan diferencias de reensamblado, ofuscación, canal lento); (iv) las **falsas alarmas** como limitación de mayor impacto práctico; y (v) la advertencia de **Sommer y Paxson** de que la detección de intrusiones es un dominio **inusualmente hostil** para el aprendizaje automático, con los tres rasgos que la sostienen —variabilidad del tráfico «normal», coste asimétrico de los errores y dificultad de obtener datos etiquetados representativos—. Duele por dos razones concretas: la obra de **Sommer y Paxson figura en la lista bibliográfica del `.docx`** y el Anexo D de `Las tareas de Kiko.md` solo le da un ancla, en el capítulo 5, de modo que la advertencia que «enmarca todo el trabajo» nunca se enuncia; y el argumento del cifrado es **el único sitio donde el trabajo defiende su propia representación de entrada** frente a la objeción evidente de que NSL-KDD no mira el contenido. *(Distinto del hallazgo 3 del capítulo 3, que reclama la cadena de `3.4.5` sobre el **coste del paradigma one-class**; aquí lo ausente son las limitaciones del **medio**, no del paradigma.)* · en el capítulo 2, en «Taxonomía de los IDS», entre «El sistema construido en este trabajo opera en modo pasivo y en diferido» y «Detección por firmas frente a detección por anomalías». **Filtros (a) y (b).**

4. **`2.2.4.3 El falso positivo como coste operativo real`: la cadena que va de la tasa al abandono efectivo del detector** · `2.2.4 Detección por firmas frente a detección por anomalías` § *2.2.4.3 El falso positivo como coste operativo real* · **acotado, porque la mitad estadística sí está publicada**: el `.docx` trae la **falacia de la tasa base** con su fuente y hasta una proyección aritmética a prevalencias hipotéticas, pero **en §5.4** («Qué significa este FPR a prevalencia realista»), no en el capítulo 2. Lo genuinamente ausente es la **cadena operativa** que convierte ese resultado estadístico en un problema humano: cada alerta consume tiempo de triaje → el volumen absoluto satura al equipo → la saturación produce **fatiga de alertas** → se ignoran por defecto o se silencian reglas enteras → **abandono efectivo**, con el IDS encendido pero sin cumplir su función. La expresión «fatiga de alertas» tiene **cero ocurrencias** en el `.docx`. Con ella se pierde también la formulación de Axelsson en los términos del analista —no importa la probabilidad de alertar **dado que hay ataque**, sino la de que haya ataque **dado que ha sonado la alerta**— y, sobre todo, **la regla que de ahí deriva el trabajo**: que la tasa de falsos positivos **se reporte siempre junto al recall, nunca por separado**, y que el umbral se documente como decisión de diseño explícita y no como un parámetro escondido en el código. Duele porque esa regla **se cumple sistemáticamente en los capítulos 4, 5 y 6** —hasta el punto de que §5.3 la invoca («El FPR, obligatoriamente en el mismo cuadro»)— sin que exista una sede que la haya establecido; y porque su fundamento **métrico** complementario (`2.1.5.4`: recall y FPR son dos coordenadas del mismo punto de una curva, no dos métricas independientes) tampoco se volcó · en el capítulo 2, dentro de «Detección por firmas frente a detección por anomalías», tras el párrafo del precio de la detección por anomalías y antes de la justificación del orden de la cascada. **Filtros (a) y (c).**

5. **`2.2.4.4`: la tabla que compara los dos paradigmas criterio a criterio, y la lectura que la cierra** · `2.2.4 Detección por firmas frente a detección por anomalías` § *2.2.4.4 Comparación de ambos paradigmas* · **acotado, porque la formulación final sí está publicada**: el `.docx` cierra la justificación del orden con «la etapa de anomalías decide **si un flujo es sospechoso**, y solo lo sospechoso pasa a la etapa de firmas, **que decide qué es**», de modo que la dicotomía *¿es esto sospechoso?* / *¿qué es esto?* **no se pierde**. Lo genuinamente ausente son dos cosas: (i) la **tabla que enfrenta los dos paradigmas en los ocho criterios** —principio de funcionamiento, ataques conocidos, ataques 0-day, falsas alarmas, explicabilidad de la alerta, mantenimiento, datos necesarios y salida que produce—, que no existe en el `.docx`, donde cada paradigma se describe **por separado y en prosa**; y (ii) **la lectura que la justifica**: que lo relevante no es cuál columna «gana», sino que **las casillas desfavorables de una columna se corresponden con las favorables de la otra**. Duele porque el `.docx` pasa de la descripción de los dos paradigmas **directamente** a «La justificación del orden de la cascada… es la siguiente»: el lector recibe la conclusión de diseño (combinarlos domina a elegir uno) y la formulación que la resume, pero no el escalón intermedio —la complementariedad criterio a criterio— que la hace inevitable en lugar de plausible. Es además la formulación que el capítulo 6 reutiliza literalmente al concluir que «firmas y anomalías no compiten, sino que se complementan» · en el capítulo 2, entre el párrafo de la etapa 1 *one-class* y «La justificación del orden de la cascada». **Filtros (a) y (c).**

6. **`2.2.5.2 Gradación de visibilidad en red`: la tabla de detectabilidad por categoría y la declaración de que la jerarquía de dificultad es una predicción *a priori*** · `2.2.5 Taxonomía de ataques y panorama de amenazas` § *2.2.5.2 Gradación de visibilidad en red* · **acotado tras verificación, porque el `.docx` sí publica la gradación en prosa**: el capítulo 2 recorre las cuatro categorías con su «Huella en el tráfico» y ordena de hecho la dificultad —DoS «la más marcada de las cuatro», Probe «relativamente visible», R2L «Aquí empieza la dificultad», U2R «Prácticamente nula»—, incluido el razonamiento que la sostiene para R2L, que «lo que delata al ataque **no está en la forma del flujo, sino en su contenido**»; y el punto ciego de `snmpgetattack` aparece también, aunque más adelante (4.3 «resultan casi invisibles» y 5.3 «punto ciego universal»). Nada de eso se reclama aquí. Lo genuinamente ausente son dos piezas: (i) la **tabla de tres columnas** que cruza las cuatro categorías con **número de conexiones implicadas, naturaleza de la señal y visibilidad por flujo**, que condensa en un solo cuadro lo que la prosa deja repartido en cuatro apartados; y (ii) la **declaración explícita de estatuto epistémico**: que la jerarquía de dificultad **DoS → Probe → R2L → U2R es una predicción *a priori*, no una observación posterior**, derivada de la representación, con su **dicotomía** —reproducirla confirma la hipótesis; contradecirla obligaría a **sospechar de una fuga de información o de un artefacto del conjunto de datos**—. **Verificado: esa jerarquía de dificultad no se enuncia como tal en ninguna sede del `.docx`; el término «jerarquía» aparece dos veces, ambas en §2.3.1 y en otro sentido —los conjuntos anidados IA/ML/DL y la «jerarquía de capas» del aprendizaje profundo—, de modo que no hay ninguna ocurrencia que enuncie esta.** Duele porque el capítulo 5 **se apoya en esa predicción para leer sus resultados** y el capítulo 6 sostiene sobre ella la limitación de las clases minoritarias, pero lo hacen **contra una expectativa que el capítulo 2 insinúa y nunca declara**: la prosa de las huellas describe la dificultad de `r2l` y `u2r`, no la convierte en hipótesis contrastable, así que el resultado del capítulo 5 no puede leerse como **confirmación** de nada. **Aviso para quien lo aplique:** esto **acota** la premisa del hallazgo 4 del capítulo 5 — la dicotomía «confirma / obliga a sospechar» **no está en el `.docx`**, luego aquel hallazgo no puede presentarla como un enunciado publicado que quede sin respuesta; ya se ha reescrito allí en consecuencia · en el capítulo 2, tras «la definición formal del espacio de salida del modelo» (`p0594`) y antes del Título 2 «Inteligencia Artificial» (`p0596`) — **no** «antes de *Vigencia de una taxonomía de 1998*», encabezado que hoy **no existe en el `.docx`** y que solo existiría si antes se aplicara el hallazgo 7. **Orden de aplicación: 6 antes de 7**; aplicado el 7, este apartado queda por delante de aquel. **Filtros (a) y (c).**

7. **`2.2.5.3 Vigencia de una taxonomía de 1998`: por qué es defendible usar una taxonomía de finales de los noventa, con el puente a MITRE ATT&CK y la tabla del equilibrio asumido** · `2.2.5 Taxonomía de ataques y panorama de amenazas` § *2.2.5.3 Vigencia de una taxonomía de 1998* · el `.docx` enuncia el criterio («los exploits caducan, los objetivos no») pero **no lo desarrolla en ningún punto del capítulo 2**, y la única defensa que ofrece llega mucho después, en `4.2`, y en una sola línea de contrapartida. Lo ausente son las **dos razones separadas** con las que el trabajo responde a la objeción de obsolescencia: (i) **lo que caduca son los tipos, no las categorías** —`smurf` inviable desde que los routers dejaron de reenviar tráfico a difusión, `teardrop` y `land` corregidos hace décadas— mientras que las **cuatro conductas** siguen describiendo las fases de una intrusión moderna, con el puente explícito a las tácticas *Reconnaissance*, *Initial Access*, *Privilege Escalation* e *Impact* de **MITRE ATT&CK** y a la *cyber kill chain* (**«MITRE» y «kill chain» tienen cero ocurrencias en el `.docx`**); y (ii) que la limitación es **de representatividad, no de validez conceptual** —las críticas clásicas de McHugh y de Tavallaee *et al.* apuntan a la **distribución** del tráfico y a la fiabilidad de las **métricas absolutas**, no a que la clasificación en cuatro conductas sea incorrecta—. Falta con ellas la **tabla de cuatro filas del equilibrio** a favor / en contra asumido. Duele porque es la objeción más previsible que puede recibir el trabajo en su defensa, y hoy el `.docx` la contesta con una frase de contrapartida sin argumento; y porque el capítulo 6 declara la antigüedad del corpus como limitación **remitiendo a un marco que no está escrito**. *(No confundir con el hallazgo 3 del capítulo 4, que reclama la **cuarta razón de la elección del dataset** en `4.2`: aquello es la elección del **corpus**, esto es la vigencia de la **taxonomía**.)* · en el capítulo 2, cerrando «Taxonomía de ataques y panorama de amenazas», antes de «Inteligencia Artificial». **Filtros (a) y (c).**

8. **`2.3.3.6` y `2.3.3.8`: el deslinde entre límite de representación y sesgo del corpus, y la tabla de síntesis que clasifica los siete límites por dónde reside su causa** · `2.3.3 Límites y consideraciones éticas` §§ *2.3.3.6 Obsolescencia y sesgo de los datos de entrenamiento* y *2.3.3.8 Síntesis* · el `.docx` corta `2.3.3` tras los tres pasos de P10; estos dos apartados no llegaron. Lo ausente es (i) el **deslinde entre dos límites de naturaleza distinta que el `.docx` mezcla**: el **límite de representación** —si dos fenómenos producen vectores indistinguibles, **ningún** algoritmo puede separarlos; no es cuestión de capacidad del modelo ni de cantidad de datos, y corregirlo exige cambiar las **características**, no el clasificador— frente al **límite de sesgo y obsolescencia del corpus** —tráfico de fondo sintético, mezcla de ataques que no refleja frecuencias reales, artefactos de generación que el modelo puede aprender como atajo, más el envejecimiento—; (ii) la **consecuencia ética** que de ahí se sigue, que el sesgo del dato **define qué ataques se detectan y cuáles no** y que un detector entrenado sobre un corpus que sobrerrepresenta ataques ruidosos será ciego ante los sigilosos, **que suelen ser los de mayor impacto**, de donde sale el criterio de **evaluación desagregada por clase y promedio macro** que la memoria sigue; y (iii) la **tabla de síntesis de siete filas** que clasifica cada límite por **dónde reside su causa** y responde a «¿se corrige con un mejor algoritmo?», con la conclusión metodológica de que **ninguno de los siete se resuelve dentro del modelo** y que por tanto la respuesta correcta no es minimizarlos sino **declararlos con su razón técnica**. Duele de forma directa y verificable, **con una remisión colgante del propio `.docx`**: en §6.1.4, al declarar que ninguna precisión publicada está proyectada a una prevalencia operativa, el documento remite a que «el enunciado de marco … está en **2.3.3 (apartado "Alcance de este trabajo")**» — y ese apartado **no existe**: `2.3.3` termina en el tercer paso de P10 («Explicitarlo convierte una vulnerabilidad argumental en un resultado delimitado») y da paso al capítulo siguiente. La remisión prueba que el volcado dejó fuera los apartados finales de `2.3.3`, que son justamente estos. *(**No** se apoya este hallazgo en ninguna frase del `.docx` sobre «el sesgo y la obsolescencia del corpus»: **«obsolescencia» tiene cero ocurrencias en el documento** y esa enumeración es la que el hallazgo 1 del capítulo 6 declara ausente.)* Y la tabla de síntesis es precisamente la plantilla de la que salen los seis límites de 6.1.4 · en el capítulo 2, cerrando «Límites y consideraciones éticas», tras los tres pasos de P10. **Filtros (a) y (c).**

> [!note] Ausencias reales que quedaron fuera del tope de 8
> Todas verificadas por grep contra la extracción; se descartan por umbral o por solape, **no por estar publicadas**:
> - **`2.3.3.4` Explicabilidad, `2.3.3.5` Privacidad y `2.3.3.7` Marco regulatorio** —la tabla interpretable-por-construcción frente a *post-hoc*, la tensión privacidad/detección con los cinco principios del RGPD aplicados al sensor, y la tabla **RGPD · AI Act · NIS2** con el callout que declara que **no se determina la categoría de riesgo**—. **«RGPD», «AI Act», «NIS2», «SHAP» y «supervisión humana» (fuera de una mención suelta en 2.1.2) tienen cero ocurrencias en el `.docx`.** Es el **bloque ético entero del TFG** y su ausencia es la mayor del capítulo en volumen; queda fuera solo porque los tres apartados sostienen menos afirmaciones vivas del resto de la memoria que los ocho de arriba, y porque `2.3.3.4` colisiona parcialmente con lo que el `.docx` ya dice sobre interpretabilidad en 3.5.3 y 4.5. **Si se amplía el tope, este es el primer candidato.**
> - **`2.3.1.3` Por qué este trabajo usa ML clásico y no aprendizaje profundo** (los tres argumentos independientes, los cuatro hechos por los que el autoencoder **no es** una red profunda, y el callout **«El criterio de la herramienta mínima»**): descartado por solape con el **hallazgo 6 del capítulo 3** y porque sus dos fuentes están en **B1**. El `.docx` deja hoy la decisión enunciada como requisito no funcional («sin dependencias pesadas de Deep Learning») y sin justificar.
> - **`2.3.1.2`** (tabla reglas escritas a mano frente a reglas aprendidas), **`2.2.3.1`** (tabla comparativa HIDS/NIDS de seis criterios), **`2.2.3.5`** (tabla de encaje del sistema en los cuatro ejes) y **`2.3.2.1`** (tabla de casos de uso defensivos, que el `.docx` reduce a una lista de cuatro rótulos sin contenido): tablas de apoyo cuya ausencia no rompe ninguna afirmación posterior.

## Capítulo 1

Contrastado contra `Resultados\docx\REPASO_20260826-1908\`. El capítulo 1 del vault son **cinco notas**
(`1.0 Sinopsis`, `1.1 Motivación`, `1.2 Preliminares`, `1.3 Objetivo` y `1.4 Estructura`) y ocupa en el
`.docx` los párrafos `p0225`–`p0299`. El inventario se cierra en **5 hallazgos, muy por debajo del tope
de 8**, y conviene decir por qué, porque el patrón es distinto al de los otros cinco capítulos: aquí no
falta *masa de texto suelta*, sino que el `.docx` conserva una **versión anterior y más pobre** de tres
de las cinco notas. `1.1 Motivación` está volcada casi entera (cambian el estilo y una subordinada, y
además falta una **oración completa**: «Además, estos sistemas permiten relacionar un ataque actual
con incidentes previos, haciendo posible el uso del método de resolución usado anteriormente»,
del párrafo «En cuanto al objetivo de este proyecto…», ausente de `p0236`; queda por debajo del
umbral, pero se deja constancia); `1.0 Sinopsis` se sustituye por un párrafo de contexto propio del `.docx` (`p0227`) que
cumple la misma función, de modo que **no se cuenta como ausencia**; y lo genuinamente perdido se
concentra en `1.3 Objetivo` —que es donde el capítulo 1 hace afirmaciones que el resto de la memoria
usa— y en dos apartados de `1.2` y `1.4`. No se ha rellenado hasta ocho: las diferencias restantes son
de redacción (por ejemplo, que la descripción del capítulo 4 en el `.docx` omite el preprocesamiento y
el entrenamiento de los dos modelos), y como tales quedan fuera del umbral.

**Excluido** todo lo ya registrado en `Las tareas de Kiko.md` por tema: **1.1** (fundir los dos Título 2
consecutivos), **1.2** (sacar de la lista el «Este último punto es precisamente el que motiva…»),
**1.3** (fundir los dos objetivos específicos casi calcados), **6.9** (declarar expresamente si se
cumplió la comparación contra «cada etapa por separado»), **3.6** (la confesión de *data snooping*, que
por eso **no** se reclama como tercera sede en el objetivo específico 4), **A14** (la `A.3 Ficha del
sistema` inexistente, con la que el hallazgo 5 se acota), **B3** (el año 1980 de Anderson) y el
**Anexo D · Capítulo 1** entero (las tres anclas de citas `[12]`, `[13]` y `[14]`). Excluida también,
por **R4**, la **detección basada en el estado**: el `.docx` la trae en `p0250` y su desarrollo se
trasladó a `2.2.2.2`, así que ya no es materia del capítulo 1.

> [!important] Aviso de numeración: Snort no lleva el mismo número en las dos sedes
> La nota `1.2 Preliminares` cita a Roesch como **`[26]`**; el `.docx` lo cita como **`[11]`** en el
> mismo párrafo. Ninguno de los hallazgos de abajo mueve esa cita, pero quien toque el párrafo de Snort
> debe conservar el `[11]` del `.docx` y **no trasladar el `[26]` del vault**.
> **Y no es un riesgo de cita huérfana, sino de cita FALSA:** en la numeración del `.docx`, `[26]` está
> **ocupada** — es *LightGBM* de Ke et al. (verificado en la lista de referencias de la extracción, y
> usada como tal en `p0385` a propósito de HistGradientBoosting). Trasladar el `[26]` del vault a este
> párrafo atribuiría la afirmación sobre Snort a un artículo de *gradient boosting*.

1. **Los nueve objetivos específicos con su criterio de cumplimiento verificable, y la declaración de trazabilidad objetivo → artefacto** · `1.3 Objetivo` § *Objetivos específicos* · el `.docx` imprime **siete** viñetas enunciadas como intenciones («Estudiar los dos métodos principales», «Entrenar los modelos… y validar los modelos entrenados»), **sin un solo criterio de cumplimiento**: la cadena «criterio de cumplimiento» tiene **cero ocurrencias** en todo el documento. Lo ausente es, en bloque: (i) los **nueve** objetivos del vault, de los que el `.docx` no recoge ni el de derivar y **validar** los splits D1/D2/D3 antes de entrenar —integridad, distribuciones, *outliers* y *drift*—, ni el tratamiento del desbalanceo y la **extracción de reglas legibles** de la etapa 2, ni la clase `unknown` como salida legítima al integrar la cascada; (ii) el **criterio verificable** que cierra cada uno, con el capítulo o el apartado donde se comprueba (5.1, 5.2, 5.3, capítulo 4, capítulo 6); y (iii) el párrafo final que declara que **los objetivos 3 a 8 se corresponden uno a uno con un artefacto de código y de resultados**, de modo que su cumplimiento puede verificarse **sin recurrir a la valoración del autor**. Duele porque el capítulo 6 **da por publicados los nueve y promete verificación uno a uno**: `p2392` dice literalmente «Sobre **los nueve objetivos específicos enunciados en 1.3**, la valoración se agrupa en cuatro bloques temáticos. Cada bloque declara qué objetivos numerados cubre, para que la correspondencia con 1.3 sea verificable uno a uno», y a continuación va nombrando «Objetivo 2» … «Objetivo 9» (`p2395`–`p2405`). Pero el `1.3` del `.docx` publica **siete** viñetas **sin numerar** y sin criterio: la correspondencia prometida **no se puede hacer**, la numeración del capítulo 6 remite a una lista que no existe y la declaración de cumplimiento no es falsable, sino que depende del juicio del autor. Es además la pieza que da sentido a la tarea **6.9**, que pide declarar expresamente el cumplimiento de una comparación concreta —sin criterios escritos, no hay contra qué declararla. *(Se deja fuera a propósito la salvedad del objetivo 4 sobre que la **elección del detector** sí se resolvió mirando D2: es el mismo tema que **3.6**, que ordena dejar esa confesión en una sola sede.)* · en el capítulo 1, sustituyendo la lista de «Objetivos específicos» entera. **Filtros (a) y (c).**

2. **La definición operativa de «híbrido» en `1.3`: la tabla de las dos etapas y el razonamiento que fija su orden** · `1.3 Objetivo` § *Objetivo general* · el `.docx` afirma en `p0267` que lo que se quiere demostrar es si «esa composición **en cascada de dos modelos (explicados posteriormente)** aporta algo medible», pero **el antecedente de «esa» no existe**: el párrafo anterior solo enuncia el objetivo general y en ningún punto del capítulo 1 se dice qué es la cascada. Lo ausente es (i) el deslinde de que por *híbrido* se entiende **una arquitectura concreta y no la coexistencia de dos detectores independientes**; (ii) la **tabla de dos filas** que fija, para cada etapa, su paradigma, sus datos de entrenamiento (D1 solo normal / D3 solo ataques conocidos) y su función en la cascada, incluida la salida `unknown`; y (iii) un **puntero explícito** a la sede donde se justifica el orden de la cascada. **Acotación deliberada:** el razonamiento del orden —las firmas no pueden reconocer un 0-day porque no hay firma previa de él; las anomalías sí lo señalan pero no saben *qué* es— **no se restituye aquí**, porque el `.docx` ya lo publica en §2.2.4 (`p0560`) y el **hallazgo 1 del capítulo 3** de este mismo fichero reclama instanciarlo en §3.2.2: escribirlo también en `1.3` lo dejaría en **tres sedes**. En el capítulo 1 va como **remisión**, no como argumento. Duele por dos razones verificables: el aplazamiento «explicados posteriormente» **no se cumple en la sede que el lector espera**, porque el `.docx` publica la justificación del orden en §2.2.4 y la descripción de las etapas en §3.2.1, sin puntero desde aquí; y esa remisión rota agrava el hallazgo 1 del capítulo 3, que ya documenta que §3.2.2 —la sede que instanciaría el argumento— tampoco está volcada. *(La tabla del vault remite a `1.2 Preliminares`, cuyo punto de IDS híbridos es el hallazgo 4 de abajo: ambos se sostienen mutuamente y conviene aplicarlos juntos.)* · en el capítulo 1, en «Objetivo», entre el enunciado del objetivo general y el párrafo «Lo que el trabajo se propone demostrar». **Filtros (a) y (b).**

3. **El callout de alcance: el sistema se construye y se mide *offline* sobre NSL-KDD, sin despliegue en red real ni captura en vivo** · `1.3 Objetivo` § callout `> [!note] Alcance` · **hallazgo acotado**: el `.docx` **sí declara el modo diferido sobre este trabajo**, y en más de un sitio —`p0540` («El sistema construido en este trabajo opera en modo pasivo y en diferido: clasifica registros ya capturados, sin intervenir en el tráfico ni en restricción de latencia»), `p0639` («este sistema se evalúa en diferido y sobre datos ya capturados») y un §3.1.4 «Requisito de alcance» (`p0679`–`p0680`)—, de modo que **no se reclama la declaración de modo diferido**. Lo ausente es (i) que esa acotación **no está en el capítulo 1**, que es la sede donde el lector fija expectativas —«offline» aparece allí cero veces, y su primera aparición, en §2.2.3 (`p0539`), es como eje de una taxonomía general—; (ii) la mención explícita de que la medición se hace **sobre NSL-KDD**, sin despliegue en red real ni captura en vivo; y (iii) la **remisión a `6.2 Líneas futuras`** como sede de lo relativo al despliegue. La restricción sí se aplica más adelante —el modelo de amenaza del capítulo 3 la presupone y el capítulo 6 la usa para acotar sus conclusiones—, pero nunca se enuncia donde el lector fija sus expectativas. Duele porque es la salvaguarda que impide leer el capítulo 5 como una medida de desempeño en producción: sin ella, las cifras de latencia y de flujos por segundo que el propio `.docx` publica invitan justamente a esa sobrelectura, que el capítulo 6 tiene después que desmontar. Sería además la única declaración de alcance **situada en el capítulo 1** · en el capítulo 1, cerrando «Objetivo», tras el párrafo «Lo que el trabajo se propone demostrar». **Filtros (a) y (c).**

4. **El desarrollo de los cinco desafíos abiertos de `1.2`, que el `.docx` reduce a rótulos, y en particular los tres que anuncian decisiones de la memoria** · `1.2 Preliminares` § lista de desafíos («En el siglo XXI estos sistemas se han convertido en un elemento esencial…») · el `.docx` imprime las cinco viñetas como **titulares de una línea o menos**: «Integración con SIEM (Sistemas de Gestión de Información y Eventos de Seguridad).» e «IDS Híbridos y nuevas Tecnologías.» quedan **sin una sola frase de contenido**. Lo ausente no es prosa de relleno, porque tres de los cinco desarrollos **anuncian una decisión que la memoria toma después**: (i) en *volumen de datos*, que el coste computacional es un **requisito de diseño y no un detalle de implementación**, «y es la razón por la que en este trabajo se mide la latencia por flujo y el número de flujos procesados por segundo, además del acierto» —hoy esas columnas aparecen en el capítulo 5 sin que nadie haya dicho por qué se miden—; (ii) en *falsos positivos y negativos*, que ajustar ese equilibrio «es el problema central de la evaluación de un IDS» y que de ahí sale el uso de precisión, exhaustividad y F1 **en lugar de la exactitud global**, que es la decisión métrica que gobierna los capítulos 5 y 6 —**declarando la contraparte**: esa decisión **no está sin justificar en la memoria**, porque `p1393` (§4.3.6) ya la razona por el desbalance («Métrica de selección: f1_macro … con ese reparto, el accuracy "miente"…»); lo ausente es que `1.2` la **anuncie** al lector antes de que aparezca aplicada, no la justificación en sí—; y (iii) en *IDS híbridos*, la premisa de complementariedad —firmas precisas con lo conocido y ciegas ante lo nuevo, anomalías al revés— que es **la única premisa del capítulo 1 que sostiene la frase siguiente**, «Este último punto es precisamente el que motiva el presente trabajo», hoy una conclusión sin antecedente. Se pierden además el porqué de que las firmas sean especialmente sensibles a la evasión y la lectura de que el SIEM convierte la salida del detector en entrada de un proceso de respuesta más amplio. *(Coordinar con la tarea **1.2**, que ordena sacar de la lista la frase «Este último punto…» y convertirla en párrafo de cierre: los dos cambios afectan al mismo tramo y conviene aplicarlos a la vez. **Y también con el `Anexo D · Capítulo 1`** de `Las tareas de Kiko.md`: la viñeta «Evasión del IDS/NIDS – el atacante conoce el mecanismo de detección y lo esquiva» es el **fragmento literal de búsqueda del ancla `[14]`**, de modo que hay **colisión de edición sobre el mismo texto** — quien desarrolle esa viñeta debe dejar el ancla localizable, o el anclado debe ir primero.)* · en el capítulo 1, en la lista de desafíos de «Preliminares», entre `p0254` y `p0261`. **Filtros (a) y (b).**

5. **El párrafo que declara qué papel cumplen los apéndices y por qué la separación es deliberada** · `1.4 Estructura` § párrafo final · **acotado y en tensión con lo publicado**: el `.docx` no omite el párrafo, sino que imprime una versión que **afirma lo contrario** —los apéndices reúnen «datos que harían compleja la lectura del informe, **además de no tener una aportación real** durante la misma»—. Lo ausente es la formulación del vault, que (i) enumera **qué hay** en ellos (columnas del dataset, fórmulas de las métricas de desempeño y ficha del sistema); (ii) declara **el criterio de reparto**: el cuerpo explica **qué se decidió y por qué**, y los valores concretos —rejillas de búsqueda, configuraciones ganadoras por algoritmo, umbrales y el volcado del barrido de semillas— viven en el apéndice; y (iii) afirma que esa separación «mantiene la argumentación legible **sin sacrificar la reproducibilidad, que queda íntegra en el apéndice**». Duele porque el capítulo 5 remite al apéndice para valores que el cuerpo no da, de modo que el «no tener una aportación real» del `.docx` **desautoriza la sede de la que depende la reproducibilidad del trabajo**, que es una de las afirmaciones de rigor del capítulo 6. **Aviso para quien lo aplique:** la enumeración menciona la **ficha del sistema**, que hoy **no existe en el `.docx`** — esto se resuelve con la tarea **A14** y no antes: si A14 decide retirar las llamadas colgantes, este párrafo se restituye **sin** ese tercer elemento · en el capítulo 1, sustituyendo el párrafo final de «Estructura» (`p0299`). **Filtros (a) y (c).**

> [!note] Diferencias reales que quedaron fuera del umbral
> Verificadas contra la extracción; se descartan por menores, **no por estar publicadas**:
> - La **nota sobre la fecha de Snort** de `1.2` (1998 es la de la herramienta; la cita es de LISA'99): aclara una aparente contradicción, pero no sostiene ninguna afirmación posterior. Iría como nota al pie, si acaso.
> - La descripción del **Capítulo 4** en `1.4`, que en el `.docx` se corta tras «el dataset NSL-KDD y su análisis previo» y omite el preprocesamiento que genera las particiones y el entrenamiento de los dos modelos; y la del **Capítulo 6**, que pierde la mención a las limitaciones y a las líneas futuras. Son recortes de enumeración, no de argumento.
> - La **agrupación por bloques temáticos** de los nueve objetivos que hace `1.0 Sinopsis`. Se descarta como *ausencia*, no como *no problema*: no es redundancia con el hallazgo 1, sino **divergencia de agrupación entre dos sedes que se citan mutuamente**. El capítulo 6 organiza el cumplimiento en **cuatro bloques (A–D)** (`p2393`–`p2402`: fundamentos · … · «contraste, medición de lo desconocido y análisis crítico»), mientras `1.0 Sinopsis` usa **cinco** y con cortes distintos (fundamentación y datos · validación de los splits · construcción de las dos etapas · integración y contraste · ataques desconocidos y lectura crítica). Como el `.docx` ya cubre la función introductoria con `p0227` y la sede que **promete** verificabilidad uno a uno es el capítulo 6, lo razonable es que **mande la agrupación A–D del capítulo 6** y que cualquier restitución de `1.0` se alinee con ella; pero **la decisión queda abierta** y debe tomarla quien aplique el hallazgo 1, no este inventario.
