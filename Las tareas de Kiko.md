# Las tareas de Kiko

Revisado el **2026-08-27** contra `Resultados\docx\REPASO_20260827-1854\` (extracción **vigente**: `.docx` con `mtime` 2026-08-27T18:52:30, Word cerrado; **3121 párrafos, 49 tablas, 109 marcadores**, `Errores: ninguno`).

Fichero hermano: `Lo que quedo fuera.md`, inventario de lo que se quedó en el vault. Los cruces se resumen en una frase dentro de cada tarea. **Dato comprobado: en `Lo que quedo fuera.md` no hay ninguna anotación manuscrita tuya** (buscado `??`, `!!`, «no entiendo», «sé más», «Quieres decir», «obviar», «deja esta», «TOTALMENTE»: cero coincidencias). Ahí no hay nada tuyo pendiente de contestar.

> [!warning] Aviso de deriva de `pNNNN`
> Respecto a la extracción anterior (`REPASO_20260827-1812`), todo lo posterior a `p0566` se desplazó **+4** y todo lo posterior a `p2368`, **+5**. Los `pNNNN` que figuraban en la versión anterior de este fichero estaban **mal**. Los de aquí abajo están verificados contra la extracción de las **1854**: úsalos tal cual.

---

## Orden de ejecución recomendado (una sola sentada de Word)

Salta lo menos posible entre capítulos: la mayoría de los errores de la pasada anterior salieron de ir y volver. El orden es **por zona del documento**, no por gravedad.

| # | Zona | Tareas | Nota |
|---|---|---|---|
| 1 | **Lista de referencias** (`p3037`–`p3111`) | **X43** (reescribir `[2]`–`[8]` en IEEE) · **B2** (`[1]`, `[10]`, `[11]`, `[66]`) · **X40 paso 1** (alta de `[75]`, `[76]`, `[77]`) | X40 paso 1 necesita al `researcher`: si no está hecho, **detente aquí y deja X40 para después** |
| 2 | **Capítulo 1** (`p0227`–`p0281`) | **1.1** · **1.2** · **1.3** | 1.3 depende del hallazgo 1 del cap. 1 de `Lo que quedo fuera.md`: decidirlo antes |
| 3 | **Capítulo 2** (`p0300`–`p0664`) | **X44** (`p0342`, riesgo de plagio) · **X40 paso 2** · **2.10** · **2.12** · **2.25** · **2.13** · **2.15** · **2.16** · **2.17** (edición 1 de 2, `p0556`) · **6.4 (nada que hacer aquí)** | 2.16 y 2.25 tocan el mismo tramo `p0644`–`p0664`: hacerlas seguidas. **X44 va primero: es la de mayor riesgo** |
| 4 | **Capítulo 3** (`p0665`–`p0850`) | **3.5** (sacar `p0717`–`p0721`) · **4.11 SEDE** (errata de `p0788`) | **3.6 está REABIERTA**, pero su edición cae en el cap. 4 (`p1234`): aquí, `p0752`, NO se toca |
| 5 | **Capítulo 4** (`p0877`–`p1443`) | **A9** + **5.15** · **4.7** (+ sus **tres** referencias rotas) · **4.8** + **4.10** (misma visita a `p1114`) · **4.9** · **4.16** · **T12** · **3.6** (`p1234`) · **2.17** (edición 2 de 2, `p1315`) · **Anexo C: librerías** y **«Python es el lenguaje de referencia»** | Tras cualquier movimiento, comprobar que la serie de pies `4.1`–`4.13` sigue contigua. **4.12 ya está CERRADA: no toques `p1177`** |
| 6 | **Capítulo 5** (`p1497`–`p2446`) | **5.4** · **5.11** (+ §5.0) + **5.12** · **5.13** + **R10** · **5.5** · **5.18** · **5.21** · **5.22** · **R7** (`p2326`) · **R8** · **X31** · **Anexo C: `≈4·10⁶ flujos/s`** | 5.11/5.12 son la misma visita; 5.13/R10 también |
| 7 | **Capítulo 6** (`p2447`–`p2790`) | **X42** + **A14** (mismo párrafo `p2503`) · **6.4** (`p2509`) · **6.5** · **6.6** · **6.7** · **X45** · **B1** | **6.6 es la más importante de la sección «Mejora»**. **A14 necesita decisión previa tuya** (¿borraste A.3 a propósito?) |
| 8 | **Apéndice** (`p2794`–`p3036`) | **X41** (incluido el `Ttulo7` vacío `p3036`) · **A16** | X41 **antes** que A16: A16 toca los mismos cuatro `Ttulo8` |
| 9 | **Erratas de una palabra** | La tabla de la sección «Erratas de una palabra» | Independientes de todo lo demás; se pueden hacer **de una sentada**, con `Ctrl+*` activado |
| 10 | **Cierre** | `Ctrl+A` y `F9` → «Actualizar toda la tabla» | **Una sola vez, al final de todo.** Es lo que arregla el índice de golpe |

> [!important] Dos cosas antes de empezar
> 1. Activa **«Mostrar todo»** con `Ctrl+*`. Media docena de tareas de este fichero son párrafos y títulos **vacíos**, y sin las marcas de párrafo a la vista son literalmente invisibles.
> 2. La **fila de `[70]` del Anexo C está bloqueada** hasta que ejecutes **X40**. Si pegas `[70]` antes, tendrás dos `[70]` con sentidos distintos en el mismo documento.

---

## HOY · Roto o mal

- [X] **X40** · **Los tres marcadores de `p0567`–`p0569` apuntan a obras EQUIVOCADAS.** `Word` + `researcher`

  **Es el fallo más grave del documento ahora mismo, y lo introdujo la pasada de ayer al pegar los cuatro párrafos de 2.11.** Y es la respuesta a tu pregunta «de las tres citas del vault (68, 69, 70), a cuáles corresponden en el word»: **a ninguna.** Al pegar arrastraste los números del **vault**, y en el `.docx` esos tres números son otras obras. Los tres marcadores nuevos que explican que el documento haya pasado de 106 a 109 marcadores son exactamente estos.

  | Sede | Marcador puesto hoy | A qué obra apunta REALMENTE en el `.docx` | Qué obra se quería citar |
  |---|---|---|---|
  | `p0567` «…entrenado solo sobre D1 **[68]**.» | `[68]` | `p3105` Pineau *et al.*, «Improving Reproducibility in Machine Learning Research», *JMLR* 22(164), 2021 | **Ji *et al.***, IDS multinivel sobre NSL-KDD |
  | `p0568` «…sin condenar en masa el tráfico legítimo **[69]**.» | `[69]` | `p3106` Demšar, «Statistical Comparisons of Classifiers over Multiple Data Sets», *JMLR* 7, 2006 | **Kim, Lee y Kim**, híbrido anomalía + *misuse* |
  | `p0569` «…la puerta dura y secuencial de este trabajo **[70]**.» | `[70]` | `p3107` Dietterich, «Approximate Statistical Tests for Comparing Supervised Classification Learning Algorithms», *Neural Computation* 10(7), 1998 | **Khraisat *et al.*** 2020, *stacking* C5.0 + OC-SVM |

  **Y lo agrava esto:** esos tres números **ya estaban en uso legítimo** en otras sedes del `.docx` — `[68]` en `p1535` y `p2515`, `[69]` en `p1867`. Hoy `[68]` y `[69]` significan **dos cosas distintas dentro del mismo documento**.

  **Arreglo, en tres pasos:**

  1. Al final de la lista de referencias, **después de `[74]` Sharafaldin (`p3111`)**, añadir tres entradas nuevas: `[75]` Ji *et al.*, `[76]` Kim, Lee y Kim, `[77]` Khraisat *et al.* 2020. La lista termina en `[74]`, así que esos tres números están libres y **no renumeran nada**.
  2. En `p0567` cambiar `[68]` → `[75]`; en `p0568` `[69]` → `[76]`; en `p0569` `[70]` → `[77]`. Ctrl+F por «entrenado solo sobre D1», «condenar en masa el tráfico legítimo» y «la puerta dura y secuencial de este trabajo». Marcador **a pelo**, sin localizador.
  3. Comprobar que `[68]` vuelve a tener **solo dos sedes** (`p1535`, `p2515`) y `[69]` **solo una** (`p1867`).

  > [!warning] Este es el ÚNICO punto del fichero que necesita al `researcher`
  > Los datos bibliográficos completos de las tres obras **no los tenemos en formato IEEE**. Están identificadas por autor en `Obsidian_TFG_Vault/99 Investigación/Bibliografia del docx - numeracion y anclas.md` (allí son `[68]`/`[69]`/`[70]` del **vault**), pero la cita hay que montarla. **No se inventan**: el paso 1 no se ejecuta hasta que el `researcher` entregue las tres entradas.

  **Bloqueo derivado:** mientras X40 no esté ejecutada, **la fila de `[70]` del Anexo C no se puede tocar** (ver abajo).

- [X] **X41** · **El apéndice se numera como «Capítulo 7» y el índice está caducado.** `Word`

  Es el resto verificable de A15, y explica por qué el encabezado se portaba mal.

  - `p2794` es un párrafo con estilo `Referencias` que dice «Apéndices», y `p2795` es un **`Ttulo1` que dice solo «Columnas del dataset NSL-KDD»**, sin el rótulo «Apéndice A». Al ser un Título 1 sin más, **hereda numeración de capítulo**: el índice lo lista como «**Capítulo 7. Columnas del dataset NSL-KDD**» (`p0188`), y sus cinco hijos salen con numeración rota — «**.1.1**», «.1.2», «.1.3», «.1.4», «.1.5» (`p0189`–`p0193`) — porque están con estilo `Ttulo8`, que no encaja en ningún nivel coherente.
  - **Incoherencia asociada:** los pies del apéndice sí se llaman «Tabla A.1 / A.2 / A.3» (`p2993`, `p3016`, `p3035`), mientras el capítulo se numera 7. Las dos convenciones conviven en la misma página.
  - **Índice caducado, aparte:** `p0175` dice «Capítulo 6. Conclusiones», pero el Título 1 del cuerpo (`p2447`) dice «**Conclusiones y Líneas Futuras**».

  **Qué hacer, paso a paso:**
  1. En `p2795`, poner el texto «**Apéndice A. Columnas del dataset NSL-KDD**» y aplicarle el estilo de apéndice que **el documento ya tiene**: se llama **`Apndice`** y hoy se usa **una sola vez**, en **`p0198` «Lista de Acrónimos»**. **No hay que crear ningún estilo nuevo:** aplica `Apndice` al `Ttulo1` de `p2795` y con eso sale de la secuencia de capítulos.
  2. Bajar los cinco `Ttulo8` — `p2800`, `p2841`, `p2901`, `p2946` y `p2997` — a un nivel de encabezado normal (Título 2 de apéndice).
  3. **Borrar el segundo encabezado vacío del documento: `p3036`, un `Ttulo7` VACÍO justo antes de «Bibliografía» (`p3037`).** Es el mismo defecto que el `Ttulo3` vacío de 2.25 (`p0664`), y **se borran en la misma pasada**: `Ctrl+*` puesto, localiza «Bibliografía» y borra la marca de párrafo que la precede.
  4. **Al final de toda la sesión**, `Ctrl+A` y `F9` → «Actualizar toda la tabla». Ese paso arregla de golpe el índice, la entrada «Capítulo 7» y la de «Capítulo 6. Conclusiones». **Una sola vez, al final.**

- [ ] **X42** · **Errata literal «Bdos».** `Word`

  Una palabra. En `p2503`: «…ninguna de las **Bdos** corridas implicadas registra el recuento de épocas…» → «las **dos** corridas». Ctrl+F «Bdos» (aparición única). **Mismo párrafo que A14: hazlas juntas.**

- [ ] **X43** · **Las entradas `[2]`–`[8]` no están en IEEE, sino en estilo autor-fecha.** `Word`

  Hallazgo nuevo al leer la lista entera. Son **siete** entradas (`[2]`–`[8]`); el resto, de `[12]` a `[74]`, sí está en IEEE. Literales de hoy:

  | Nº | `pNNNN` | Cómo está hoy |
  |---|---|---|
  | `[2]` | `p3039` | `"Computer Security Threat Monitoring and Surveillance". James P. Anderson Co.` |
  | `[3]` | `p3040` | `Denning, Dorothy E. (1987). "An Intrusion-Detection Model". IEEE Transactions on Software Engineering, SE-13(2), 222-232` |
  | `[4]` | `p3041` | `Chio, C., & Freeman, D. (2018) Machine Learning & Security… O'Reilly Media.` |
  | `[5]` | `p3042` | `Geron, A. (2019). Hands-On Machine Learning… (2nd Ed.). O'Reilly Media.` |
  | `[6]` | `p3043` | `Goodfellow, I., Bengio, Y., & Courville, A. (2016). Deep Learning. MIT Press.` |
  | `[7]` | `p3044` | `Murphy, K. P. (2012). Machine Learning: A Probabilistic Perspective. MIT Press.` |
  | `[8]` | `p3045` | `"Computer Security: Principles and Practice" William Stallings` |

  **Entradas ya redactadas en IEEE, listas para pegar sustituyendo cada una** (son los datos que el proyecto ya tiene fijados; `[8]` Stallings es la **5.ª ed. 2023**, fijada el 2026-08-18):

  - `[2]` J. P. Anderson, *Computer Security Threat Monitoring and Surveillance*, informe técnico. Fort Washington, PA: James P. Anderson Co., feb. 1980.
  - `[3]` D. E. Denning, «An Intrusion-Detection Model», *IEEE Transactions on Software Engineering*, vol. SE-13, n.º 2, pp. 222-232, feb. 1987.
  - `[4]` C. Chio y D. Freeman, *Machine Learning and Security: Protecting Systems with Data and Algorithms*. Sebastopol, CA: O'Reilly Media, 2018.
  - `[5]` A. Géron, *Hands-On Machine Learning with Scikit-Learn, Keras, and TensorFlow*, 2.ª ed. Sebastopol, CA: O'Reilly Media, 2019.
  - `[6]` I. Goodfellow, Y. Bengio y A. Courville, *Deep Learning*. Cambridge, MA: MIT Press, 2016.
  - `[7]` K. P. Murphy, *Machine Learning: A Probabilistic Perspective*. Cambridge, MA: MIT Press, 2012.
  - `[8]` W. Stallings y L. Brown, *Computer Security: Principles and Practice*, 5.ª ed. Hoboken, NJ: Pearson, 2023.

  **Aviso:** `[1]`, `[10]`, `[11]` y `[66]` son **URL desnudas** y las cubre **B2**, no esta ficha. Y al reescribir `[2]`, `[3]` y `[8]` desaparecen de paso tres de las comillas rectas que cuenta **4.16**.

- [ ] **2.25** · **Eliminar el Título 3 vacío que cierra el capítulo 2.** `Word`

  **Respuesta a tu «No entiendo a cuál te refieres»: aquí está el mapa exacto.** Los seis últimos párrafos del capítulo 2, en orden:

  | `pNNNN` | Estilo | Contenido |
  |---|---|---|
  | `p0660` | `Prrafodelista` | **vacío** |
  | `p0661` | `Prrafodelista` | «Explicitarlo convierte una vulnerabilidad argumental en un resultado delimitado…» |
  | `p0662` | `-` | **vacío** |
  | `p0663` | `Cdigofuente` | **vacío** |
  | `p0664` | `Ttulo3` | **VACÍO** ← **este es el que se ve en el índice como una entrada sin nombre** |
  | `p0665` | `Ttulo1` | «Diseño del sistema» |

  **Paso a paso:**
  1. `Ctrl+*` para ver las marcas de párrafo. **Sin esto no se ve nada, y es exactamente lo que no estabas entendiendo: un Título 3 vacío es invisible.**
  2. Ctrl+F «Explicitarlo convierte una vulnerabilidad argumental» y ponte al **final** de ese párrafo.
  3. A partir de ahí, borrar los **tres** párrafos siguientes (`p0662`, `p0663`, `p0664`) hasta que «Diseño del sistema» quede pegado al final del capítulo 2.
  4. Borrar además el vacío `p0660`, que está **antes** de «Explicitarlo».

  > [!note] Hay un SEGUNDO encabezado vacío del mismo tipo — hazlos juntos
  > **`p3036` es un `Ttulo7` vacío justo antes de «Bibliografía» (`p3037`)**. Mismo defecto, misma técnica (`Ctrl+*` y borrar la marca de párrafo). Figura también en **X41** paso 3: con las marcas de párrafo ya a la vista, hazlos en la misma pasada.

- [ ] **5.4** · **Insertar en §5.0 el diccionario de códigos de decisión.** `Word` + `vault`

  **Respuesta a tu «¿la nota representa lo que tengo que meter?»: SÍ. El bloque de abajo es literalmente el párrafo que hay que pegar, tal cual, sin adaptarlo.**

  > Las decisiones de diseño se identifican con un código estable a lo largo de la memoria. **Q1/C** establece que el conjunto de características (54 o 122) no se fija a priori sino por experimento, y quedó resuelto en 54. **H-2** designa el Autoencoder como detector por defecto de la etapa 1. **H-3** designa RandomForest como clasificador de la etapa 2, fijo entre las dos variantes. **H-6** establece que la evaluación multiclase del híbrido se reporta como matriz 5×6 y desglosada por alcance, sin un número único.

  **Sede exacta, verificada:** Título 1 «Evaluación» = `p1497`; el párrafo «Previo a la presentación de las cifras de resultados de los modelos, se presentan las reglas con las que se han producido.» = `p1498`; los rótulos que siguen son «Particiones y su función» (`p1500`), «Elementos congelados» (`p1506`) y «Métricas Obligatorias» (`p1519`). **El párrafo va entre `p1498` y `p1500`.**

  **Cómo meterlo:** como **párrafo normal**, precedido de un rótulo en negrita «**Códigos de las decisiones de diseño**», al mismo nivel visual que «Particiones y su función».

  **Prueba de que la tarea es real:** los códigos se usan y **nunca se definen** — `Q1/C` en `p2320` y `p2482`; `H-2` en `p1673`, `p1679`, `p2461`, `p2480`, `p2641`; `H-3` en `p1802`, `p2481`; `H-5` en `p2254`; `H-6` en `p2222`. Y «código de decisión» / «códigos de decisión» tiene **cero** apariciones en el documento.

  Sede gemela en el vault: `Obsidian_TFG_Vault/05 Evaluación/5.0 Protocolo de evaluación.md`, como apartado nuevo antes de `## 5.0.1 Las particiones y la función de cada una`. `Lo que quedo fuera.md` (cap. 3) excluye 5.4 por nombre: **ningún hallazgo la resuelve**.

- [ ] **2.10** · **Los cuatro párrafos nuevos (`p0567`–`p0570`) no tienen encabezado: cuelgan de la sección anterior.** `Word`

  El bloque de trabajos relacionados que pegaste con 2.11 entró **sin título**, así que el lector lo lee como continuación de lo que hay encima en vez de como apartado propio, y **no aparece en el índice**.

  **Qué hacer, paso a paso:**
  1. `Ctrl+F` por «entrenado solo sobre D1» para plantarte en `p0567`.
  2. Sitúa el cursor **al principio** de `p0567` y crea un párrafo nuevo por encima.
  3. Escribe **`Trabajos relacionados`** y aplícale **Título 4**.

  > [!warning] Título 4, no Título 3
  > Con **Título 3** romperías la jerarquía de `p0572`, que ya es un Título 3 y pasaría a colgar de este en vez de de su sección. **Título 4** lo mete como subapartado sin tocar nada de lo que viene después.

  El índice se arregla solo en el paso 10 del orden de ejecución (`Ctrl+A` → `F9`). No lo actualices ahora.

  **Va junto con X40 paso 2**, que toca esos mismos párrafos: una sola visita a `p0567`.

- [ ] **2.12** · **Fijar el sentido (a) al que apunta el inciso «dentro del sentido (a) fijado más arriba».** `Word`

  **Respuesta a tu «¿?¿?¿?¿ TOTALMENTE NECESARIO?»: sí, pero con matiz, y hay una opción que cuesta una línea.**

  Lo verificado:
  - El inciso «dentro del sentido (a) fijado más arriba» sigue en `p0566`.
  - Lo único que hay **antes** es `p0531`, una sola frase: «Sistemas híbridos: combinan ambos mecanismos dentro de un mismo sistema, buscando que las carencias de uno queden cubiertas por el otro.» — **sin letras ni enumeración**.
  - «sentido (a)» tiene **una única aparición** en todo el documento: la del propio inciso.

  Es decir: **hoy el texto remite a algo que no existe.** Un tribunal que busque el «sentido (a)» no lo encuentra. Por eso es necesario; lo que no es necesario es el coste alto.

  | Opción | Qué implica | Coste |
  |---|---|---|
  | (a) Meter la tabla de los cuatro sentidos de «híbrido» del vault (`2.2.3` → `### El problema del término «híbrido»`) | Cuatro filas + callout | Alto, **y va contra 2.16**, que pide comprimir el capítulo 2 |
  | **(b) RECOMENDADA — una frase** | Ver abajo | Una línea |
  | (c) Borrar cuatro palabras en `p0566`, dejando «es uno de los patrones con más recorrido…» | — | Cero, pero pierde la precisión terminológica que el capítulo presume |

  **Frase a añadir al final de `p0531` (opción b):**

  > En este trabajo, «híbrido» se usa siempre en este sentido (a) —combinación de detección por anomalías y por firmas en un mismo sistema—, y no en los otros tres sentidos habituales del término: (b) híbrido de algoritmos, (c) de regímenes de supervisión y (d) de *ensemble*.

  **Por qué (b):** es lo único que cuesta una línea y a la vez **conserva el argumento**. (a) engorda el capítulo que 2.16 manda adelgazar; (c) resuelve la referencia rota tirando la precisión por la ventana.

- [ ] **4.16** · **Comillas rectas `"` → comillas españolas `«»`.** `Word`

  **Respuesta a tu «¿y por qué apóstrofe crees que es correcto reemplazarla?»: no se ha propuesto tocar ningún apóstrofo.** El cambio es **solo** la comilla doble recta `"` → `«»`. Los apóstrofos se quedan como están.

  > [!warning] Un «Reemplazar todos» de `"` es peligroso aquí
  > Entre los 22 párrafos con comilla recta hay **tres que son código o valores literales**, donde la comilla recta es la correcta y **no se debe tocar**:
  > - `p1457` — flag TCP `S0`
  > - `p1463` — `is_guest_login`
  > - `p2220` — `class_weight='balanced'` (que además es **apóstrofo simple**, no comilla doble)
  >
  > Y **tres que son entradas de la bibliografía** (`p3039`, `p3040`, `p3045`), que las arregla **X43** al reescribirlas en IEEE, no esta tarea.

  **Sedes reales a convertir (16):** `p0242` (comilla **mixta**: abre curva y cierra recta), `p0342`, `p0993`, `p1073`, `p1230`, `p1325`, `p1392`, `p1451`, `p1965`, `p1993`, `p2239`, `p2243`, `p2254`, `p2471`, `p2478`, `p2717`.

  **Cómo:** abrir Buscar, escribir `"` y avanzar **una a una con `F3`**, revisando cada acierto. **No «Reemplazar todos».**

  Dos apuntes: `p1073` conserva además **tres espacios espurios** al principio del párrafo (quítalos de paso), y el caso que da nombre a la tarea sigue ahí: «…permite medir la detección de ataques "0-day" por tipo.»

- [ ] **5.18** · **Tabla 5.14: decisión de dos opciones. Recomendada, terminarla.** `Word`

  > [!important] Antes de obviarla: **ya la tenías montada**
  > Cuando dijiste «vamos a obviar esta tarea» no sabíamos que el esqueleto de la tabla **ya estaba insertado en el documento y estructuralmente completo**. Verificado en `REPASO_20260827-1854` (`tablas.txt` → `TABLA 38 filas=9 celdas_fila1=6 inicio=p2373`): **9 filas × 6 columnas**, con la fila de encabezado entera (`Algoritmo` / `Set` / `Entrenamiento (s)` / `Inferencia (s)` / `Ms/flujo` / `Flujos/s`) y **las ocho filas de datos con su algoritmo y su set ya escritos**, en el orden exacto del bloque de respaldo de abajo: DecisionTree/54, RandomForest/54, KNN/54, HistGradientBoosting/54, DecisionTree/122, RandomForest/122, KNN/122, HistGradientBoosting/122.
  > **Celdas de datos vacías: exactamente 31.** La fila 2 ya tiene su `Entrenamiento (s)` (`p2381` = `1,815`), así que le faltan 3; a las otras siete les faltan 4 cada una → 3 + 28 = 31.
  > **Los 31 valores están en esta misma ficha**, en el bloque plegado. No hay que calcular ni consultar nada: es copiar de una tabla a otra.
  > Por eso **obviarla ahora no es «no hacer trabajo»: es borrar el trabajo que ya hiciste.**

  No tiene pie, y el número **5.14 está libre**: el esqueleto queda entre la Tabla 5.13 (pie `p2357`) y la Tabla 6.1 (pie `p2615`).

  ### (a) RECOMENDADA — terminarla

  Rellenar las **31 celdas** copiando del bloque de respaldo y añadir el pie:

  > Tabla 5.14. Coste temporal e inferencia de los cuatro clasificadores de firmas.

  **Tres motivos:**

  1. **El cuerpo ya está escrito dando la tabla por hecha.** `p2434` empieza «Y la propia fila es incoherente consigo misma…» y `p2371` discute `latencia_ms_por_flujo` y `flujos_por_segundo` **por su nombre de columna**. Ese texto se escribió esperando la tabla; con ella, deja de necesitar parches.
  2. **El trabajo está hecho al 80 %** y los valores están a la vista: son 31 copias, no una tarea de investigación.
  3. **Cierra la única laguna real:** hoy el cuerpo comenta cifras (4.196.350,2 flujos/s, latencia 0,000238 ms) que **ninguna tabla publicada muestra**.

  **Caveat obligatorio al pie** (ya lo enuncia el propio texto en `p2430`): estas dos columnas miden **solo** el `predict`/`score` sobre el vector ya construido y en memoria; **no** incluyen captura del tráfico, ensamblado de los paquetes en flujos ni extracción de las 41 características. Son **techo de inferencia en laboratorio, no capacidad operativa**.

  **Y la incoherencia que confiesa `p2434` se resuelve, no se parchea:** publicar `tiempo_inferencia_s` **con más decimales** (los del CSV), de modo que `Flujos/s` sea reconstruible dividiendo y el aviso de incoherencia se pueda **retirar**. Es la razón por la que (a) además **simplifica** `p2434` en vez de remendarlo.

  ### (b) Obviarla de verdad — y entonces BORRAR el esqueleto entero

  Borrar la tabla completa con su encabezado (`p2373`–`p2428`). Dejar una tabla vacía y sin pie en el capítulo de resultados es **peor que las dos alternativas**. En ese caso siguen siendo **obligatorias** las dos ediciones de prosa:

  1. **`p2434`** («Y la propia fila es incoherente consigo misma: `flujos_por_segundo` se calcula del tiempo sin redondear, mientras que `tiempo_inferencia_s` se publica a tres decimales…») **habla de «la propia fila» sin que haya ninguna fila publicada.**
     - «Y la propia fila es incoherente consigo misma» → «**Y las dos columnas del artefacto son incoherentes entre sí**»
     - «La fila invita a una reconstrucción que no cierra» → «**El artefacto invita a una reconstrucción que no cierra**»
  2. **`p2371`** («…Las columnas `latencia_ms_por_flujo` y `flujos_por_segundo` miden exclusivamente el predict/score…»). Añadir al final:
     > Las cifras se citan en el texto y no se tabulan, por ser techo de laboratorio y no resultado del sistema.
     - Con eso la omisión queda **declarada** en vez de parecer un olvido.

  ### Coste comparado

  **(a) son 31 copias más un pie; (b) es borrar una tabla más dos ediciones de prosa. (a) cuesta poco más que (b) y resuelve la laguna en vez de taparla.**

  **Cruce con el Anexo C:** se mantiene la nota al pie de la fila «≈4·10⁶ flujos/s». Precisión: **si se elige (a), esa nota deja de ser necesaria**, porque la tabla ya respalda la cifra.

  <details><summary>Los 31 valores que hay que copiar</summary>

  Cifras de `Resultados/metricas_firmas.csv`, semilla 42, `n_inferencia` = 9.083 en las ocho filas:

  | Algoritmo | Set | Entren. (s) | Inferencia (s) | ms/flujo | flujos/s |
  |---|---:|---:|---:|---:|---:|
  | DecisionTree | 54 | 1,815 | 0,002 | 0,000238 | 4.196.350,2 |
  | RandomForest | 54 | 61,444 | 0,066 | 0,007232 | 138.276,6 |
  | KNN | 54 | 42,648 | 1,565 | 0,172337 | 5.802,6 |
  | HistGradientBoosting | 54 | 60,745 | 0,029 | 0,003234 | 309.248,5 |
  | DecisionTree | 122 | 2,301 | 0,004 | 0,000391 | 2.556.719,0 |
  | RandomForest | 122 | 51,097 | 0,031 | 0,003366 | 297.079,6 |
  | KNN | 122 | 68,557 | 2,528 | 0,278364 | 3.592,4 |
  | HistGradientBoosting | 122 | 122,82 | 0,098 | 0,0108 | 92.594,4 |

  </details>

- [ ] **T4** · **Pegar los marcadores que faltan recorriendo el Anexo C.** Quedan **5 filas**. `Word`

  **Respuesta a tu «estas citas están ya pegadas!!!»: no lo están, y esta es la prueba.** Verificado en `marcadores.txt` de la extracción de las 1854, que contiene los 109 marcadores del documento: **`[57]`, `[58]`, `[62]` y `[63]` no aparecen ni una sola vez.**

  **Caso aparte, `[70]`:** aparece **una vez, en `p0569`, y está MAL puesta** — es uno de los tres marcadores erróneos que arregla **X40** (ahí `[70]` es Dietterich, cuando se quería citar a Khraisat *et al.*). Es decir: la cita de `[70]` que pide el Anexo C **sigue sin pegarse**, y además hay un `[70]` espurio que hay que repuntar a `[77]`. Por eso esta fila está bloqueada hasta ejecutar X40.

  Lo que sí se pegó ayer fueron los **trece** marcadores de la pasada anterior (`[15]`, `[18]`, `[19]`, `[20]`, `[21]`, `[37]`, `[50]`, `[54]`, `[56]`, `[59]`, `[66]`, `[67]`, `[69]`) — esas filas ya se borraron del anexo, y por eso el recuento bajó — **más los tres de X40, que están mal puestos**. De ahí el 106 → 109.

  Cada marcador va **al final de la frase, antes del punto, y siempre a pelo**.

- [ ] **B2** · **Formato IEEE de las URL desnudas. Son cuatro, no tres.** `Word`

  **Respuesta a tu «deja esta tarea aquí, puesto que no sé cómo hay que poner la bibliografía cuando es un enlace». La regla IEEE es esta:**

  > `Autor/Entidad, *Título del recurso*. [En línea]. Disponible en: <URL> [Consultado: dd-mmm-aaaa].`

  Cinco elementos, en este orden y con esta puntuación: **autor o entidad · título del recurso · tipo de soporte · disponibilidad · fecha de consulta**. Si no hay autor, se empieza por el título entre comillas. Dos cosas no negociables: **la URL nunca va sola**, y **la fecha de consulta es obligatoria**, justamente porque el recurso puede cambiar o desaparecer.

  **Entradas ya montadas para pegar:**
  - `[1]` (`p3038`): "Principales ciberataques en España en 2024", *ChannelPartner*. [En línea]. Disponible en: https://www.channelpartner.es/seguridad/principales-ciberataques-en-espana-en-2024/ [Consultado: 27-ago-2026].
  - `[10]` (`p3047`): Centro Criptológico Nacional, *Glosario de términos y abreviaturas*, CCN-STIC 401. [En línea]. Disponible en: https://www.ccn-cert.cni.es/publico/seriesCCN-STIC/series/400-Guias_Generales/401-glosario_abreviaturas/index.html [Consultado: 27-ago-2026].
  - `[11]` (`p3048`): M. Roesch, "Snort — Lightweight Intrusion Detection for Networks", en *Proc. 13th USENIX LISA*, Seattle, WA, 1999, pp. 229-238.
  - **`[66]` (`p3103`) — NUEVA en esta pasada.** Hoy es solo la URL desnuda. Montada: *sklearn.neighbors.LocalOutlierFactor* — documentación de scikit-learn. [En línea]. Disponible en: https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.LocalOutlierFactor.html [Consultado: 27-ago-2026].

  **Dos precisiones:**
  - **`[11]` es el caso especial:** no es un recurso web, es una **ponencia** (Roesch, USENIX LISA 1999). Se cita como ponencia y la URL se puede omitir — por eso su entrada montada **no lleva «[En línea]»**.
  - **`[64]` (`p3101`, KDD Cup 1999 / UCI) no hace falta tocarla:** ya tiene cita completa más la URL al final.

  > [!note] La parte del `[9]` de esta tarea ya está HECHA
  > La opción (a) se ejecutó íntegra y la contradicción con B1 quedó **cerrada y sin efecto**: `[9]` es hoy `p3046` (Moustafa/UNSW-NB15), la lista acaba en `[74]` Sharafaldin y el cuerpo ya cita `[9]` en `p2746`. Nada que hacer por esta parte.

- [X] **B1** · **`TabArena` se cita por su nombre y no tiene entrada en la bibliografía.** `Word`

  Único resto vivo de B1. Sede verificada **`p2667`**: «El benchmark **TabArena** documenta que algunos modelos aparecen sobrerrepresentados…» — **sin marcador**, y sin entrada en la lista.

  | Opción | Qué implica | Coste |
  |---|---|---|
  | **(a) RECOMENDADA — atenuar** | «**Se ha documentado que** algunos modelos aparecen sobrerrepresentados…», quitando el nombre propio | Cuatro palabras, **cero citas nuevas** |
  | (b) Dar de alta la obra vía `researcher` | Suma otro número al final de la lista, después de los tres de X40 | Una cita nueva + un pase de `researcher` |

  **Por qué (a):** el argumento del párrafo **no depende de qué benchmark lo documentó**. Es una afirmación de contexto, no un dato en disputa.

  **Aviso que se conserva:** las obras huérfanas de `Bibliografía.md` del vault (Barbará, Grinsztajn, McElfresh, Shwartz-Ziv, Hollmann, Bergstra, Storkey, Micci-Barreca, Pargent, Hwang, Behrouz, *Model Cards*, CRISP-DM, Pendlebury) **no están en la lista del `.docx`**: buscarlas en Word no dará resultados.

- [X] **X31** · **Cambiar `[74]` por `[72]` en `p2305`.** Una edición. `Word`

  `p2305` sigue con el número equivocado: «La revisión realizada para este trabajo descartó 14 de las 16 fuentes… e ilustra la magnitud del problema sin medirla. **[74]**».

  Ctrl+F «ilustra la magnitud del problema sin medirla» y sustituir el número del corchete final, **a pelo**. Verificado: **`[72]` = Goldschmidt y Chudá** («Network intrusion datasets: A survey, limitations, and recommendations», `p3109`), que es exactamente de lo que habla la frase; **`[74]` = Sharafaldin** (CIC-IDS2017, `p3111`), correcto en su otra aparición.

- [ ] **A14** · **Dos remisiones a «A.3 Ficha del sistema»: el apéndice EXISTE en el vault, pero no está en el `.docx`.** `Word`

  > [!important] Esta es la ÚNICA tarea del fichero cuya decisión depende de tu memoria
  > **Hay que preguntártelo:** ¿borraste el apéndice «Ficha del sistema» del Word a propósito? De la respuesta depende cuál de las tres opciones de abajo es la correcta.

  El `pNNNN` que traía la ficha era **falso**: el párrafo ya no es `p2447` (hoy `p2447` es el `Ttulo1` «Conclusiones y Líneas Futuras»). Es **`p2503`**, dentro del Título 3 «Límites de lo medible con NSL-KDD» (`p2486`).

  Las dos llamadas, literales:
  - «…y se recoge en **A.3 Ficha del sistema**: esas columnas miden solo el predict/score…»
  - «…(véase **A.3 Ficha del sistema § Dispersión del tiempo_s**, sin causa atribuida)»

  **Dato verificado que cambia la lectura por completo — las remisiones NO son promesas vacías:**

  - `Obsidian_TFG_Vault\Apéndices\A.3 Ficha del sistema.md` **existe y está COMPLETO**: más de 600 líneas, frontmatter `estado: redactada`, secciones `A.3.1`–`A.3.9` (la ficha o *model card*) más `A.3.10` y `A.3.11` (anexo teórico), y **contiene efectivamente el apartado «Dispersión del tiempo_s»** al que remite `p2503`.
  - `Obsidian_TFG_Vault\00 Índice TFG.md` lo marca como «**redactada (📊 volcado, 2026-08-14)**», es decir, **consta como ya volcado al `.docx`**.
  - Y hoy **no está en el `.docx`**.

  **Conclusión honesta: apuntan a un apéndice que existe, está escrito y en algún momento se volcó. Que hoy no esté en el Word significa, con toda probabilidad, que lo borraste a mano y a propósito.**

  **Colisión de rótulo, que sigue en pie:** «A.3» **sí existe** en el apéndice del `.docx`, pero es **otra cosa** — `p3035`, «Tabla A.3. Los 40 tipos de etiqueta agrupados por categoría».

  | Opción | Qué implica | Coste |
  |---|---|---|
  | (a) **Re-volcar `A.3 Ficha del sistema` al `.docx`** | El contenido está listo, **no hay que redactar nada**. Pero **re-volcar es destructivo**: reviviría un apéndice que pudiste retirar deliberadamente. Y arrastra la colisión de numeración: el rótulo `A.3` ya lo ocupa la tabla de `p3035`, así que el apéndice tendría que ser `A.4` o habría que renumerar las tablas | Medio, y **reabre una decisión de contenido** |
  | **(b) RECOMENDADA — borrar las dos remisiones de `p2503`** | Queda «…esas columnas miden solo el predict/score…» y «(sin causa atribuida)» | Dos borrados |
  | (c) **Redirigir las dos remisiones al artefacto** | Dejar «(véase `Resultados/dispersion_semillas.md`, sin causa atribuida)» | Dos ediciones, **cero contenido nuevo, cero referencias rotas** |

  **Por qué (b):** `p2503` **ya contiene en su propio cuerpo todo lo que el apéndice aportaba** — el alcance exacto de las columnas de tiempo y la banda de 4,8× con su fuente citada, `Resultados/dispersion_semillas.md`. La remisión no añade información y sí crea una referencia rota. Y evita reabrir una decisión de contenido que parece que ya tomaste.

  **(c) es la más honesta si no quieres decidir hoy:** conserva el puntero al dato, pero al artefacto en vez de a un apéndice ausente.

  **Cruce: esta tarea toca el mismo párrafo que X42** («Bdos»). Una sola visita a `p2503`.

- [ ] **A16** · **El pie de la Tabla A.1 y el anuncio de la partición.** `Word`

  **La partición ya está hecha.** Verificado: la tabla está partida en **cuatro tablas físicas**, cada una con su encabezado `Ttulo8`:

  | Bloque | Encabezado | `pNNNN` |
  |---|---|---|
  | 1 | Características básicas (1–9) | `p2800` |
  | 2 | Características de contenido (10–22) | `p2841` |
  | 3 | Características de tráfico basadas en tiempo (23–31) | `p2901` |
  | 4 | Características de tráfico basadas en host (32–41) | `p2946` |

  Las cuatro comparten **un único pie**, `p2993`, que hoy dice solo «Tabla A.1. Características del dataset completo NSL-KDD.»

  **Dos ediciones:**
  1. Sustituir `p2993` por: «Tabla A.1. Características del dataset completo NSL-KDD, desglosadas en los cuatro bloques anteriores: básicas (1–9), de contenido (10–22), de tráfico basadas en tiempo (23–31) y de tráfico basadas en host (32–41).»
  2. El párrafo de anuncio es **`p2798`**, y **ya enumera los cuatro bloques conceptualmente** («Los 41 atributos se agrupan tradicionalmente en cuatro bloques: básicos…, de contenido…, de tráfico basados en tiempo…, y de tráfico basados en host…»), pero **no dice que la tabla vaya partida** ni da los rangos. Añadir al final: «La Tabla A.1 se presenta partida en esos cuatro bloques, con los rangos 1–9, 10–22, 23–31 y 32–41.»

  **Cruce: X41 toca esos mismos cuatro `Ttulo8`. Haz X41 primero.**

- [ ] **X44** · **PRIORIDAD ALTA — `p0342` parece una traducción casi literal de Murphy `[7]`, y no lleva cita.** `Word`

  **Es la única tarea de este fichero con riesgo de plagio.** Texto de hoy:

  > «El otro enfoque es el de aprendizaje no supervisado, donde solo se nos proporcionan entradas y el objetivo es encontrar "patrones interesantes" en los datos. Esto a veces se denomina descubrimiento de conocimientos. Se trata de un problema peor definido, ya que no se nos dice qué tipo de patrones debemos buscar y no existe una métrica de error obvia que podamos utilizar (a diferencia del aprendizaje supervisado, en el que podemos comparar nuestra predicción de y para un x dado con el valor observado).»

  **Dos señales convergentes:**
  1. **Rompe el registro impersonal de todo el resto de la memoria**: «se nos proporcionan», «no se nos dice», «podamos utilizar», «nuestra predicción». En ningún otro sitio el trabajo habla en primera persona del plural.
  2. **Reproduce el pasaje de Murphy sobre aprendizaje no supervisado casi frase por frase**, incluidas «patrones interesantes» y «descubrimiento de conocimientos» (*knowledge discovery*).

  **Y no lleva marcador.**

  **Arreglo — las dos cosas, no una:**
  - **Reescribirlo en registro impersonal** («…el objetivo es encontrar patrones de interés en los datos… no se especifica qué tipo de patrones buscar y no existe una métrica de error evidente…»).
  - **Añadir `[7]` al final del párrafo**, a pelo.

  **Caso hermano sin riesgo de plagio: `p0786`** («no disponemos de», «Resumo los puntos clave de esta decisión») tiene el mismo problema de registro en menor grado. Ahí **basta impersonalizar**: «no se dispone de», «Los puntos clave de esta decisión son».

- [ ] **X45** · **`p2717` remite a «esta fuente» sin nombrarla ni citarla.** `Word` (+ `researcher` si hace falta)

  Sede: **`p2717`**, en §6.2.5: «**De esta fuente** se cita únicamente el mecanismo (el bucle de realimentación anomalías → minería de episodios → firma → base de datos). Las cifras de detección que reporta proceden solo de su resumen: el texto completo está tras muro de pago…»

  **Verificado: el párrafo anterior que debería nombrarla, `p2712`, NO lleva ningún marcador `[n]`.** Dice «La literatura ya publicó una respuesta: un H-IDS que no es serie ni paralelo sino un bucle…», sin autor y sin número. **Un lector no puede saber de qué fuente se habla**, y el callout de verificación pendiente queda apuntando al vacío.

  | Opción | Qué implica |
  |---|---|
  | **(a)** Identificar la obra y **ponerle su marcador en `p2712`** | Encargo para el `researcher`, **junto con X40** |
  | (b) Reformular a «De la fuente citada en el párrafo anterior…» | **Solo válido si esa fuente sí queda citada**; hoy no lo está, así que por sí sola no arregla nada |

  **Comprobado el tramo `p2711`–`p2716`: la obra no aparece por nombre en ninguno de los seis párrafos.** Luego hace falta (a).

> [!note] Punto cerrado — la nota `[45]`, resuelta a favor de no tocar nada
> El párrafo cambió de sede: es **`p0597`**, no `p0593`. Dice: «Correspondencia con el espacio de salida del sistema. … La taxonomía no es aquí un adorno teórico, sino la definición formal del espacio de salida del modelo **[45]**.» `[45]` es **Kendall**, *A Database of Computer Attacks…*, MIT 1999 (`p3082`).
> **Veredicto: la cita es defendible y se recomienda dejarla.** La frase habla de la taxonomía DARPA/KDD como definición del espacio de salida, y Kendall es precisamente la tesis que **cataloga esos ataques**.
> *Alternativa anotada, por si algún día se quiere afinar:* el candidato mejor sería **`[43]` Lippmann** (`p3080`), la evaluación DARPA 1998 de la que sale la taxonomía. No es necesario.

---

## Recorte

> [!important] Respuesta única a las doce veces que preguntas «¿debo borrar todas esas frases completamente? Sé más específico»
> **No. En ningún caso se borra la frase entera.** El patrón es siempre el mismo y tiene tres pasos:
> 1. Se elige **una** sede que conserva la explicación completa: la que está en el capítulo donde el argumento «vive».
> 2. En las demás sedes se **sustituye la explicación por la afirmación escueta más una remisión**, del tipo «…(véase §5.3)».
> 3. **La cifra se queda en todas**, porque la cifra es el dato. Lo que se recorta es el **razonamiento repetido**.
>
> Un párrafo nunca desaparece: **encoge**. En cada tarea de abajo se marca con **SEDE** la que se conserva íntegra y con **→ remitir** las que se abrevian. Y donde una sede es una **celda de tabla** o un **pie**, se marca **NO TOCAR**.

> [!important] Plantilla de «abreviado» ya redactada dentro del documento
> Si dudas de cuánto hay que dejar, mira **`p2478`** (§6.1.3), que ya está escrito exactamente en la forma corta que se busca: afirmación + consecuencia, **sin repetir la explicación ni la cifra**:
> > «Enrutado a "desconocido" limitado. De los 0-day que el sistema sí detecta, solo una fracción pequeña acaba correctamente etiquetada como desconocido; el clasificador de firmas, cerrado, tiende a asignar con confianza excesiva una categoría conocida a lo que nunca ha visto.»
>
> **Ese es el molde.** Cuando una tarea diga «→ abreviar», el resultado debe parecerse a `p2478`.
>
> **Y una exención que vale para TODA esta sección: el Resumen (`p0046`) no se recorta nunca.** Un resumen tiene que ser autocontenido, así que sus menciones al percentil 95 (4.11) y al «patrón dominante en la literatura» (R8) **no cuentan como repetición** y no se tocan, aunque alguna ficha las liste como sede.

> [!warning] Exención general de toda esta sección — el Resumen no se recorta NUNCA
> **`p0046` es el Resumen de la memoria**, y menciona tanto el percentil 95 (4.11) como el «patrón dominante en la literatura» (R8). **Un resumen debe ser autocontenido: no cuenta como repetición y no se toca en ninguna tarea de recorte.** Vale al menos para **4.11** y **R8**.

- [ ] **5.21** · El 13,4 % se explica en **ocho** sedes, no siete → dejar la explicación en una. `Word`

  | `pNNNN` | Qué dice | Acción |
  |---|---|---|
  | `p1946` | «El 13,4 % se cita con **dos** salvedades…» (§5.2.4) | → remitir |
  | `p2017` | celda de tabla, solo la cifra | **NO TOCAR** |
  | **`p2029`** | «Este 13,4 % no es un defecto de ejecución, sino el comportamiento que la teoría predice…» (§5.3) | **SEDE — íntegra** |
  | `p2030` | «Lo que el 13,4 % cuantifica es la calidad del etiquetado de lo desconocido…» (§5.3) | **conservar íntegro**, es la continuación de la sede |
  | `p2177` | «…de estos 0-day detectados, solo el 13,4 % termina etiquetado como unknown» (§5.3) | → remitir |
  | `p2324` | «El RandomForest está sobre-confiado ante lo desconocido: … solo el 13,4 % se enruta a unknown…» (§5.4) | → remitir |
  | `p2523` | «Es la explicación teórica del 13,4 % de 0-day detectados…» (cap. 6) | → remitir |
  | `p2669` | «…de los 0-day que la etapa 1 sí detecta, solo el 13,4 % llegan a etiquetarse unknown…» (§6.2.2) | → remitir |

  En las cuatro sedes a remitir (`p2177`, `p2324`, `p2523`, `p2669`) **se deja la cifra y se corta la explicación teórica**, cerrando con «(véase §5.3)».

  > [!important] Plantilla explícita: cómo debe quedar una sede abreviada
  > No hay que inventarse el formato — **`p2478`** (§6.1.3) **ya está redactado exactamente así**, sin repetir la explicación ni la cifra:
  > > «Enrutado a "desconocido" limitado. De los 0-day que el sistema sí detecta, solo una fracción pequeña acaba correctamente etiquetada como desconocido; el clasificador de firmas, cerrado, tiende a asignar con confianza excesiva una categoría conocida a lo que nunca ha visto.»
  >
  > **Usa `p2478` como molde para `p2177`, `p2324`, `p2523` y `p2669`.** Eso es lo que significa «abreviar»: rótulo + afirmación + consecuencia, y la remisión. (En estas cuatro **la cifra sí se conserva**, a diferencia de `p2478`; el molde es el **tono y la longitud**.)

  **Corrección de la propia ficha:** decía «El 13 se cita con **tres** salvedades». El texto real de `p1946` dice **dos**. Era una errata de este fichero, no del `.docx`.

  El recorte va **antes** que los hallazgos 5 y 6 del cap. 5 de `Lo que quedo fuera.md`, que reponen contenido justo ahí.

- [ ] **4.10** · El desbalance 883:1 se desarrolla en **seis** sedes → dejarlo en una. `Word`

  | `pNNNN` | Qué dice | Acción |
  |---|---|---|
  | `p0595` | «…un desbalance de ratio ≈ 883:1 entre DoS y U2R» (cap. 2, viñeta «Huella en el tráfico» de U2R) | → remitir |
  | `p0991` | «Este desbalance extremo (ratio ≈ 883:1 entre DoS y U2R) es lo que motiva el mini-experimento…» (§4.2.1) | → remitir |
  | **`p1114`** | «…la clase mayoritaria (dos) supera a la minoritaria (u2r) en una proporción cercana a 883:1…» (§4.3.4) | **SEDE — íntegra.** Es donde vive el mini-experimento de balanceo |
  | `p1281` | pie de la Tabla 4.8 | **NO TOCAR** — un pie con la cifra es útil |
  | `p1283` | «La Tabla 4.8 recoge la distribución de D3 por categoría de ataque, con un desbalance de ≈ 883:1…» | **fundir con `p1285`** |
  | `p1285` | «La proporción entre la clase mayoritaria y la minoritaria es ≈ 883:1. Este desbalance es el que motiva el mini-experimento de balanceo **de** y la elección sistemática de f1_macro…» | **fundir con `p1283`** |

  **Dos cosas que solo se ven leyendo `p1283` y `p1285` juntos, y que la ficha no decía:**
  - **Son casi el mismo párrafo, consecutivos**, separados solo por un vacío (`p1284`). Ahí el recorte no es abreviar: es **fundir dos en uno**.
  - `p1285` tiene una **errata de redacción**: «el mini-experimento de balanceo **de** y la elección» — sobra el «de». Arréglala de paso.

  **`p1114` es también la sede de 4.8: se hacen en la misma visita.**

- [ ] **4.11** · La justificación del umbral p95 se repite en **tres** sedes de prosa → dejarla en una. `Word`

  | `pNNNN` | Qué dice | Acción |
  |---|---|---|
  | **`p0788`** | «El umbral se fija en el percentil 95 del score de anomalía sobre un subconjunto de validación reservado de D1. Esto equivale a aceptar, por diseño, una 5% de los falsos positivos sobre tráfico normal similar al de entrenamiento.» | **SEDE — íntegra.** Está bajo el Título 3 «**Diseño del umbral de decisión**» (`p0785`), la sección cuyo cometido *es* justificar el umbral. **Lleva errata:** «una 5% de los falsos positivos» → «**un 5 % de falsos positivos**» |
  | `p0556` | «El umbral se fija en el percentil 95 de la puntuación de anomalía sobre validación, lo que equivale a aceptar por diseño en torno a un 5 % de falsas alarmas a cambio de sensibilidad.» (§2.2.4) | → **abreviar** a «con un umbral fijado por percentil (§3.4.3)» |
  | `p1325` | §4.4.1, el protocolo de entrenamiento y validación | **NO ABREVIAR.** Es el **protocolo operativo**, no la justificación: añade el criterio idéntico para los cuatro algoritmos y el «Nunca se ajusta el umbral mirando D2», que es **información propia**. Función distinta, no repetición |

  **O sea: una sola edición de recorte, en `p0556`.** Y esa misma edición es la que pide **2.17** en el mismo párrafo: **se hacen a la vez, en una sola visita a `p0556`**.

  **NO TOCAR** las demás apariciones, que son menciones operativas o pies de tabla: `p0889`, `p1352`, `p1356`, `p1379`, `p1383`, `p1501`, `p1550`, `p1552`, `p1588`, `p1651`, `p1667`, `p2233`, `p2515`, `p2753`.

  **Ojo al orden:** la sede superviviente debe **enlazar** con la explicación que el hallazgo 3 del cap. 5 de `Lo que quedo fuera.md` repone en §5.1 (por qué el p95 promete ≈5 % y se mide 10,2 %). Si ese hallazgo se aplica, añade la remisión cruzada.

  > [!note] Aviso sobre el número de sección
  > El Título 3 «Diseño del umbral de decisión» es `p0785` y está en el **capítulo 3** (§3.4.3), que es a donde debe remitir `p0556`. El del capítulo 4 es «Protocolo de entrenamiento y validación» (`p1317`), que es otra cosa: **no remitas a §4.4.1**.

- [ ] **5.22** · **Recomendación: NO fundir. Una sola edición y se cierra.** `Word`

  **El diagnóstico de la ficha era erróneo: las dos viñetas no están juntas, están en secciones distintas.**

  | `pNNNN` | Sección | Qué dice |
  |---|---|---|
  | **`p2220`** | §5.3 «Resultados del sistema híbrido» | Versión **larga**: recuentos «12 de 2.754 ataques», recall u2r = 0.010, y la frase del «"~99 % de accuracy"». **SEDE** |
  | `p2243` | §5.4 «Conclusiones del capítulo» | Versión **corta**, pero repite las mismas tres cifras con el mismo detalle |

  **Por qué no se funden:** una es **el resultado en su sección** y la otra es **la conclusión del capítulo**. Que la conclusión resuma el resultado es correcto, no duplicación. Lo único que sobra es que `p2243` repita el 0.909→0.472 **y** las dos cifras de recall con el mismo nivel de detalle.

  **Edición única en `p2243`**, dejándolo así:
  > El baseline también falla donde su validación cruzada prometía: su f1_macro se desploma de 0.909 a 0.472 sobre D2, con las clases minoritarias colapsadas (véase §5.3).

- [ ] **6.4** · **NO invertir la dirección del recorte: ya está aplicada, y bien. Una sola edición.** `Word`

  > [!warning] La ficha ordena lo contrario de lo que el documento hace hoy
  > La ficha decía «dejar el desarrollo en el capítulo 6 y reducir el capítulo 2 a una mención». **El documento hace hoy exactamente lo contrario, y lo hace bien:** marco conceptual en el capítulo teórico, instancia concreta en el de conclusiones. **Esa es la estructura correcta en un TFG.** Invertirla obligaría a mover el marco conceptual de capítulo.

  Estado verificado:

  | `pNNNN` | Sección | Qué es |
  |---|---|---|
  | `p0644` + `p0645`–`p0661` | cap. 2, «El modelo de amenaza como requisito metodológico (P10)» | **El marco conceptual**: las seis dimensiones en `p0647`–`p0653` y el razonamiento de tres pasos en `p0657`–`p0661` |
  | `p2507`–`p2511` | cap. 6, límite 3 | **La instancia concreta**, y **ya remite explícitamente al capítulo 2**: `p2511` dice «El marco conceptual —qué es un modelo de amenaza, sus seis dimensiones y por qué un trabajo de detección debe explicitarlo— está en 2.3.3 § El modelo de amenaza como requisito metodológico (P10)…» |
  | `p2612` | §6.2 | Lo repite: «marco conceptual en 2.3.3, enunciado concreto en el límite 3 de este apartado» | 

  **Nada que hacer con las citas:** **`[50]` (Biggio y Roli) tiene una sola aparición en todo el documento, `p2509`**, ya junto al `[49]`. Es exactamente donde la ficha lo quería.

  **La única edición — lo único literalmente duplicado es la frase del adversario estático:**

  - `p0658`: «Medir un detector sobre un conjunto de tráfico fijo equivale a suponer un adversario que no modifica su comportamiento en respuesta a la defensa, es decir, un adversario estático» → **CONSERVAR** (es el marco).
  - `p2509`: «Evaluar sobre un conjunto de tráfico fijo equivale a suponer un adversario estático, que no modifica su comportamiento en respuesta a la defensa» → **dejarlo en**: «Razón técnica. El supuesto de adversario estático (2.3.3) no es verificable sobre un conjunto fijo —el pitfall P10, Inappropriate Threat Model. **[49] [50]**»

  **Cruce con 2.25 y 2.16:** las tres tocan el tramo `p0644`–`p0664`. Hazlas seguidas y en este orden: 6.4 → 2.16 → 2.25.

- [ ] **T12** · No fusionar. **Reconvertir la Tabla 4.7** en tabla de reparto por categoría. `Word`

  **La duplicación está confirmada, columna a columna:**

  | | Tabla 3.1 (pie `p0744`, cuerpo `p0730`–`p0743`) | Tabla 4.7 (pie `p1255`, cuerpo `p1239`–`p1254`) |
  |---|---|---|
  | Columnas | SUBCONJUNTO · CONTENIDO · PROPÓSITO | Split · Contenido · **Tamaño** · Función |
  | D1 | «Solo tráfico normal del entrenamiento» → «Entrenar etapa 1» | «Solo tráfico normal del train» · 67.343 · «Entrenamiento del detector de anomalías…» |
  | D3 | «Solo ataques conocidos del entrenamiento» → «Entrenar etapa 2» | «Solo ataques del train» · 58.630 · «Entrenamiento del clasificador de firmas…» |
  | D2 | «Conjunto de test completo…» → «Evaluar» | «Conjunto de test completo (normal + ataques)» · 22.544 · «Evaluación realista…» |

  Es decir: **las dos son tablas de *función*, con las mismas tres filas.** La 4.7 solo añade la columna «Tamaño».

  **Decisión: conservar la 3.1 como tabla de función y reconvertir la 4.7.** En qué se convierte, columna a columna:

  | Categoría en D2 | n | % sobre D2 (22.544) | % sobre los 12.833 ataques |
  |---|---:|---:|---:|
  | DoS | 7.458 | 33,08 % | 58,12 % |
  | Probe | 2.421 | 10,74 % | 18,86 % |
  | R2L | 2.754 | 12,22 % | 21,46 % |
  | U2R | 200 | 0,89 % | 1,56 % |
  | normal | 9.711 | 43,08 % | — |

  (Los porcentajes son aritmética directa sobre los recuentos del hallazgo 2 del cap. 4 de `Lo que quedo fuera.md`; suman 12.833 los ataques y 22.544 el total. **El doble denominador es el punto de la tabla**: es lo que impide las dos erratas de denominador que ese hallazgo documenta.)

  **Nuevo pie propuesto:** «Tabla 4.7. Reparto de D2 por categoría, con doble denominador: porcentaje sobre D2 completo y sobre el subconjunto de 12.833 ataques. La función de cada subconjunto se recoge en la Tabla 3.1 y no se repite aquí.»

  El texto de anuncio (`p1237`) hay que reescribirlo: hoy dice «La Tabla 4.7 detalla el tamaño y la función experimental de cada uno de los subconjuntos D1, D2 y D3», que dejará de ser verdad.

  > [!warning] Serie `4.x`
  > Cualquier movimiento en el capítulo 4 obliga a comprobar que la serie de pies queda contigua. Hoy va **4.1–4.13 sin huecos**: `p0915`, `p0981`, `p1071`, `p1097`, `p1173`, `p1227`, `p1255`, `p1281`, `p1310`, `p1345`, `p1379`, `p1417`, `p1443`.

- [ ] **3.6** · **REABIERTA — el data snooping se explica tres veces, no dos.** `Word`

  Se dio por cerrada porque el capítulo 6 ya remite en vez de repetir. Pero al cerrarla se miraron **dos** sedes y hay **tres**:

  | `pNNNN` | Sección | Qué dice | Acción |
  |---|---|---|---|
  | `p0752` | §3.3.2 | «Tres decisiones del sistema se tomaron comparando resultados sobre D2 … es decir data snooping —el pitfall P3…» | **SEDE — no tocar.** Es la confesión con `[49]` |
  | **`p1234`** | §4.3.5 | «…la elección del set de características es, formalmente, selección de modelo sobre el test: **data snooping**. Es la tercera de las tres decisiones del sistema tomadas mirando D2 —junto con el detector de la etapa 1 y el clasificador de la etapa 2—…» | → **RECORTAR.** Reexplica el concepto entero y vuelve a enumerar las tres decisiones |
  | `p2483` | §6.x | «Las tres son, formalmente, selección de modelo sobre el conjunto de test, es decir data snooping … **La misma limitación se declara en 3.3.2 y 5.4**…» | **CONSERVAR.** Ya remite en vez de repetir |

  **Qué hacer:** `Ctrl+F` por «es, formalmente, selección de modelo sobre el test» para plantarte en `p1234`. Sustituye la explicación del concepto y la reenumeración de las tres decisiones por una **remisión**, dejando solo lo que es propio de esta sección (que el set de características se eligió mirando D2). Algo como:

  > «La elección del set de características se hizo mirando D2, con la limitación de *data snooping* que se declara en 3.3.2.»

  **Por qué importa:** `p1234` no añade nada que no esté en `p0752`, y sí repite la enumeración completa. Es justo el patrón que esta tarea venía a eliminar — la ficha original hablaba de «las cuatro sedes del cap. 4» y ese cruce se perdió al cerrarla.

- [ ] **2.17** · **La más discutible de esta sección. Recomendación: cerrarla con DOS ediciones.** `Word`

  **La SEDE no es `p0390`, es `p0355`**, porque es donde el término se declara canónico:

  > « Por consecuencia, se trata de un aprendizaje semisupervisado de una sola clase (one-class), y es el término que este trabajo adopta como canónico para los cuatros algoritmos utilizados en esta etapa (IsolationForest, OneClassSVM, LocalOutlierFactor y el autoencoder).»

  **Dos erratas en `p0355`:** «los **cuatros** algoritmos» → «los **cuatro** algoritmos», y **sobra el espacio inicial** del párrafo.

  Las tres sedes que la ficha listaba, verificadas una a una:

  | `pNNNN` | Sección | Qué hace | Acción |
  |---|---|---|---|
  | **`p0355`** | §2.1.4 | **Declara el término canónico** del trabajo | **SEDE — íntegra** (solo las dos erratas) |
  | `p0390` | §2.1.4 | «Estos algoritmos se denominan semisupervisados (one-class): reciben una supervisión parcial… **[28]**» | **CONSERVAR** — define el régimen **frente a «no supervisado»**, que es contenido propio |
  | `p0409` | §2.1.4 | «…se mencionan solo para delimitar por contraste el régimen semisupervisado (one-class) **[17]**.» | **CONSERVAR** — es una **delimitación por contraste**, no una definición repetida |
  | `p0556` | §2.2.4 | «La etapa 1 materializa este paradigma con detectores semisupervisados (one-class) entrenados solo con tráfico normal (D1)…» | → **abreviar. Es la ÚNICA redundante del capítulo 2** |
  | **`p1315`** | §4.4 | **Vuelve a explicar el régimen entero**, no solo a nombrarlo | → **abreviar también. Es sede real, y la ficha se la dejaba** |

  **Recomendación honesta: las tres primeras sedes hacen trabajos distintos** —declarar el término, definirlo frente a lo no supervisado, delimitarlo por contraste— **y las redundantes son DOS: `p0556` y `p1315`.** Cerrar la tarea con esas dos ediciones, en vez de tocar las cinco.

  > [!warning] Corrección respecto a la primera versión de esta ficha
  > Decía «una sola edición, en `p0556`» y **se dejaba fuera `p1315`**. Hay **12 apariciones de «one-class» en disco** y la tabla solo listaba las del capítulo 2. `p1315` (§4.4) no es una mención de paso: reexplica el régimen completo, que es exactamente lo que esta tarea viene a recortar. La tabla de orden de ejecución sigue diciendo «una sola edición, `p0556`» — **son dos visitas: `p0556` en la zona 3 y `p1315` en la zona 5**.

  **`p0556` es el mismo párrafo que abrevia 4.11: las dos ediciones se hacen en la misma visita.**

  **NO CUENTAN** como repetición: `p0388` (encabezado «Algoritmos Semisupervisados de una clase (one-class)»), `p2312` (§5.4, crítica a Hindy *et al.*), y **`p0567`, `p0568` y `p0569`**, que contienen «one-class» por los párrafos nuevos de 2.11 y son **descripción de la literatura ajena**.

  Ojo al hallazgo 3 del cap. 3 de `Lo que quedo fuera.md`, que repone `3.4.5` y sería una sede más.

- [ ] **R7** · **Está ABIERTA: hay TRES sedes, y dos de ellas están en el mismo capítulo.** `Word`

  > [!warning] Corrección de la pasada anterior
  > La versión previa de este fichero la cerraba «sin tocar nada» partiendo de **dos** sedes. Hay **tres**, y la que faltaba (`p2173`) es la que convierte esto en duplicación real: `p2173` y `p2326` **están las dos dentro del capítulo 5** y dicen casi lo mismo.

  | `pNNNN` | Sección | Qué dice | Acción |
  |---|---|---|---|
  | **`p2173`** | §5.3 (`Prrafodelista`) | «snmpgetattack (n = 178) es un punto ciego universal: los cuatro detectores lo fallan (recall máximo 0.152, de IsolationForest), y el resultado se repite en la variante de 122 características. La explicación apunta al límite de la representación por-flujo del NSL-KDD, no a los modelos: se trata de tráfico SNMP cuyas conexiones individuales apenas se distinguen del tráfico normal en las características disponibles. Ningún ajuste de los detectores actuales puede recuperarlo; requeriría características de secuencia o temporales.» | **SEDE — íntegra** |
  | `p2326` | §5.4 | «snmpgetattack es un punto ciego universal (los cuatro detectores lo fallan, en ambos conjuntos de características): un límite de la representación por-flujo del NSL-KDD, no de los modelos, que ningún ajuste de estos puede recuperar.» | → **abreviar** |
  | `p2755` | §6.2.6 | «El tipo de ataque snmpgetattack (n = 178) lo fallan los cuatro detectores en los dos conjuntos de características: el mejor registro es 0,152 (IsolationForest con 54 características) y el detector que el sistema lleva a producción, el autoencoder, lo falla por completo —0,000 en ambos conjuntos» | **NO TOCAR** |

  **Edición única: `p2326`** queda en «snmpgetattack sigue siendo el punto ciego universal de los cuatro detectores (§5.3).»

  **Por qué `p2755` no se toca:** es la **derivación del peldaño de líneas futuras** en §6.2.6 y necesita el `n = 178` y las cifras 0,152 / 0,000 como soporte. Fundirla dejaría la línea futura sin fundamento.

  **NO TOCAR** tampoco las dos apariciones de paso: `p0941` (§4.2.1, la explicación por características) y `p2241` (§5.4, dentro de la lista de tipos con recall 0).

- [ ] **R8** · «patrón dominante en la literatura» en **cuatro** sedes, una exenta → una sola edición real. `Word`

  | `pNNNN` | Sección | Qué dice | Acción |
  |---|---|---|---|
  | `p0046` | **Resumen** | «…un random forest monolítico de cinco clases, representativo del patrón dominante en la literatura sobre este dataset» | **EXENTA — no se toca.** Un resumen es autocontenido (ver el aviso general al inicio de «Recorte») |
  | **`p2181`** | §5.3 | «La comparación central de esta sección —y la tesis del trabajo— enfrenta el sistema híbrido al patrón dominante en la literatura sobre NSL-KDD: un RandomForest monolítico entrenado sobre todo el train (D1+D3, 5 clases, class_weight='balanced') y evaluado sobre D2 completo…» | **SEDE — íntegra.** Es donde se **define** qué es ese patrón |
  | `p2229` | §5.4 | «Este capítulo ha evaluado por separado las dos etapas del sistema y, después, el sistema híbrido completo frente al patrón dominante en la literatura sobre NSL-KDD.» | → **abreviar** a «…frente al baseline monolítico de control (§5.3)» |
  | `p2584` | cap. 6 | «Baseline no trivial: RandomForest monolítico de cinco clases, que además es el patrón dominante en la literatura sobre este dataset (5.3)» | **NO TOCAR** — **ya remite** a 5.3 |

- [ ] **R10** · «Métricas de desempeño» se usa **dos** veces como encabezado, no tres → renombrar las dos. `Word`

  Verificado: solo hay dos encabezados reales con ese rótulo — **`p1597`** (Título 3 de §5.1.2) y **`p1708`** (Título 3 de §5.2.2) — y así sale duplicado en el índice (`p0166` y `p0170`). **El tercero, el de A.2, ya no existe como encabezado.**

  **Nombres propuestos:**
  - `p1597` → «**Métricas de la etapa de anomalías sobre D2**»
  - `p1708` → «**Métricas de la etapa de firmas sobre D3 y D2**»

  **Motivo:** dos entradas idénticas en el índice obligan al lector a mirar el número de página para saber cuál es cuál.

  **Se hace en la misma visita que 5.13**, que renombra los otros dos encabezados de esas mismas secciones.

- [ ] **1.1** · Fundir los Título 2 «Motivación» y «Preliminares», consecutivos. `Word`

  Sedes verificadas: **`p0227`** `Ttulo2` «Motivación» y **`p0238`** `Ttulo2` «Preliminares», con el cuerpo `p0228`–`p0237` entre ambos.

  | Opción | Qué implica |
  |---|---|
  | (a) Borrar el encabezado `p0238` y dejar su contenido corriendo bajo «Motivación» | El índice pierde una entrada y no dice qué hay dentro |
  | **(b) RECOMENDADA** | Renombrar `p0227` a «**Motivación y preliminares**» y borrar `p0238` |

  **Por qué (b):** el índice queda **más honesto sobre lo que hay dentro** de la sección, en vez de esconder los preliminares bajo un rótulo que no los anuncia.

  Cruza con el hallazgo 4 del cap. 1 de `Lo que quedo fuera.md`: **fundir primero, desarrollar los cinco desafíos después.** Y recuerda que el índice se rehace con `Ctrl+A` + `F9` **una sola vez al final**, junto con X41.

- [ ] **1.3** · Fundir dos objetivos específicos que dicen lo mismo. `Word`

  Lista de objetivos transcrita, bajo el Título 2 «Objetivo» (`p0260`):

  | `pNNNN` | Objetivo |
  |---|---|
  | `p0262` | rótulo «Objetivos generales» |
  | `p0264` | «Diseñar, implementar y evaluar un Sistema de Detección de Intrusiones Híbrido en Red (H-NIDS) basado en técnicas de aprendizaje automático, aplicado al conjunto de datos NSL-KDD.» |
  | `p0265` | «Lo que el trabajo se propone demostrar es que si esa composición en cascada de dos modelos (explicados posteriormente) aportan algo medible frente a las alternativas obvias: cada etapa por separado o un clasificador monolítico.» |
  | `p0267` | rótulo «Objetivos específicos» |
  | `p0269` | «Estudiar los dos métodos principales… la Detección de Firmas… y la Detección de Anomalías para ataques 0-day.» |
  | `p0270` | «Revisar la estructura del dataset NSL-KDD…» |
  | `p0272` | «Analizar los posibles algoritmos de ML a utilizar…» |
  | `p0273` | «Entrenar los modelos de ML con los distintos subgrupos… y validar los modelos entrenados.» |
  | **`p0275`** | «Estudiar los resultados obtenidos tras la integración de ambas etapas, **validando los resultados y midiendo la capacidad de detección de ataques desconocidos**.» ← **una de las dos** |
  | `p0276` | «Contrastar el rendimiento del híbrido frente a un baseline monolítico de control.» |
  | **`p0278`** | «**Medir la capacidad de detección de ataques desconocidos y analizar críticamente los resultados** obtenidos.» ← **la otra** |

  Las dos formulaciones a fundir son `p0275` y `p0278`. (`p0271`, `p0274`, `p0277` y `p0279` son párrafos de lista **vacíos**: bórralos de paso.)

  > [!warning] Propuesta de orden: NO la hagas todavía
  > El **hallazgo 1 del cap. 1** de `Lo que quedo fuera.md` **sustituye la lista entera** por nueve objetivos numerados. Si se aplica, 1.3 se absorbe. **Decide ese hallazgo primero:** fundir dos objetivos que van a desaparecer es trabajo perdido.

- [ ] **A9** · La más barata del fichero. Una edición, diez segundos. `Word`

  Sede exacta: **`p2889`**, celda del apéndice: «Comandos salientes en una sesión FTP. Constante a 0 en todo el NSL-KDD (por eso el filtro de varianza la elimina, **véase 4.3 Preprocesamiento de los datasets §4.3.5**).»

  Ctrl+F «véase 4.3 Preprocesamiento» y dejar «**véase §4.3.5**».

  **Cruza con 5.15: es una instancia del mismo problema.** Hazlas juntas.

---

## Mejora

- [ ] **2.13** · Mover el Título 3 «IA, ML y DL» al principio del capítulo 2. `Word`

  Hoy es **`p0605`**, `Ttulo3`, y es la **primera subsección de §2.3 «Inteligencia Artificial»** (`p0599`) — o sea, **al final del capítulo**, que es exactamente el problema: se explica qué es la IA después de haber usado ML durante cuarenta páginas.

  **Destino:** delante del Título 2 «Machine Learning», **`p0300`**.

  **Paso a paso:** seleccionar `p0605` y su cuerpo **hasta antes de `p0612`** («La IA en Ciberseguridad»), cortar, y pegar justo antes de `p0300`.

  **El bloque a mover es `p0605`–`p0611`.**

  > [!warning] Efecto colateral sobre la Figura 2.1 — y la prueba literal
  > La **Figura 2.1 «Relación entre Inteligencia Artificial, Machine Learning y Deep Learning»** (imagen `p0307`, pie `p0308`) vive hoy en **§2.1.1** y es **justamente la figura que ilustra «IA, ML y DL»**.
  > **La prueba de que la figura tiene que acompañar al bloque está en `p0606`**, que dice: «Los tres términos se usan a menudo como sinónimos, pero designan conjuntos anidados: cada uno es un subconjunto estricto del anterior. **La relación ya se ilustró gráficamente en la Figura 2.1.**» Es una remisión **hacia atrás**: si el bloque se mueve al principio del capítulo y la figura se queda en §2.1.1, la frase remitiría a una figura **que aún no ha aparecido**.
  > **Dos salidas:**
  > 1. **RECOMENDADA — mover la figura con el bloque.** Es su ilustración natural, y **sigue siendo la Figura 2.1** porque ya es la primera del capítulo: la numeración no cambia.
  > 2. Dejar la figura donde está y cambiar en `p0606` «ya se ilustró» por «**se ilustra**».

- [ ] **2.15** · Bajar los cinco hijos de «Etapas del Ciclo de vida de un proyecto de ML» de Título 5 a Título 4. `Word`

  El Título 3 es **`p0357`** «Etapas del Ciclo de vida de un proyecto de ML», y sus cinco hijos están con estilo **`Ttulo5`**:

  | `pNNNN` | Encabezado |
  |---|---|
  | `p0358` | Comprensión y recolección de datos |
  | `p0361` | Limpieza y preparación de datos |
  | `p0363` | «` Selección del tipo de modelo`» ← **con un espacio inicial espurio** |
  | `p0366` | Entrenamiento del modelo y ajuste de hiperparámetros |
  | `p0369` | Evaluación del rendimiento |

  **Paso a paso:** seleccionar los cinco (con `Ctrl` para selección múltiple) y aplicarles **Título 4**. Y quitar el espacio inicial de `p0363`.

  **Dato que refuerza la tarea:** en **todo el documento no se usa ningún Título 4**. El salto 3 → 5 es exactamente lo que deja estos cinco encabezados fuera del índice.

- [ ] **2.16** · Comprimir el Título 3 «Límites y consideraciones éticas». `Word`

  Sede: **`p0628`**, y llega hasta **`p0663`** — es decir, **unos 36 párrafos**, más que varias secciones de resultados enteras.

  **Orden obligado, que no cambia:** primero **comprimir P9 y P10**, después restituir lo que pide el hallazgo 8 del cap. 2 de `Lo que quedo fuera.md` (`2.3.3.6`, `2.3.3.8` y el bloque ético `2.3.3.4/5/7`). El saldo de longitud puede quedar neutro, y eso está bien: el problema no es el número de párrafos, es que sobra desarrollo de P9/P10 y falta el bloque ético.

  **Los dos bloques a comprimir:**
  - **`p0645`** en adelante, «El modelo de amenaza como requisito metodológico (P10)».
  - **`p0657`–`p0661`**, el razonamiento de **tres pasos encadenados** («Por qué un trabajo de detección debe explicitarlo»). Es el mismo tramo que visita 6.4: **hazlas juntas**. Pero ojo: **6.4 ya NO comprime aquí** — conserva `p0658` como marco y edita `p2509` en el capítulo 6. Si 2.16 comprime este tramo, **`p0658` debe sobrevivir** a la compresión, porque es la sede del supuesto de adversario estático.

  > [!warning] Conserva el `[49]`
  > `p0653` es la viñeta «Adaptatividad -- ¿Reacciona al defensor? **[49]**». Al comprimir, **el marcador `[49]` se mantiene**: es el único ancla de Arp en ese bloque.

- [ ] **3.5** · Mover la medición contrafactual de la cascada invertida al capítulo 5. `Word`

  **Sede verificada: `p0717`**, y son tres párrafos de prosa más sus vacíos:

  | `pNNNN` | Contenido |
  |---|---|
  | `p0717` | «Para comprobar que el orden de la cascada fijado en §3.2.1 es el adecuado, se ha realizado una medición contrafactual del orden inverso…» |
  | `p0719` | «El resultado es concluyente. … 6.558 de las 9.711 filas legítimas (67,53 %) quedarían condenadas como ataque; en la de 122 características, 3.329 (34,28 %)…» |
  | `p0721` | «Ambas cifras son, además, una cota inferior: las filas que no superan el umbral no quedan exoneradas, sino que se etiquetan como unknown…» |

  **Pertenecen hoy al capítulo 3**, dentro del Título 3 «Diseño de los subconjuntos» / arquitectura en cascada (§3.2.x). Y son un **resultado medido**, no un diseño: por eso desencajan ahí.

  **Destino exacto:** capítulo 5, al **final de §5.3 «Resultados del sistema híbrido»** (Título 2 en `p1958`), después del cierre `p2224` («En conjunto, la comparación confirma la hipótesis que motivó la arquitectura…») y **antes** del Título 2 «Conclusiones del capítulo» (`p2227`). Ahí quedan como el último apartado de resultados del híbrido, que es lo que son.

  **Al mover, deja en el capítulo 3 una frase de remisión** del tipo «El orden de la cascada se justifica además con una medición contrafactual del orden inverso, que se presenta en §5.3.»

- [ ] **4.7** · Reordenar los Título 3 de §4.3 al orden real del pipeline. `Word`

  Orden real hoy:

  | # | `pNNNN` | Título 3 |
  |---|---|---|
  | 1 | `p1038` | Adición y eliminación de etiquetas |
  | 2 | `p1075` | **Normalización** de variables numéricas |
  | 3 | `p1101` | **Codificación** de variables categóricas |
  | 4 | `p1113` | Equilibrio y balanceo de muestras |
  | 5 | `p1181` | Selección de características |
  | 6 | `p1236` | División del conjunto de datos |
  | 7 | `p1287` | Preprocesado sin fuga de datos |

  El orden que pide la tarea (codificación → normalización → selección) exige **una sola permuta: intercambiar `p1075` con `p1101`**, arrastrando sus cuerpos. **Selección ya está detrás de las dos**, así que no hay nada más que mover.

  > [!warning] La permuta ROMPE TRES referencias — checklist de tres Ctrl+F inmediatamente después
  > En todo el `.docx` solo hay **tres** párrafos que citen §4.3.2 o §4.3.3 **por número**, y los tres se rompen al intercambiar `p1075` con `p1101`. Son fáciles de olvidar y dejan el documento remitiendo a la sección equivocada:
  >
  > | ☐ | `pNNNN` | Sección | Dice hoy | Pasa a ser |
  > |---|---|---|---|---|
  > | ☐ | `p1184` | §4.3.5 | «…habría eliminado justo los dummies exclusivos de ataque recuperados por el fix de **4.3.3**…» | **4.3.2** |
  > | ☐ | `p1325` | §4.4.1 | «…por el drift train→test documentado en **4.3.2**…» | **4.3.3** |
  > | ☐ | `p1451` | §4.5.4 | «…los umbrales operan sobre los valores escalados a [0, 1] de **4.3.2**…» | **4.3.3** |
  >
  > **Extra en `p1451`:** tiene un **paréntesis sin cerrar** — «(truncadas a profundidad 5 para legibilidad.» → falta el `)`. Arréglalo en la misma visita.
  >
  > Y el índice hay que rehacerlo (`Ctrl+A`, `F9`) — **una sola vez al final**, con X41.

- [ ] **4.8** · Añadir el aviso de que el eje de balanceo no queda establecido entre semillas. `Word`

  Sede: **`p1114`**, «El conjunto D3, con el que se entrena el clasificador de firmas, está fuertemente desbalanceado: la clase mayoritaria (dos) supera a la minoritaria (u2r) en una proporción cercana a 883:1. Antes de fijar una estrategia de balanceo por defecto, se realizó un mini-experimento controlado…»

  **Frase a añadir al final del párrafo:**
  > El eje SMOTE frente a `class_weight` no queda establecido entre semillas: el ganador por algoritmo depende de la semilla, y la dispersión se documenta en §5.2.4.

  **Misma visita que 4.10**, que tiene su SEDE en este mismo párrafo.

- [ ] **4.9** · La Tabla 4.6 es un resultado del capítulo 5 que vive en el capítulo 4. `Word`

  Sede: pie **`p1227`**, «Tabla 4.6. Experimento H1: comparación del sistema híbrido completo sobre D2 con los conjuntos de 54 y 122 características.»

  | Opción | Qué implica | Coste |
  |---|---|---|
  | (a) Mover la tabla al capítulo 5 | **Renumera 4.7–4.13 a 4.6–4.12** y obliga a perseguir todas las llamadas de la prosa | Alto y arriesgado: **trece pies** y sus llamadas |
  | **(b) RECOMENDADA — marcarla como adelanto** | Añadir al pie: «(resultado del capítulo 5, adelantado aquí para justificar la decisión Q1/C)» | Una frase |

  **Por qué (b):** cuesta una frase y **resuelve la extrañeza** de encontrar un resultado en el capítulo de implementación. (a) mueve trece pies y todas sus llamadas por un beneficio meramente de orden.

- [ ] **5.5** · Sacar del cuerpo las rutas de artefacto → **nota al pie**. `Word`

  **Dos de las tres rutas que listaba la ficha ya NO existen en el `.docx`:** `specialized_nsl_kdd_mappings_and_info.txt` y `metricas_hibrido_0day.csv` tienen **cero apariciones**. Las rutas vivas son:

  | `pNNNN` | Ruta en el cuerpo |
  |---|---|
  | `p1187` | «El detalle completo, con el ranking de las 54 conservadas y el motivo de cada eliminación, está en **Resultados\selected_features.txt**.» |
  | `p2294` | «…el bin_accuracy = 0,8605 de la fila set_features=54 de **Resultados/metricas_hibrido.csv**…» |
  | `p2315` | «Es precisamente lo que mide **Resultados/metricas_anomalias.csv** en este proyecto…» |
  | `p2516` | «…se publica por celda n, media, desviación típica muestral, mínimo y máximo (**Resultados/dispersion_semillas.md**).» |

  **Recomendación: nota al pie, NO apéndice.** Motivo: **A14 está borrando remisiones a un apéndice inexistente**, así que crear un apéndice nuevo para cuatro rutas sería incoherente con la decisión que se acaba de tomar dos capítulos más allá.

  En cada caso: cortar la ruta del cuerpo, dejar la afirmación («…está documentado en el artefacto correspondiente»), e insertar la ruta como nota al pie.

- [ ] **5.11** · §5.3 y §5.4 no tienen **ningún** Título 3 dentro. `Word`

  **Los datos que hacen esto urgente y que la ficha no daba:**

  | Sección | Rango | Extensión | Títulos 3 |
  |---|---|---|---|
  | §5.3 «Resultados del sistema híbrido» | `p1958`–`p2226` | **268 párrafos**, páginas 119→131 del índice | **ninguno** |
  | §5.4 «Conclusiones del capítulo» | `p2227`–`p2446` | **219 párrafos**, páginas 131→143 | **ninguno** |

  Son **las dos secciones más largas del documento** y **las únicas dos de nivel 2 sin subdividir**: en el índice aparecen como **una entrada cada una para doce páginas**.

  **Candidatos a Título 3 en §5.3 — los rótulos ya existen en el cuerpo, solo hay que aplicarles el estilo:**

  | `pNNNN` | Rótulo actual | |
  |---|---|---|
  | `p1961` | Evaluación binaria (normal vs ataque) | **verificado** |
  | `p1965` | Evaluación multiclase — matriz 5×6 con «desconocido» | **verificado** |
  | `p1995` | Calibración del umbral de confianza | |
  | `p2038` | Análisis de ataques desconocidos (0-day) | |
  | `p2179` | Comparación con el baseline RF monolítico | |

  **Candidatos a Título 3 en §5.4 (`p2227`–`p2446`), verificados y en orden:**

  | `pNNNN` | Rótulo actual |
  |---|---|
  | `p2231` | Sobre cada etapa por separado |
  | `p2237` | Sobre el sistema híbrido: la tesis del trabajo |
  | `p2247` | Los caveats que acompañan a la tesis |
  | `p2256` | El sistema frente a la literatura publicada sobre NSL-KDD |
  | `p2318` | Hallazgos que trascienden la comparación 54 vs 122 |
  | `p2330` | Qué significa este FPR a prevalencia realista |
  | `p2370` | Qué NO mide la latencia publicada |

  **Mismo problema en pequeño en §5.0 — agrúpalo aquí, es el mismo capítulo y la misma edición:**

  | `pNNNN` | Rótulo actual | |
  |---|---|---|
  | `p1500` | Particiones y su función | párrafo normal haciendo de rótulo |
  | `p1506` | Elementos congelados | párrafo normal haciendo de rótulo |
  | `p1519` | Métricas **Obligatorias** | ídem, **y con mayúscula intercalada rara** → «Métricas obligatorias» |

  **Paso a paso:** Ctrl+F por cada rótulo, colocarse en el párrafo y aplicar **Título 3**. No hay que escribir nada nuevo: **los once rótulos ya están redactados**, solo llevan estilo de párrafo normal.

  **Dentro de `p2256` hay además una segunda capa de rótulos** (`p2258`, `p2261`, `p2295`, `p2299`, `p2302`, `p2304`, `p2307`, `p2310`) que, si quieres, van a **Título 4** — pero eso es opcional y solo tras 2.15, que es la que introduce el uso del Título 4 en el documento.

  **Esto cierra también 5.12.**

- [ ] **5.12** · Partir §5.4 en «Discusión» y «Conclusiones». `Word`

  **Misma visita que 5.11**: es el mismo rango `p2227`–`p2446`.

  **Corte concreto, verificado por contenido: cae en `p2247`.**

  | Nueva sección | Rango | Contiene |
  |---|---|---|
  | §5.4 **Conclusiones del capítulo** | `p2229`–`p2245` | Balance de resultados: «Sobre cada etapa por separado», «Sobre el sistema híbrido: la tesis del trabajo» |
  | §5.5 **Discusión** | **`p2247`**–`p2446` | «Los caveats que acompañan a la tesis», «El sistema frente a la literatura publicada», «Hallazgos que trascienden la comparación 54 vs 122», «Qué significa este FPR a prevalencia realista», «Qué NO mide la latencia publicada» |

  **Por qué en `p2247` y no en `p2256`:** de `p2229` a `p2245` es **balance de resultados** (conclusión); de `p2247` en adelante es **discusión de caveats, comparación externa y límites**. Los caveats son discusión, no conclusión.

  **Paso a paso:** insertar un `Ttulo2` nuevo, «Discusión», **justo antes de `p2247`**.

  **Nota:** el orden natural sería Discusión → Conclusiones, pero invertirlo obliga a mover 180 párrafos. **Recomiendo el corte tal cual**, dejando las conclusiones delante; la ganancia es que el índice pasa de una entrada de doce páginas a dos secciones con siete Títulos 3 dentro.

- [ ] **5.13** · Unificar el nombre de los dos apartados de cierre. `Word`

  Sedes:
  - **`p1672`** `Ttulo3` «Resultados finales» (cierre de §5.1)
  - **`p1800`** `Ttulo3` «Comparación de los resultados de cada algoritmo» (cierre de §5.2)

  **Par unificado propuesto:**
  - `p1672` → «**Resultados finales de la etapa de anomalías**»
  - `p1800` → «**Resultados finales de la etapa de firmas**»

  **Se hace en la misma visita que R10**, que renombra los **otros dos** encabezados de esas mismas dos secciones (`p1597` y `p1708`). Hacerlas juntas es lo que garantiza que los cuatro rótulos queden en paralelo: «Métricas de la etapa de X» + «Resultados finales de la etapa de X».

- [ ] **5.15** · Unificar las referencias cruzadas, hoy en **tres** formatos. `Word`

  | Formato | Sedes |
  |---|---|
  | «(4.3.5)» | `p1000`, `p1484`, `p2482` |
  | «en 3.3.2 y 5.4» | `p2483` |
  | «§4.3.5» | dentro de la forma larga de `p2889` |

  **Formato único recomendado: `§4.3.5`.**

  **Motivo:** es el que **ya usa la mayoría del documento**, y es el que hace **inequívoco** que se refiere a una sección y no a un número suelto entre paréntesis (que en el capítulo 4, lleno de tablas `4.x`, se confunde con una llamada a tabla).

  **Se hace junto con A9**, que es la misma normalización en `p2889`.

- [ ] **6.5** · Añadir a «Tasa de falsos positivos condicionada por el drift» los dos avisos que le faltan. `Word`

  Sede: **`p2476`**, «Tasa de falsos positivos condicionada por el drift. El detector de anomalías opera a un FPR efectivo del ~10 % sobre el test, por encima del ~5 % de diseño, debido al desplazamiento distribucional… ambas cifras deben leerse juntas.»

  Hoy **no** lleva el aviso de prevalencia ni el de latencia.

  **Frase a añadir al final del párrafo:**
  > Ese FPR debe además leerse a prevalencia realista: proyectado a una prevalencia de ataque del 1 %, más de nueve de cada diez alarmas serían falsas, que es la conclusión clásica de la falacia de la tasa base — el factor limitante de la detección de intrusiones es la tasa de falsos positivos, no la sensibilidad [73]. Y en el mismo sentido, la latencia y el caudal publicados son techo de inferencia en laboratorio, no capacidad operativa: no incluyen captura, ensamblado en flujos ni extracción de características.

  Apoyos verificados: **`[73]` Axelsson** (base-rate fallacy, `p3110`), ya citado en `p2332` y `p2360` — **no es un marcador nuevo**; y la declaración de techo de laboratorio está en `p2430` y `p2432`.

- [ ] **6.6** · **LA MÁS IMPORTANTE DE ESTA SECCIÓN.** El capítulo 6 incumple una regla que el propio capítulo 5 se impone. `Word`

  Sede: **`p2469`** Título 3 «Validación de la tesis del híbrido», con cuerpo en `p2471` y `p2473`.

  **Verificado: ni el 0,8223 ni su banda aparecen en ninguno de los dos.** `p2471` publica 0.771/0.150 y el desplome 0.909→0.472, y nada más.

  **Por qué esto no es una mejora estética, sino una incoherencia interna del documento:** §5.2.4 **se impone explícitamente la regla** de no citar nunca el 0,8223 sin su banda. El capítulo 6 —**el que lee el tribunal**— no la cumple. Un tribunal que lea 5.2.4 y luego 6.1 encuentra al propio trabajo saltándose su propia norma.

  **Material a trasladar, literal de `p1871`/`p1872` (§5.2.4):**
  > El f1_macro de 54-RandomForest que se publica en las tablas anteriores, 0,8223, no es un punto cualquiera del barrido: está por encima del máximo de las diez semillas (0,8205), con media 0,8035. Contando la corrida original hay once ejecuciones y el valor publicado es el más favorable de todas.
  >
  > …siempre que se cite el 0,8223 debe citarse junto a su banda [0,7779–0,8205] y su media 0,8035. Presentar el titular solo sobreestima el rendimiento típico… en unos 1,9 puntos de f1_macro.

  **Qué añadir a `p2471`:** el titular del clasificador de firmas **con su banda desde el primer momento** — «f1_macro = 0,8223, que es el más favorable de once corridas y debe leerse junto a su banda [0,7779–0,8205] y su media 0,8035 (§5.2.4)». **Nunca el 0,8223 solo.**

- [ ] **6.7** · Desdoblar «Clases minoritarias u2r y r2l» en dos limitaciones. `Word`

  Sede: **`p2477`**, «Clases minoritarias u2r y r2l. Con muy pocas muestras de entrenamiento (del orden de decenas para u2r), ningún algoritmo ni técnica de balanceo (SMOTE, class_weight) logra un rendimiento sólido sobre ellas. Es el punto débil universal de la etapa de firmas.»

  **Por qué desdoblar, y este es el argumento de fondo: los dos casos NO tienen la misma causa.**

  | Clase | Causa | Cifras que la sostienen |
  |---|---|---|
  | **u2r** | **Escasez de muestras.** 52 muestras en D3 | recall u2r = 0.010 (`p2220`); soporte ~52 (`p1799`, `p2235`) |
  | **r2l** | **Solape con el tráfico normal** en las características por flujo — no es escasez: hay 2.754 ataques r2l en D2 | recall r2l = 0.004, **12 de 2.754** (`p2220`, `p0595`, `p1285`); la explicación del solape, en `p0941` |

  Meterlas en la misma viñeta hace pensar que a r2l le pasa lo mismo que a u2r, y **no**: r2l tiene datos de sobra y falla igual. Eso es una limitación **distinta**, y además apunta a una línea futura distinta (representación, no muestreo).

- [ ] **1.2** · Sacar de la lista el párrafo de cierre de «Motivación». `Word`

  Sede: **`p0259`**, con estilo `Prrafodelista`: «Este último punto es precisamente el que motiva el presente trabajo.» Está **dentro de una lista**, justo antes del Título 2 «Objetivo» (`p0260`).

  **Paso a paso:** seleccionar `p0259` → Inicio → **desactivar Viñetas** → dejarlo como párrafo normal de cierre. (De paso, `p0258` es un párrafo de lista **vacío**: bórralo.)

  Coordinar con el hallazgo 4 del cap. 1 de `Lo que quedo fuera.md`.

---

## Erratas de una palabra

Todas verificadas en disco contra `REPASO_20260827-1854`, y **ninguna estaba fichada**. Cada una es **un solo golpe de `Ctrl+F`**; se pueden hacer todas de una sentada, sin depender de ninguna otra tarea.

| ☐ | `pNNNN` | Está escrito | Debe decir | Cruce |
|---|---|---|---|---|
| ☐ | `p2503` | «ninguna de las **Bdos** corridas» | «ninguna de las **dos** corridas» | = **X42** |
| ☐ | `p1959` | «**la etapa asigna** a cada sospechoso» | «**la etapa 2 asigna** a cada sospechoso» | — |
| ☐ | `p1285` | «el mini-experimento de balanceo **de** y la elección» | «el mini-experimento de balanceo **y** la elección» | dentro de **4.10** |
| ☐ | `p0788` | «**una 5%** de los falsos positivos» | «**un 5 %** de falsos positivos» | dentro de **4.11** |
| ☐ | `p0355` | «los **cuatros** algoritmos» | «los **cuatro** algoritmos» | dentro de **2.17** |
| ☐ | `p1451` | «(truncadas a profundidad 5 para legibilidad**.**» | falta cerrar el paréntesis: «…legibilidad**).**» | dentro de **4.7** |
| ☐ | `p1392` | «el desbalance descrito en (4.3.6) **u2r supone**» | falta puntuación: «…en (4.3.6)**:** u2r supone» | — |
| ☐ | `p2639` | «Refinamientos sobre el sistema actual» | «**A.** Refinamientos sobre el sistema actual» — el grupo B **sí** lleva su letra (`p2719`, «B. Ampliación del alcance experimental»), el A no | — |
| ☐ | `p1073` | **tres espacios** al principio del párrafo | quitarlos | también en **4.16** |
| ☐ | `p0363` | espacio inicial en « Selección del tipo de modelo» | quitarlo | también en **2.15** |
| ☐ | `p0500` | espacio inicial en « Sistemas de detección de intrusiones» | quitarlo | — |
| ☐ | `p0744`, `p1255` | espacio inicial espurio (los dos son **pies de tabla**: « Tabla 3.1…» y « Tabla 4.7…») | quitarlo | `p1255` también en **T12** |
| ☐ | `p0355` | espacio inicial espurio | quitarlo | misma visita que la errata «cuatros» |

> [!note] Cómo cazar los espacios iniciales
> Con `Ctrl+*` activado se ven de inmediato al principio de la línea. Si prefieres buscar: en «Buscar y reemplazar», con **«Usar caracteres comodín»** activado, `^13 ` (marca de párrafo + espacio) los localiza uno a uno — pero **revisa cada acierto con `F3`**, **no «Reemplazar todos»**, porque el documento tiene sangrías legítimas.

---

# Anexo C · Citas que faltan por pegar

Solo quedan las obras que **no aparecen citadas ni una vez** en el cuerpo del `.docx`. Verificado el 2026-08-27 contra `REPASO_20260827-1854`: el documento tiene **109 marcadores**. Los trece marcadores de la pasada anterior (`[15]`, `[18]`, `[19]`, `[20]`, `[21]`, `[37]`, `[50]`, `[54]`, `[56]`, `[59]`, `[66]`, `[67]`, `[69]`) **ya están pegados** y sus filas se borraron. **Hoy quedan 5 filas** (8 marcadores por pegar), **verificadas como NO pegadas** en `marcadores.txt`: ver **T4**.

## Capítulo 2

| ☐ | Marcador | Obra | Qué hacer |
|---|---|---|---|
| ☐ | `[CITA: Shostack, Threat Modeling: Designing for Security, 2014]` | Shostack | **Ancla verificada: `p0647`** — «Un modelo de amenaza es la especificación explícita de las hipótesis sobre el adversario bajo las cuales se afirma que un sistema cumple su función.» **No lleva marcador.** Confirmado: «Shostack» tiene **cero apariciones** en todo el `.docx` y **no hay entrada suya** en la lista. Ver la decisión abajo. |

**Decisión sobre Shostack — recomendada la (b):**

| Opción | Qué implica | Coste |
|---|---|---|
| (a) Dar de alta la obra | Solo lo puede hacer el `researcher`, y arrastra un número nuevo `[78]` **tras** los tres de X40 | Cita nueva + pase de `researcher` |
| **(b) RECOMENDADA — retirar la fila** | Dejar la afirmación sin cita | Cero |

**Motivo de (b):** es una **definición de marco** que el propio texto desarrolla acto seguido con su enumeración de dimensiones (`p0648`–`p0653`), y **`p0653` ya lleva `[49]` Arp cerrando el bloque**. El coste de (a) es una cita nueva para una frase que **no está en disputa**.

## Capítulo 4

**Fila de librerías — sede exacta localizada.** La tabla es la **Tabla 4.1 «Librerías de Python»**, pie en `p0915`, cuerpo `p0884`–`p0913`, 10 filas × 3 columnas. Estado celda a celda de la columna «Función en el proyecto»:

| Librería | Celda | Marcador hoy | Falta |
|---|---|---|---|
| Numpy | `p0889` | — | **`[57]`** |
| Pandas | `p0892` | — | **`[58]`** |
| Scikit-learn | `p0895` | `[59]` ✓ | — |
| Imbalanced-learn | `p0898` | `[60]` ✓ | — |
| Scipy | `p0901` | `[61]` ✓ | — |
| Matplotlib | `p0904` | — | **`[62]`** |
| Seaborn | `p0907` | — | **`[63]`** |
| Joblib | `p0910` | — | (no lleva) |
| pillow | `p0913` | — | (no lleva) |

**Textos de Ctrl+F, literales:**

| Buscar | Pegar |
|---|---|
| «percentiles para el umbral p95 del detector de anomalías» | `[57]` |
| «manipulación de los conjuntos D1/D2/D3 como DataFrames» | `[58]` |
| «Generación de todas las figuras (EDA, matrices de confusión, curvas ROC/PR)» | `[62]` |
| «Visualizaciones estadísticas sobre matplotlib (distribuciones, mapas de calor)» | `[63]` |

En las cuatro, el marcador va **al final de la celda**, exactamente igual que están ya `[60]` y `[61]`.

**«Python es el lenguaje de referencia» — recomendación: atenuar, no citar.**

Sede: **`p0877`**, «Ecosistema científico maduro: Python es el lenguaje de referencia para el aprendizaje automático, con librerías consolidadas (scikit-learn, pandas, numpy) que cubren todo el pipeline sin necesidad de herramientas externas.» Sin marcador y **sin obra que lo respalde**.

**Cambiar** «es el lenguaje de referencia para» **por** «**es uno de los lenguajes más extendidos en**».

**Coste: cuatro palabras**, y la afirmación pasa de **indefendible** (¿referencia según quién?) a **trivialmente cierta**, que es lo que se necesita en una justificación de herramienta.

**«≈4·10⁶ flujos/s» — recomendación: UNA sola nota al pie, no dos.**

Dos sedes, ambas sin respaldo:
- **`p2432`** — «El valor más alto de la semilla 42 es de 4.196.350,2 flujos/s, con latencia_ms_por_flujo = 0,000238…»
- **`p2434`** — «…el caudal debe citarse con una sola cifra significativa (≈4·10⁶ flujos/s a 54 características y ≈2,6·10⁶ a 122)…»

Verificado: `pies.txt` **no contiene ninguna nota** con «flujos/s» ni «4.196» → **no hay nota al pie hoy**.

**Poner la nota en `p2432`**, que es donde aparece la cifra por primera vez, con este texto:
> Cifras medidas por `Implementacion/app/firmas.py`; artefactos `Resultados/metricas_firmas.csv` y `Resultados/dispersion_semillas.md`, semilla 42.

**Cruce con 5.18:** si se aplica la edición de `p2434` que propone 5.18, **esta nota al pie es lo que sustituye a la tabla que no se va a hacer**. Es el respaldo del dato.

## Capítulo 5

| ☐ | Marcador | Obra | Buscar este texto |
|---|---|---|---|
| ⛔ | `[70]` | Dietterich (en la lista del `.docx`) | **`p1867`**, «Comparación pareada (semilla a semilla): … La variante de 122 características arroja el mismo 8 de 10, con distintas semillas. **[69]**». `[69]` Demšar ya está pegado ahí; falta `[70]` al lado, en la forma « [69] [70]». |

> [!warning] Esta fila está BLOQUEADA por X40
> **No pegues `[70]` hasta ejecutar X40.** Hoy hay otro `[70]` mal puesto en `p0569`, y si pegas este habrá **dos `[70]` con sentidos distintos** en el mismo documento — exactamente el problema que X40 viene a arreglar.

**Notas de aplicación**

1. Cada marcador va **al final de la frase, antes del punto, y siempre a pelo, sin localizador**.
2. `[37]` tenía otra fila anclada en «un weighted alto, porque el peso n_i de la clase mayoritaria domina la suma»; ese fragmento está en el `.docx` con mayúscula inicial («Un weighted alto…»), así que se le dio la sede del capítulo 4 para evitar un fallo de búsqueda. Ya está pegado.
3. `[9]` **ya está resuelto**: ver el bloque `Registro` al final del fichero. No añadas fila para él.

> [!warning] AVISO DE NUMERACIÓN — dos numeraciones distintas conviviendo
> La lista del `.docx` tiene **74 entradas**, `[1]`–`[74]` contigua, y **termina en `[74]` Sharafaldin** (`p3111`). Los **primeros números libres son `[75]`, `[76]` y `[77]`**, que X40 reserva para Ji, Kim y Khraisat.
> `Bibliografía.md` del vault es **otra numeración**: **102 entradas vivas, máximo `[110]`**, con `[9]`, `[27]`, `[28]`, `[37]`, `[40]`, `[44]`, `[45]` y `[76]` **quemadas** y primer libre `[111]`.
> **Un mismo número significa obras distintas en cada sitio.** **Nunca copies un marcador del vault al Word sin traducirlo** por el mapa de `Obsidian_TFG_Vault/99 Investigación/Bibliografia del docx - numeracion y anclas.md`.
> **X40 es exactamente lo que pasa cuando se hace.** Tres marcadores del vault pegados a pelo en el Word, y hoy `[68]` y `[69]` significan dos cosas dentro del mismo documento. Léela antes de pegar nada.

---
